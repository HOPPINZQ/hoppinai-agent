[
  {
    "version": "s01",
    "locale": "zh",
    "title": "s01: The Agent Loop (智能体循环)",
    "content": "# s01: The Agent Loop (智能体循环)\r\n\r\n`[ s01 ] s02 > s03 > s04 > s05 > s06 | s07 > s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"One loop & Tool is all you need\"* -- 一个工具 + 一个循环 = 一个智能体。\r\n\r\n## 问题\r\n\r\n语言模型能推理代码, 但碰不到真实世界 -- 不能读文件、跑测试、看报错。没有循环, 每次工具调用你都得手动把结果粘回去。你自己就是那个循环。\r\n\r\n## 解决方案\r\n\r\n![Agent Loop](/agent/agent-loop.svg)\r\n\r\n```\r\n+--------+      +-------+      +---------+\r\n|  User  | ---> |  LLM  | ---> |  Tool   |\r\n| prompt |      |       |      | execute |\r\n+--------+      +---+---+      +----+----+\r\n                    ^                |\r\n                    |   tool_result  |\r\n                    +----------------+\r\n                    (loop until stop_reason != \"tool_use\")\r\n```\r\n\r\n一个退出条件控制整个流程。循环持续运行, 直到模型不再调用工具。\r\n\r\n## 工作原理\r\n\r\n### 1. 用户输入处理\r\n\r\n将用户的输入添加到消息列表中。在Java中,我们使用`MessageParam`对象来表示消息:\r\n\r\n```java\r\nMessageParam userMessage = MessageParam.builder()\r\n        .role(MessageParam.Role.USER)\r\n        .content(userInput)\r\n        .build();\r\nmessageParams.add(userMessage);\r\n```\r\n\r\n**代码解释**:\r\n- `MessageParam.builder()`: 使用建造者模式创建消息参数\r\n- `role(MessageParam.Role.USER)`: 设置消息角色为用户\r\n- `content(userInput)`: 设置消息内容为用户输入的文本\r\n- `messageParams`: 累积式消息列表,保存整个对话历史\r\n\r\n### 2. 发送请求到LLM\r\n\r\n将消息和工具定义一起发给LLM:\r\n\r\n```java\r\nprotected Message chatMessage(List<MessageParam> messageParams){\r\n    List<ToolUnion> anthropicTools = new ArrayList<>();\r\n    for (ToolDefinition tool : tools) {\r\n        anthropicTools.add(ToolUnion.ofTool(\r\n                Tool.builder()\r\n                        .name(tool.getName())\r\n                        .description(tool.getDescription())\r\n                        .inputSchema(tool.getInputSchema())\r\n                        .build()\r\n        ));\r\n    }\r\n\r\n    MessageCreateParams.Builder messageBuilder = MessageCreateParams.builder()\r\n            .model(model)\r\n            .messages(messageParams)\r\n            .tools(anthropicTools);\r\n\r\n    if (systemPrompt != null && !systemPrompt.isEmpty()) {\r\n        messageBuilder.system(systemPrompt);\r\n    }\r\n    messageBuilder.maxTokens(MAX_TOKENS);\r\n    messageBuilder.temperature(TEMPERATURE);\r\n\r\n    return client.messages().create(messageBuilder.build());\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- `ToolUnion.ofTool()`: 将工具定义转换为Anthropic API所需的格式\r\n- `MessageCreateParams.builder()`: 构建API请求参数\r\n- `client.messages().create()`: 发送请求到Anthropic API\r\n- `systemPrompt`: 可选的系统提示词,用于引导LLM行为\r\n- 返回的`Message`对象包含模型的响应内容和停止原因\r\n\r\n### 3. 处理LLM响应 -- 打印文本 + 判断 stop_reason\r\n\r\n每次 `create` 后做两件事: 打印 assistant 的文本块 (`printText`), 再看 `stop_reason` 决定要不要进工具循环。**官方推荐的 canonical agentic loop 以 `stop_reason == TOOL_USE` 为循环条件**, 而不是遍历 content 块自己累加 `hasToolUse` 标志 -- 因为模型可能输出空 tool_use, 或被 `max_tokens` 截断, `stop_reason` 更可靠。\r\n\r\n```java\r\nMessage message = chatMessage(messageParams);\r\nappendMessage(message.toParam());   // 回灌 + 持久化(若设置了 SessionManager)\r\nprintText(message);                 // 每次都打印,避免纯文本回复被静默吞掉\r\n\r\nwhile (isToolUse(message)) {        // stop_reason == TOOL_USE\r\n    MessageParam toolResultMessage = executeToolCalls(message);\r\n    appendMessage(toolResultMessage);\r\n    message = chatMessage(messageParams);\r\n    appendMessage(message.toParam());\r\n    printText(message);\r\n}\r\nwarnIfTruncated(message);           // 命中 MAX_TOKENS 时打印警告\r\n```\r\n\r\n```java\r\nprivate boolean isToolUse(Message message) {\r\n    return message.stopReason()\r\n            .map(StopReason.TOOL_USE::equals)\r\n            .orElse(false);\r\n}\r\n\r\nprivate void printText(Message message) {\r\n    for (ContentBlock content : message.content()) {\r\n        if (content.isText()) {\r\n            String text = content.text().map(TextBlock::text).orElse(\"\");\r\n            if (!text.isBlank()) {\r\n                System.out.printf(\"AI: %s%n\", text);\r\n            }\r\n        }\r\n    }\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- `message.stopReason()`: 模型给出的停止原因, `TOOL_USE` 表示还要继续工具循环\r\n- `printText()`: 每轮都跑, 即使是无工具调用的纯文本回复也会被打印 (修复了老版本纯文本被吞的 bug)\r\n- `warnIfTruncated()`: `stop_reason == MAX_TOKENS` 时打印警告, 避免工具调用被静默截断导致死循环\r\n- `appendMessage()`: 替代裸 `messageParams.add(...)`, 若设置了 `SessionManager` 会同步把消息落盘\r\n\r\n### 4. 执行工具并返回结果 -- executeToolCalls\r\n\r\n`executeToolCalls` 遍历所有 tool_use 块, 匹配工具名 → `invokeTool` → 收集 `ToolResultBlockParam`, 最后封装成一条 user 角色消息返回 (内部已处理\"工具未找到\"和\"执行抛异常\"两种失败, 都标记 `isError=true` 回灌):\r\n\r\n```java\r\nprivate MessageParam executeToolCalls(Message message) {\r\n    List<ContentBlockParam> toolResults = new ArrayList<>();\r\n    for (ContentBlock content : message.content()) {\r\n        if (!content.isToolUse()) continue;\r\n        ToolUseBlock toolUse = content.asToolUse();\r\n        // ... 匹配 tool, 调 invokeTool, 收集 toolResult / toolError ...\r\n        toolResults.add(ContentBlockParam.ofToolResult(\r\n                ToolResultBlockParam.builder()\r\n                        .toolUseId(toolUse.id())\r\n                        .content(toolError != null ? toolError.getMessage() : toolResult)\r\n                        .isError(toolError != null)\r\n                        .build()\r\n        ));\r\n    }\r\n    return MessageParam.builder()\r\n            .role(MessageParam.Role.USER)\r\n            .content(MessageParam.Content.ofBlockParams(toolResults))\r\n            .build();\r\n}\r\n```\r\n\r\n`invokeTool` 第一个分支的修复值得注意: SDK 的 `JsonValue` 不能直接被 Jackson 序列化 (它是内部包装类型), 老代码 `OBJECT_MAPPER.writeValueAsString(map)` 会出错。新版本先把 `JsonValue` 落到 `JsonNode` 再组装:\r\n\r\n```java\r\nObjectNode root = OBJECT_MAPPER.createObjectNode();\r\nroot.set(\"input\", input.convert(JsonNode.class));\r\nroot.put(\"tool_name\", tool.getName());\r\nreturn tool.getFunction().apply(root.toString());\r\n```\r\n\r\n### 完整的智能体循环\r\n\r\n将上述步骤组装为完整的ZQAgent类:\r\n\r\n```java\r\npublic class ZQAgent {\r\n    protected final AnthropicClient client;\r\n    protected final String model;\r\n    private final List<ToolDefinition> tools;\r\n    protected final List<MessageParam> messageParams = new ArrayList<>();\r\n    private String systemPrompt;\r\n    private SessionManager sessionManager;   // 可选,设置后自动持久化 + 启动恢复\r\n\r\n    public void run() {\r\n        // 启动恢复: 若有历史会话, 把 .sessions/<id>.json 回灌到 messageParams\r\n        if (sessionManager != null) {\r\n            int n = sessionManager.historySize();\r\n            if (n > 0) {\r\n                sessionManager.populate(messageParams);\r\n                System.out.printf(\"已恢复会话 %s,共 %d 条历史消息%n\",\r\n                        sessionManager.getSessionId(), n);\r\n            }\r\n        }\r\n        System.out.println(\"开始对话吧\");\r\n\r\n        while (true) {\r\n            System.out.print(\"你: \");\r\n            String userInput = scanner.nextLine();\r\n            if (userInput.isEmpty()) continue;\r\n\r\n            MessageParam userMessage = MessageParam.builder()\r\n                    .role(MessageParam.Role.USER)\r\n                    .content(userInput)\r\n                    .build();\r\n            appendMessage(userMessage);\r\n\r\n            Message message = chatMessage(messageParams);\r\n            appendMessage(message.toParam());\r\n            printText(message);\r\n\r\n            while (isToolUse(message)) {        // stop_reason == TOOL_USE\r\n                appendMessage(executeToolCalls(message));\r\n                message = chatMessage(messageParams);\r\n                appendMessage(message.toParam());\r\n                printText(message);\r\n            }\r\n            warnIfTruncated(message);\r\n        }\r\n    }\r\n\r\n    // appendMessage 替代裸 messageParams.add -- 设置了 sessionManager 就同步落盘\r\n    protected void appendMessage(MessageParam param) {\r\n        messageParams.add(param);\r\n        if (sessionManager != null) {\r\n            sessionManager.onMessageAppended(param);\r\n        }\r\n    }\r\n}\r\n```\r\n\r\n**核心要点**:\r\n- **外层循环**: 处理用户对话,持续运行直到程序退出\r\n- **内层循环**: 以 `stop_reason == TOOL_USE` 为条件, 持续运行直到模型不再调用工具\r\n- **消息累积**: `messageParams`保存整个对话历史,包括用户消息、AI响应和工具结果\r\n- **退出条件**: 当 `stop_reason != TOOL_USE` 时退出内层循环 (空 tool_use / max_tokens 截断都能正确处理)\r\n- **会话持久化**: `appendMessage` 把每条消息同步写到 `.sessions/<sessionId>.json`, 重启后可恢复\r\n\r\nAgent01 使用**组合模式** -- 不继承ZQAgent, 而是在main方法中创建实例:\r\n\r\n```java\r\npublic class Agent01 {\r\n    public static void main(String[] args) {\r\n        AnthropicClient client = AnthropicOkHttpClient.builder()\r\n                .apiKey(API_KEY).baseUrl(BASE_URL).build();\r\n\r\n        List<ToolDefinition> tools = new ArrayList<>();\r\n        tools.add(BashDefinition);\r\n\r\n        ZQAgent agent = new ZQAgent(client, MODEL, tools);\r\n        agent.setSystemPrompt(buildSystemPrompt());\r\n        agent.setSessionManager(bootstrapSession(args));   // 启动时恢复历史会话或开新会话\r\n        agent.run();\r\n    }\r\n}\r\n```\r\n\r\n不到200行,这就是整个智能体。后面 20+ 个章节都在这个循环上叠加机制 -- 循环本身始终不变。\r\n\r\n## 变更内容\r\n\r\n| 组件 | 之前 | 之后 |\r\n|-----|------|------|\r\n| Agent loop | (无) | 外层 `while(true)` + 内层 `stop_reason == TOOL_USE` 驱动 |\r\n| Tools | (无) | `bash` (单一工具) |\r\n| Messages | (无) | `List<MessageParam>`累积式消息列表 + `appendMessage` 同步落盘 |\r\n| Control flow | (无) | `stop_reason != TOOL_USE` 退出条件 (官方 canonical 实现) |\r\n| Session | (无) | `.sessions/<id>.json` 自动持久化 + 启动恢复 |\r\n| 类型安全 | 动态类型 | 静态类型,编译时检查 |\r\n\r\n## 试一试\r\n\r\n```bash\r\n# 编译项目\r\nmvn clean install\r\n\r\n# 运行示例智能体\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent01\"\r\n```\r\n\r\n试试这些prompt (英文prompt对LLM效果更好,也可以用中文):\r\n\r\n1. `Create a file called hello.java that prints \"Hello, World!\"`\r\n2. `List all Java files in this directory`\r\n3. `What is the current git branch?`\r\n4. `Create a directory called test_output and write 3 files in it`\r\n5. `查看当前系统时间`\r\n6. `创建一个Python脚本,计算斐波那契数列前10项`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s02",
    "locale": "zh",
    "title": "s02: Tool Use (工具使用)",
    "content": "# s02: Tool Use (工具使用)\r\n\r\n`s01 > [ s02 ] s03 > s04 > s05 > s06 | s07 > s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"加一个工具, 只加一个 handler\"* -- 循环不用动, 新工具注册进 dispatch map 就行。\r\n\r\n## 问题\r\n\r\n只有 `bash` 时, 所有操作都走 shell。`cat` 截断不可预测, `sed` 遇到特殊字符就崩, 每次 bash 调用都是不受约束的安全面。专用工具 (`read_file`, `write_file`) 可以在工具层面做路径沙箱。\r\n\r\n关键洞察: 加工具不需要改循环。\r\n\r\n## 解决方案\r\n\r\n![Tool Dispatch](/agent/tool-dispatch.svg)\r\n\r\n```\r\n+--------+      +-------+      +----------------------+\r\n|  User  | ---> |  LLM  | ---> | Tool Dispatch        |\r\n| prompt |      |       |      | {                    |\r\n+--------+      +---+---+      |   bash:       bash   |\r\n                    ^           |   read_file:  read   |\r\n                    |           |   write_file: write  |\r\n                    +-----------+   edit_file:  edit   |\r\n                    tool_result |   list_files: list   |\r\n                                |   content_search:   |\r\n                                |                   } |\r\n                                +----------------------+\r\n\r\nThe dispatch map is a dict: {tool_name: handler_function}.\r\nOne lookup replaces any if/elif chain.\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. 工具定义 -- 每个工具 = Schema + Handler\r\n\r\nAgent02 引入了 `ToolDefinition` 类, 将工具的 Schema 描述和执行函数封装在一起。每个工具由五个要素组成:\r\n\r\n```java\r\npublic class ToolDefinition {\r\n    private String name;          // 工具名称\r\n    private String description;   // 工具描述 (LLM 用来理解工具用途)\r\n    private Tool.InputSchema inputSchema;  // JSON Schema 参数定义\r\n    private Class<?> type;        // 参数类型 (用于反序列化)\r\n    private Function<String, String> function;  // 处理函数\r\n    private TypedToolInvoker typedInvoker;       // 类型化调用器\r\n}\r\n```\r\n\r\n以 `read_file` 为例:\r\n\r\n```java\r\npublic static ToolDefinition ReadFileDefinition = new ToolDefinition(\r\n        \"read_file\",\r\n        \"读取指定相对文件路径的内容。适用于需要查看文件内容的场景，支持查看各种类型文件的完整内容。\",\r\n        createInputSchema(\r\n                Map.of(\"path\", createProperty(\"string\", \"工作目录中文件的相对路径。\")),\r\n                List.of(\"path\")    // required 字段\r\n        ),\r\n        ReadFileInput.class,      // 参数类型\r\n        Tools::readFile           // 方法引用作为 handler\r\n);\r\n```\r\n\r\n**代码解释**:\r\n- `createInputSchema()`: 辅助方法, 将 `Map<String, Object>` 转换为 Anthropic API 所需的 `Tool.InputSchema`\r\n- `createProperty()`: 辅助方法, 创建 `{type, description}` 属性定义\r\n- `ReadFileInput.class`: 告诉框架如何将 JSON 反序列化为 Java 对象\r\n- `Tools::readFile`: 方法引用, 等价于 `input -> Tools.readFile(input)`\r\n\r\n### 2. 注册 6 个工具\r\n\r\nAgent02 在 `main()` 中注册了 6 个工具 -- 相比 Agent01 的 1 个, 扩展了文件操作能力:\r\n\r\n```java\r\nList<ToolDefinition> tools = new ArrayList<>();\r\ntools.add(BashDefinition);          // bash - 执行 Shell 命令\r\ntools.add(EditFileDefinition);      // edit_file - 编辑文本文件\r\ntools.add(WriteFileDefinition);     // write_file - 写入文件\r\ntools.add(ReadFileDefinition);      // read_file - 读取文件\r\ntools.add(ListFilesDefinition);     // list_files - 列出文件和目录\r\ntools.add(ContentSearchDefinition); // content_search - 搜索代码内容\r\n```\r\n\r\n每个工具的参数 Schema:\r\n\r\n| 工具 | 必需参数 | 可选参数 | 说明 |\r\n|------|---------|---------|------|\r\n| `bash` | `command`, `type` | -- | `type` 可选 cmd/powershell/bash |\r\n| `read_file` | `path` | -- | 读取文件完整内容 |\r\n| `write_file` | `path`, `content` | -- | 写入文件, 不存在则创建 |\r\n| `edit_file` | -- | `path`, `oldStr`, `newStr` | 替换文件中的文本片段 |\r\n| `list_files` | -- | `path`, `fileType` | 列出目录内容, 支持按扩展名过滤 |\r\n| `content_search` | `pattern` | `path`, `fileType`, `caseSensitive` | 基于 ripgrep 的正则搜索 |\r\n\r\n**注意**: `edit_file` 和 `list_files` 没有必需参数 -- LLM 会根据上下文自行决定传哪些参数。\r\n\r\n### 3. 三路分派 -- invoke()\r\n\r\nAgent02 的核心创新是 `invoke()` 方法, 支持三种调用模式:\r\n\r\n```java\r\npublic String invoke(Object convertedInput) throws Exception {\r\n    // 路径 1: 类型化调用器 (最高优先级)\r\n    if (typedInvoker != null) {\r\n        return typedInvoker.apply(convertedInput);\r\n    }\r\n    // 路径 2: String 函数, 直接传入原始 JSON\r\n    if (function != null) {\r\n        if (convertedInput instanceof String) {\r\n            return function.apply((String) convertedInput);\r\n        }\r\n        // 路径 3: 非 String 输入, 先序列化为 JSON 再调用\r\n        return function.apply(OBJECT_MAPPER.writeValueAsString(convertedInput));\r\n    }\r\n    throw new IllegalStateException(\"没有该工具的处理方法: \" + name);\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- **路径 1** `typedInvoker`: 通过 `TypedToolFunction<T>` 接口创建, 支持泛型, 自动类型转换\r\n- **路径 2** `function(String)`: 接收原始 JSON 字符串, handler 自行反序列化\r\n- **路径 3** `function(Object→JSON)`: 输入被序列化为 JSON 后再传给 handler\r\n- 三路分派保证了向后兼容 -- 新旧两种工具注册方式都能正常工作\r\n\r\n### 4. 类型化接口 -- TypedToolFunction<T>\r\n\r\nAgent02 引入了泛型函数式接口, 让工具 handler 可以直接接收反序列化后的 Java 对象:\r\n\r\n```java\r\n@FunctionalInterface\r\npublic interface TypedToolFunction<T> {\r\n    String apply(T input) throws Exception;\r\n}\r\n\r\n@FunctionalInterface\r\npublic interface TypedToolInvoker {\r\n    String apply(Object input) throws Exception;\r\n}\r\n```\r\n\r\n构造器自动将 `TypedToolFunction<T>` 包装为 `TypedToolInvoker`:\r\n\r\n```java\r\npublic <T> ToolDefinition(String name, String description,\r\n        Tool.InputSchema inputSchema, Class<T> type,\r\n        TypedToolFunction<T> typedFunction) {\r\n    this.typedInvoker = input -> typedFunction.apply(type.cast(input));\r\n}\r\n```\r\n\r\n这意味着 handler 方法可以直接声明参数类型, 无需手动反序列化:\r\n\r\n```java\r\n// 旧方式: 需要在 handler 内部反序列化\r\nnew ToolDefinition(\"read_file\", ..., ReadFileInput.class,\r\n    (String json) -> {\r\n        ReadFileInput input = mapper.readValue(json, ReadFileInput.class);\r\n        return doRead(input);\r\n    })\r\n\r\n// 新方式: 框架自动反序列化, 直接拿到类型安全的对象\r\nnew ToolDefinition(\"read_file\", ..., ReadFileInput.class,\r\n    (TypedToolFunction<ReadFileInput>) input -> doRead(input))\r\n```\r\n\r\n### 5. 组合模式 -- 不改循环\r\n\r\nAgent02 和 Agent01 一样使用**组合模式**, 不继承 ZQAgent, 而是在 main 中创建实例:\r\n\r\n```java\r\npublic class Agent02 {\r\n    public static void main(String[] args) {\r\n        AnthropicClient client = AnthropicOkHttpClient.builder()\r\n                .apiKey(API_KEY).baseUrl(BASE_URL).build();\r\n\r\n        List<ToolDefinition> tools = new ArrayList<>();\r\n        tools.add(BashDefinition);\r\n        tools.add(EditFileDefinition);\r\n        tools.add(WriteFileDefinition);\r\n        tools.add(ReadFileDefinition);\r\n        tools.add(ListFilesDefinition);\r\n        tools.add(ContentSearchDefinition);\r\n\r\n        ZQAgent agent = new ZQAgent(client, MODEL, tools);\r\n        agent.setSystemPrompt(buildSystemPrompt());\r\n        agent.run();\r\n    }\r\n}\r\n```\r\n\r\n循环体与 s01 完全一致 -- 工具从 1 个变成 6 个, 但 `ZQAgent.run()` 一行代码都没改。\r\n\r\n## 变更内容\r\n\r\n| 组件 | 之前 (s01) | 之后 (s02) |\r\n|-----|------|------|\r\n| Tools | 1 (仅 bash) | 6 (bash, read_file, write_file, edit_file, list_files, content_search) |\r\n| Tool Definition | 无独立类 | `ToolDefinition` 类 (name + schema + handler) |\r\n| 类型安全 | 无 | `TypedToolFunction<T>` + `TypedToolInvoker` 接口 |\r\n| 调用机制 | 硬编码 | `invoke()` 三路分派 |\r\n| 路径安全 | 无 | 工作目录路径解析 + normalize |\r\n| Agent loop | `while(true)` + `stop_reason == TOOL_USE` 驱动 | **不变** |\r\n\r\n## 试一试\r\n\r\n```bash\r\n# 编译项目\r\nmvn clean install\r\n\r\n# 运行 Agent02\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent02\"\r\n```\r\n\r\n试试这些 prompt (英文 prompt 对 LLM 效果更好, 也可以用中文):\r\n\r\n1. `Read the file pom.xml`\r\n2. `Create a file called test.txt with some content`\r\n3. `Edit test.txt to modify the content`\r\n4. `Read test.txt to verify the edit worked`\r\n5. `List all Java files in the src directory`\r\n6. `Search for all TODO comments in the project`\r\n7. `查看当前目录下的项目结构`\r\n8. `搜索项目中所有包含 \"ToolDefinition\" 的代码`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s03",
    "locale": "zh",
    "title": "s03: Todo Planning (任务规划)",
    "content": "# s03: TodoWrite (待办写入)\r\n\r\n`s01 > s02 > [ s03 ] s04 > s05 > s06 | s07 > s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"没有计划的 agent 走哪算哪\"* -- 先列步骤再动手, 完成率翻倍。\r\n\r\n## 问题\r\n\r\n多步任务中, 模型会丢失进度 -- 重复做过的事、跳步、跑偏。对话越长越严重: 工具结果不断填满上下文, 系统提示的影响力逐渐被稀释。一个 10 步重构可能做完 1-3 步就开始即兴发挥, 因为 4-10 步已经被挤出注意力了。\r\n\r\n## 解决方案\r\n\r\n![Todo Overview](/agent/todo-overview.svg)\r\n\r\n```\r\n+--------+      +-------+      +---------+\r\n|  User  | ---> |  LLM  | ---> | Tools   |\r\n| prompt |      |       |      | + todo  |\r\n+--------+      +---+---+      +----+----+\r\n                    ^                |\r\n                    |   tool_result  |\r\n                    +----------------+\r\n                          |\r\n              +-----------+-----------+\r\n              | TodoManager state     |\r\n              | [ ] task A            |\r\n              | [>] task B  <- doing  |\r\n              | [x] task C            |\r\n              +-----------------------+\r\n                          |\r\n              if rounds_since_todo >= 3:\r\n                inject <reminder> into toolResult\r\n```\r\n\r\nAgent03 是**第一个使用继承模式**的模块 -- `extends ZQAgent` 而非组合。这样做是为了能够 `@Override onToolExecution()` 钩子, 在每次工具执行后检查 todo 是否被更新。\r\n\r\n## 工作原理\r\n\r\n### 1. 继承模式与钩子函数\r\n\r\nAgent03 继承 ZQAgent, 重写 `onToolExecution()` 方法。这是 Agent01/Agent02（组合模式）做不到的:\r\n\r\n```java\r\npublic class Agent03 extends ZQAgent {\r\n    public static TodoManager todoManager;\r\n    private int roundsSinceTodo = 0;\r\n    private long lastTodoVersion = 0;\r\n\r\n    public Agent03(AnthropicClient client, String model,\r\n                   List<ToolDefinition> tools, TodoManager todoManager) {\r\n        super(client, model, tools);\r\n        Agent03.todoManager = todoManager;\r\n        this.lastTodoVersion = todoManager.getVersion();\r\n    }\r\n}\r\n```\r\n\r\n**静态字段桥接设计**: `todoManager` 必须是 `static` 的, 因为 `TodoDefinition` 的 handler 引用了 `Agent03.todoManager::updateTodos` -- 静态方法引用要求目标字段也是静态的。\r\n\r\n### 2. 工具注册: 7 个工具\r\n\r\n```java\r\nList<ToolDefinition> tools = new ArrayList<>();\r\ntools.add(BashDefinition);           // bash 命令执行\r\ntools.add(EditFileDefinition);       // 编辑文件\r\ntools.add(WriteFileDefinition);      // 写入文件\r\ntools.add(ReadFileDefinition);       // 读取文件\r\ntools.add(ListFilesDefinition);      // 列出目录\r\ntools.add(ContentSearchDefinition);  // 搜索内容\r\ntools.add(TodoDefinition);           // 待办事项管理\r\n```\r\n\r\n其中 `TodoDefinition` 使用**嵌套 JSON Schema** 定义输入:\r\n\r\n```java\r\npublic static ToolDefinition TodoDefinition = new ToolDefinition(\r\n    \"todo\",\r\n    \"管理待办事项列表。使用此工具可以添加、更新或完成任务。\"\r\n    + \"支持任务状态追踪，包括待处理、进行中和已完成三种状态。\",\r\n    createInputSchema(\r\n        Map.of(\"todos\",\r\n            Map.of(\r\n                \"type\", \"array\",\r\n                \"description\", \"待办事项列表\",\r\n                \"items\", Map.of(\r\n                    \"type\", \"object\",\r\n                    \"properties\", Map.of(\r\n                        \"id\",      Map.of(\"type\", \"string\",\r\n                                          \"description\", \"任务的唯一标识符\"),\r\n                        \"content\", Map.of(\"type\", \"string\",\r\n                                          \"description\", \"待办事项的内容描述\"),\r\n                        \"status\",  Map.of(\r\n                            \"type\", \"string\",\r\n                            \"enum\", List.of(\"pending\", \"in_progress\", \"completed\"),\r\n                            \"description\", \"任务状态：pending-待处理, in_progress-进行中, completed-已完成\")\r\n                    ),\r\n                    \"required\", List.of(\"id\", \"content\", \"status\")\r\n                )\r\n            )\r\n        ),\r\n        List.of(\"todos\")\r\n    ),\r\n    TodoInput.class,\r\n    Agent03.todoManager::updateTodos\r\n);\r\n```\r\n\r\n### 3. TodoManager: 版本追踪与约束\r\n\r\n`TodoManager` 使用 `synchronized update()` 方法, 通过 `version` 字段追踪更新次数:\r\n\r\n| 约束 | 说明 |\r\n|-----|------|\r\n| 最多 20 项 | `validated.size() > 20` 时抛异常 |\r\n| 同时仅 1 个 in_progress | `inProgressCount > 1` 时抛异常 |\r\n| 内容不能为空 | 每项 `content.trim()` 后检查 |\r\n| 状态三选一 | `pending` / `in_progress` / `completed` |\r\n\r\n渲染格式使用状态标记:\r\n\r\n```\r\n[ ] 待处理的任务\r\n[>] 进行中的任务 <- 当前正在执行的操作描述\r\n[x] 已完成的任务\r\n\r\n(1/3 已完成)\r\n```\r\n\r\n### 4. Nag Reminder: 3 轮不更新就追着问\r\n\r\n`onToolExecution()` 在每次工具调用后被 ZQAgent 基类触发。它检查 `todoManager.getVersion()` 是否变化:\r\n\r\n```java\r\n@Override\r\nprotected void onToolExecution(List<ContentBlockParam> toolResults) {\r\n    long currentVersion = todoManager.getVersion();\r\n    if (currentVersion > lastTodoVersion) {\r\n        roundsSinceTodo = 0;\r\n        lastTodoVersion = currentVersion;\r\n    } else {\r\n        roundsSinceTodo++;\r\n    }\r\n\r\n    if (roundsSinceTodo >= 3) {\r\n        String reminder = String.format(\"\"\"\r\n            <reminder>\r\n            您已经 %d 个回合没有更新待办事项列表了。请更新列表以反映当前进度。\r\n\r\n            建议操作：\r\n            1. 检查当前任务状态\r\n            2. 更新已完成的任务\r\n            3. 添加新的任务（如果需要）\r\n            4. 调整任务优先级\r\n            </reminder>\r\n            \"\"\", roundsSinceTodo);\r\n        toolResults.add(ContentBlockParam.ofText(TextBlockParam.builder()\r\n                .text(reminder).build()));\r\n    }\r\n}\r\n```\r\n\r\n关键设计: reminder 不是重新发送系统提示, 而是作为**工具结果的一部分**注入 -- 这样模型在下一轮推理中会看到这个提醒, 但不会占用额外的 API 消息槽位。\r\n\r\n## 变更内容\r\n\r\n| 组件 | 之前 (s02) | 之后 (s03) |\r\n|-----|-----------|-----------|\r\n| 模式 | 组合 (new ZQAgent) | 继承 (extends ZQAgent) |\r\n| Tools | 6 (bash + 5 file) | 7 (bash + 5 file + todo) |\r\n| Todo | (无) | TodoManager + TodoDefinition |\r\n| 钩子 | (无) | `onToolExecution()` nag reminder |\r\n| 静态字段 | (无) | `static TodoManager todoManager` |\r\n| 约束 | (无) | 最多 20 项, 同时仅 1 个 in_progress |\r\n| 版本追踪 | (无) | `version` 字段驱动 nag 判断 |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-03\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent03\"\r\n```\r\n\r\n试试这些 prompt (英文 prompt 对 LLM 效果更好, 也可以用中文):\r\n\r\n1. `重构 TodoManager 类：优化代码结构，添加完整的 JavaDoc 注释，并改进错误处理`\r\n2. `在项目中创建一个缓存模块，包含 CacheManager.java 和对应的单元测试`\r\n3. `检查 src/main/java 目录下所有 Java 文件的命名规范，并统一代码风格`\r\n4. `为 Tools 类添加三个新工具：文件搜索、内容替换、目录遍历，每个工具都要有完整的注释`\r\n5. `创建一个简单的 REST API 框架，包含控制器、服务层和数据访问层`\r\n6. `Read all .java files in the tool/ directory and summarize each class's responsibility`\r\n7. `Create a unit test for TodoManager covering all edge cases: empty list, max items, duplicate in_progress`\r\n8. `Refactor the buildSystemPrompt method to use a template file instead of inline string`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s04",
    "locale": "zh",
    "title": "s04: Subagent (子智能体)",
    "content": "# s04: Subagents（子智能体）\r\n\r\n`s01 > s02 > s03 > [ s04 ] s05 > s06 | s07 > s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"大任务拆小，每个小任务干净的上下文\"* -- 子智能体使用独立的 messages[]，不污染主对话。\r\n\r\n## 问题\r\n\r\n智能体工作越久，messages 数组越胖。每次读文件、跑命令的输出都永久留在上下文里。\"这个项目用什么测试框架？\" 可能要读 5 个文件，但父智能体只需要一个词：\"Maven + JUnit。\"\r\n\r\n## 解决方案\r\n\r\n![Subagent Overview](/agent/subagent-overview.svg)\r\n\r\n```\r\nParent agent                     SubAgent (static utility)\r\n+------------------+             +------------------+\r\n| messages=[...]   |             | messages=[]      | <-- fresh\r\n|                  |  dispatch   |                  |\r\n| tool: sub_agent  | ----------> | while tool_use:  |\r\n|   prompt=\"...\"   |             |   call tools     |\r\n|                  |  summary    |   append results |\r\n|   result = \"...\" | <---------- | return last text |\r\n+------------------+             +------------------+\r\n\r\nParent context stays clean. SubAgent context is discarded.\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. SubAgent -- 静态工具类\r\n\r\n子智能体不是 Agent04 的实例方法，而是 `com.hoppinzq.agent.base.SubAgent` 中的一个**静态方法**：\r\n\r\n```java\r\npublic class SubAgent {\r\n    public static String executeSubAgent(AnthropicClient client,\r\n                                         String model,\r\n                                         SubAgentInput input) {\r\n        // 创建子智能体工具列表（不含 sub_agent，防止递归）\r\n        List<ToolDefinition> subTools = new ArrayList<>();\r\n        subTools.add(ReadFileDefinition);\r\n        subTools.add(EditFileDefinition);\r\n        subTools.add(WriteFileDefinition);\r\n\r\n        // 创建子智能体实例（独立上下文）\r\n        ZQAgent subAgent = new ZQAgent(client, model, subTools);\r\n        subAgent.setSystemPrompt(\"你是一个子智能体...\");\r\n\r\n        // 子智能体运行自己的循环，只返回最终文本\r\n        String result = subAgent.runTask(input.getPrompt());\r\n        return result;\r\n    }\r\n}\r\n```\r\n\r\n**要点**：\r\n- `SubAgent` 在 `base` 包下，不是 `tool` 包\r\n- `executeSubAgent()` 是静态方法，不依赖任何实例状态\r\n- 子智能体通过 `new ZQAgent()` 创建，**组合模式**，不复用父类\r\n\r\n### 2. 父子智能体工具分离\r\n\r\n```\r\n父智能体 (8 个工具)              子智能体 (3 个工具)\r\n├── bash                         ├── read_file\r\n├── read_file                    ├── edit_file\r\n├── edit_file                    └── write_file\r\n├── write_file\r\n├── list_files\r\n├── content_search\r\n├── todo\r\n└── sub_agent        ← 子智能体没有此工具，防止递归生成\r\n```\r\n\r\n子智能体只有文件操作工具，**没有** bash（安全考虑）、没有 sub_agent（防止递归）、没有 todo（子任务不需要任务管理）、没有 list_files 和 content_search。\r\n\r\n### 3. sub_agent 工具定义\r\n\r\n在 ToolDefinition.java 中注册：\r\n\r\n```java\r\npublic static ToolDefinition SubAgentDefinition = new ToolDefinition(\r\n    \"sub_agent\",\r\n    \"将子任务委托给子智能体处理。子智能体拥有读取文件、写入文件、\"\r\n    + \"编辑文件和搜索内容等工具能力...\",\r\n    createInputSchema(\r\n        Map.of(\"prompt\", createProperty(\"string\",\r\n            \"给子智能体的任务提示词，详细描述需要完成的任务\")),\r\n        List.of(\"prompt\")\r\n    ),\r\n    SubAgentInput.class,\r\n    Tools::executeSubAgent    // 静态方法引用\r\n);\r\n```\r\n\r\n### 4. 智能文件编辑特性\r\n\r\n子智能体的编辑操作实现了空白字符归一化处理，消除 Windows/Unix 换行符和制表符/空格的差异：\r\n\r\n```java\r\nprivate static String normalizeForReplace(String str) {\r\n    String normalized = str.replace(\"\\r\\n\", \"\\n\").replace(\"\\r\", \"\\n\");\r\n    normalized = normalized.replace(\"\\t\", \"    \");\r\n    return normalized;\r\n}\r\n```\r\n\r\n## 变更内容\r\n\r\n| 组件 | s03（Todo 管理） | s04（子智能体） |\r\n|-----|-----------------|----------------|\r\n| 模式 | 继承 ZQAgent | 继承 ZQAgent |\r\n| 工具数量 | 7 | 8（+sub_agent） |\r\n| TodoManager | ✅ 静态字段 | ✅ 静态字段（保留） |\r\n| 催办提醒 | ✅ 3 回合未更新 | ✅ 3 回合未更新（保留） |\r\n| 子智能体 | ❌ | ✅ SubAgent 静态工具类 |\r\n| 子工具数 | - | 3（read/edit/write） |\r\n| 递归防护 | - | 子工具列表不含 sub_agent |\r\n| base 包 | - | 新增 `base/SubAgent.java` |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-04\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent04\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `使用子智能体查找这个项目使用的测试框架`\r\n2. `派发子任务：读取所有 .java 文件并总结各自的功能`\r\n3. `用子智能体创建一个新文件 hello.java，然后读取它验证内容`\r\n4. `用子任务读取所有 .md 文件并总结项目结构`\r\n5. `使用子智能体搜索代码中的 \"TODO\" 注释并汇总`\r\n6. `创建一个 todo 列表，然后用子智能体逐一完成每个任务`\r\n7. `用子智能体分析 ToolDefinition.java 的工具定义模式`\r\n8. `比较父智能体和子智能体的工具差异`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s05",
    "locale": "zh",
    "title": "s05: Skill Loading (技能加载)",
    "content": "# s05: Skills（技能加载）\r\n\r\n`s01 > s02 > s03 > s04 > [ s05 ] s06 | s07 > s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"用到什么知识，临时加载什么知识\"* -- 通过 tool_result 注入，不塞 system prompt\r\n\r\n## 问题\r\n\r\n你希望智能体遵循特定领域的工作流：git 约定、测试模式、代码审查清单。全塞进系统提示太浪费 -- 10 个技能，每个 2000 token，就是 20,000 token，大部分跟当前任务毫无关系。\r\n\r\n## 解决方案\r\n\r\n![Skill Overview](/agent/skill-overview.svg)\r\n\r\n![Layer1 Budget](/agent/layer1-budget.svg)\r\n\r\n```\r\n系统提示 (Layer 1 -- 始终存在):\r\n+--------------------------------------+\r\n| 你是一个带有待办事项列表的编程智能体  |\r\n| 可用技能：                           |\r\n|   - git: Git工作流助手和最佳实践      |  ~100 tokens/skill\r\n|   - test: 测试最佳实践和模式          |\r\n+--------------------------------------+\r\n\r\n当模型调用 load_skill(\"git\") 时:\r\n+--------------------------------------+\r\n| tool_result (Layer 2 -- 按需加载):    |\r\n| <skill name=\"git\">                   |\r\n|   完整的 Git 工作流说明...           |  ~2000 tokens\r\n|   步骤 1: ...                         |\r\n| </skill>                             |\r\n+--------------------------------------+\r\n```\r\n\r\n第一层：系统提示中放技能名称（低成本）。第二层：tool_result 中按需放完整内容。\r\n\r\n## 工作原理\r\n\r\n### 1. 技能文件结构\r\n\r\n每个技能是一个目录，包含 `SKILL.md` 文件和 YAML frontmatter：\r\n\r\n```\r\nsrc/main/resources/skills/\r\n  code-review/\r\n    SKILL.md    # ---\\n name: code-review\\n description: 审查代码\\n ---\\n ...\r\n  git/\r\n    SKILL.md    # ---\\n name: git\\n description: Git工作流\\n ---\\n ...\r\n  test/\r\n    SKILL.md    # ---\\n name: test\\n description: 测试实践\\n ---\\n ...\r\n```\r\n\r\n### 2. SkillLoader 实现\r\n\r\n```java\r\npublic class SkillLoader {\r\n    private final Map<String, String> skills = new HashMap<>();\r\n\r\n    public SkillLoader() {\r\n        loadSkills();  // 递归扫描所有 SKILL.md 文件\r\n    }\r\n\r\n    // Layer 1: 获取所有技能的简短描述（用于系统提示）\r\n    public String getDescriptions() {\r\n        StringBuilder sb = new StringBuilder();\r\n        skills.forEach((name, body) -> {\r\n            String desc = \"-\";\r\n            if (body.contains(\"description=\\\"\")) {\r\n                int start = body.indexOf(\"description=\\\"\") + 13;\r\n                int end = body.indexOf(\"\\\"\", start);\r\n                if (end > start) desc = body.substring(start, end);\r\n            }\r\n            sb.append(String.format(\"  - %s: %s\\n\", name, desc));\r\n        });\r\n        return sb.toString().trim();\r\n    }\r\n\r\n    // Layer 2: 获取指定技能的完整内容（用于 tool_result）\r\n    public String getSkill(String skillName) {\r\n        if (!skills.containsKey(skillName)) {\r\n            return \"错误：未知技能 '\" + skillName + \"'。可用技能：\"\r\n                + String.join(\", \", skills.keySet());\r\n        }\r\n        return skills.get(skillName);\r\n    }\r\n}\r\n```\r\n\r\n### 3. YAML Frontmatter 解析\r\n\r\n```java\r\nprivate void parseSkillContent(String entryName, String content) {\r\n    Pattern pattern = Pattern.compile(\"^---\\\\n(.*?)\\\\n---\\\\n(.*)\", Pattern.DOTALL);\r\n    Matcher matcher = pattern.matcher(content);\r\n\r\n    String name = entryName;\r\n    String description = \"\";\r\n    String body = content;\r\n\r\n    if (matcher.find()) {\r\n        String meta = matcher.group(1);\r\n        body = matcher.group(2).trim();\r\n\r\n        for (String line : meta.split(\"\\n\")) {\r\n            String[] parts = line.split(\":\", 2);\r\n            if (parts[0].trim().equals(\"name\"))\r\n                name = parts[1].trim();\r\n            else if (parts[0].trim().equals(\"description\"))\r\n                description = parts[1].trim();\r\n        }\r\n    }\r\n\r\n    skills.put(name, String.format(\r\n        \"<skill name=\\\"%s\\\" description=\\\"%s\\\">\\n%s\\n</skill>\",\r\n        name, description, body));\r\n}\r\n```\r\n\r\n### 4. load_skill 工具定义\r\n\r\n```java\r\npublic static ToolDefinition SkillsDefinition = new ToolDefinition(\r\n    \"load_skill\",\r\n    \"加载指定的技能（工具集合）。可用技能：\"\r\n        + String.join(\", \", Agent05.skillLoader.getAvailableSkills()),\r\n    createInputSchema(\r\n        Map.of(\"skill_name\", createProperty(\"string\", \"要加载的技能名称\")),\r\n        List.of(\"skill_name\")\r\n    ),\r\n    LoadSkillInput.class,\r\n    Tools::skillLoad\r\n);\r\n```\r\n\r\n注意 `Agent05.skillLoader` -- 静态字段桥接，ToolDefinition 中的静态工具定义通过它访问 SkillLoader 实例。\r\n\r\n### 5. 双模式加载\r\n\r\nSkillLoader 支持两种加载环境：\r\n\r\n```java\r\nprivate void loadSkills() {\r\n    URL resourceUrl = getClass().getClassLoader().getResource(SKILL_PATH);\r\n    String protocol = resourceUrl.getProtocol();\r\n\r\n    if (\"file\".equals(protocol)) {\r\n        loadSkillsFromFileSystem(resourceUrl);  // 开发时：文件系统\r\n    } else if (\"jar\".equals(protocol)) {\r\n        loadSkillsFromJar(resourceUrl);          // 打包后：从 JAR 读取\r\n    }\r\n}\r\n```\r\n\r\n## 变更内容\r\n\r\n| 组件 | s04（子智能体） | s05（技能加载） |\r\n|-----|----------------|----------------|\r\n| 工具数量 | 8 | 9（+load_skill） |\r\n| SkillLoader | ❌ | ✅ 两层注入 |\r\n| 技能文件 | ❌ | `skills/*/SKILL.md` |\r\n| YAML 解析 | ❌ | ✅ 正则解析 frontmatter |\r\n| Token 优化 | 无 | 按需加载节省 ~19,000 tokens/请求 |\r\n| JAR 支持 | ❌ | ✅ 文件系统 + JAR 双模式 |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-05\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent05\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `有哪些技能可用？`\r\n2. `加载 git 技能，告诉我如何创建新分支`\r\n3. `加载 code-review 技能，帮我审查 Agent05.java 的代码质量`\r\n4. `加载 test 技能，展示单元测试的 AAA 模式`\r\n5. `使用 todo 工具创建任务列表，然后帮我完成代码重构`\r\n6. `先加载 test 技能，再让子智能体帮我写测试用例`\r\n7. `加载 git 技能，然后帮我查看当前的提交历史`\r\n8. `对比加载和不加载技能时，智能体回答质量的差异`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s06",
    "locale": "zh",
    "title": "s06: Context Compaction (上下文压缩)",
    "content": "# s06: Context Compact（上下文压缩）\r\n\r\n`s01 > s02 > s03 > s04 > s05 > [ s06 ] | s07 > s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"上下文总会满，要有办法腾地方\"*\r\n\r\n## 问题\r\n\r\nAgent05 已经具备完整的工具集（9 个工具），但在长时间运行或处理大型项目时，对话历史会不断膨胀，最终耗尽上下文窗口。具体表现为：\r\n\r\n- 工具结果不断累积，每次 `read_file` 返回的文件内容全部保留\r\n- 多轮对话后，token 数量逼近 API 限制\r\n- 智能体无法继续工作，或被迫丢失早期上下文\r\n\r\n## 解决方案\r\n\r\n![Compact Overview](/agent/compact-overview.svg)\r\n\r\n![Compaction Layers](/agent/compaction-layers.svg)\r\n\r\n![Micro Compact](/agent/micro-compact.svg)\r\n\r\n![Auto Compact](/agent/auto-compact.svg)\r\n\r\nAgent06 引入 **ContextCompactor** 三层压缩策略，在对话过程中持续控制上下文大小：\r\n\r\n```\r\n                    上下文压缩流程\r\n┌──────────────────────────────────────────────────────┐\r\n│                                                      │\r\n│  ┌─────────────────────────────────────────────┐     │\r\n│  │  Layer 1: 微压缩 (microCompact)             │     │\r\n│  │  ├─ 触发: 每次 LLM 调用前 + 每次工具执行后  │     │\r\n│  │  ├─ 方式: 旧 tool_result → \"[已执行: xxx]\"  │     │\r\n│  │  └─ 保留: 最近 KEEP_RECENT (3) 个完整结果   │     │\r\n│  └──────────────────┬──────────────────────────┘     │\r\n│                     │                                │\r\n│  ┌──────────────────▼──────────────────────────┐     │\r\n│  │  estimateTokens() > TOKEN_THRESHOLD?        │     │\r\n│  │  算法: JSON序列化.length() / 4               │     │\r\n│  └──────────────────┬──────────────────────────┘     │\r\n│              YES    │                                │\r\n│  ┌──────────────────▼──────────────────────────┐     │\r\n│  │  Layer 2: 自动压缩 (autoCompact)             │     │\r\n│  │  ├─ 保存完整对话 → .transcripts/*.jsonl      │     │\r\n│  │  ├─ LLM 生成摘要 (maxTokens=2000)           │     │\r\n│  │  └─ [压缩提示+摘要, 助手确认] 替换原始列表   │     │\r\n│  └──────────────────┬──────────────────────────┘     │\r\n│                     │                                │\r\n│  ┌──────────────────▼──────────────────────────┐     │\r\n│  │  Layer 3: 手动压缩 (compact 工具)            │     │\r\n│  │  ├─ 触发: 用户调用 compact(focus=...)        │     │\r\n│  │  └─ 执行: manualCompactRequested → autoCompact│     │\r\n│  └─────────────────────────────────────────────┘     │\r\n│                                                      │\r\n└──────────────────────────────────────────────────────┘\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. 双触发点压缩\r\n\r\nAgent06 在两个位置都嵌入压缩逻辑，形成双保险：\r\n\r\n```java\r\n// 触发点1: chatMessage() - LLM 调用前\r\n@Override\r\nprotected Message chatMessage(List<MessageParam> messageParams) {\r\n    // Layer 1: 微压缩（传入副本，不影响原列表）\r\n    List<MessageParam> compactedParams = compactor.microCompact(new ArrayList<>(messageParams));\r\n\r\n    // Layer 2: 自动压缩\r\n    if (ContextCompactor.estimateTokens(compactedParams) > TOKEN_THRESHOLD) {\r\n        compactedParams = compactor.autoCompact(compactedParams);\r\n        this.messageParams.clear();\r\n        this.messageParams.addAll(compactedParams);\r\n    }\r\n    return super.chatMessage(compactedParams);\r\n}\r\n\r\n// 触发点2: onToolExecution() - 工具执行后\r\n@Override\r\nprotected void onToolExecution(List<ContentBlockParam> toolResults) {\r\n    // Todo 提醒 + Layer 1 + Layer 2 + Layer 3\r\n    compactor.microCompact(messageParams);\r\n\r\n    if (compactor.estimateTokens(messageParams) > TOKEN_THRESHOLD) {\r\n        List<MessageParam> compressed = compactor.autoCompact(messageParams);\r\n        messageParams.clear();\r\n        messageParams.addAll(compressed);\r\n    }\r\n\r\n    if (manualCompactRequested) {\r\n        List<MessageParam> compressed = compactor.autoCompact(messageParams);\r\n        messageParams.clear();\r\n        messageParams.addAll(compressed);\r\n        manualCompactRequested = false;\r\n    }\r\n}\r\n```\r\n\r\n### 2. 微压缩实现（microCompact）\r\n\r\n```\r\n输入: 10 个工具结果的消息列表\r\n  ├─ [tool_result: bash 完整输出 500行]\r\n  ├─ [tool_result: read_file 完整内容 200行]\r\n  ├─ ... (7 个更多结果)\r\n  └─ [tool_result: edit_file 确认]\r\n\r\nmicroCompact(KEEP_RECENT=3):\r\n  ├─ [tool_result: \"[已执行: bash]\"]          ← 占位符\r\n  ├─ [tool_result: \"[已执行: read_file]\"]     ← 占位符\r\n  ├─ ... (4 个更多占位符)\r\n  ├─ [tool_result: 完整内容]                   ← 保留\r\n  ├─ [tool_result: 完整内容]                   ← 保留\r\n  └─ [tool_result: 完整内容]                   ← 保留\r\n```\r\n\r\n实现细节：\r\n- 使用 `ToolResultInfo` record 跟踪工具结果位置（消息索引 + 内容块索引）\r\n- 建立 `toolId → toolName` 映射，生成有意义的占位符\r\n- 仅在结果数 > `KEEP_RECENT` 时才执行压缩\r\n\r\n### 3. 自动压缩实现（autoCompact）\r\n\r\n```\r\n步骤1: 保存对话\r\n  .transcripts/transcript_1710000000000.jsonl\r\n  └─ 每条消息一行 JSON\r\n\r\n步骤2: 生成摘要\r\n  截取消息文本（限制 80000 字符）\r\n  → LLM 生成: \"已完成工作 / 当前状态 / 关键决策\"\r\n\r\n步骤3: 构建新消息列表\r\n  [user: \"[对话已压缩。完整记录: .transcripts/xxx.jsonl]\\n\\n摘要...\"]\r\n  [assistant: \"收到。我已从摘要中获取了上下文。继续工作。\"]\r\n```\r\n\r\n### 4. 工具数量\r\n\r\n| 序号 | 工具名 | 来源 | 说明 |\r\n|-----|--------|------|------|\r\n| 1 | bash | 基础 | Shell 命令执行 |\r\n| 2 | read_file | 基础 | 读取文件 |\r\n| 3 | write_file | 基础 | 写入文件 |\r\n| 4 | edit_file | 基础 | 编辑文件 |\r\n| 5 | list_files | 基础 | 列出目录 |\r\n| 6 | content_search | 基础 | 搜索内容 |\r\n| 7 | sub_agent | s04 | 子智能体委托 |\r\n| 8 | todo | s03 | 待办事项管理 |\r\n| 9 | load_skill | s05 | 技能加载 |\r\n| 10 | **compact** | **s06** | **手动压缩** |\r\n\r\n## 变更内容\r\n\r\n| 组件 | s05 | s06 | 说明 |\r\n|------|-----|-----|------|\r\n| 工具数量 | 9 | **10** | 新增 `compact` 工具 |\r\n| ContextCompactor | 无 | **新增** | 三层压缩器，`tool/compact/` 包 |\r\n| chatMessage() | 未重写 | **重写** | 添加 Layer 1+2 压缩逻辑 |\r\n| onToolExecution() | Todo 提醒 | **Todo + 三层压缩** | 添加 Layer 1+2+3 |\r\n| manualCompactRequested | 无 | **新增** | `public static boolean` 标志位 |\r\n| TOKEN_THRESHOLD | 无 | **1000** | 自动压缩 token 阈值 |\r\n| KEEP_RECENT | 无 | **3** | 微压缩保留数量 |\r\n| .transcripts/ | 无 | **新增** | JSONL 对话记录目录 |\r\n| estimateTokens() | 无 | **静态方法** | `json.length() / 4` |\r\n\r\n## 试一试\r\n\r\n```\r\n1. 请读取项目中所有的 Java 文件，观察微压缩效果\r\n2. 持续执行命令直到触发自动压缩，观察控制台输出\r\n3. 使用 compact 工具手动压缩，并指定 focus=\"保留错误信息\"\r\n4. 创建多个待办事项，连续 3 回合不更新，观察催办提醒\r\n5. 加载 git 技能，然后执行 git 操作\r\n6. 使用子智能体读取多个文件并生成摘要\r\n7. 同时触发手动压缩和自动压缩，观察压缩优先级\r\n8. 检查 .transcripts/ 目录下的 JSONL 文件内容\r\n```\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s07",
    "locale": "zh",
    "title": "s07: Task System (任务系统)",
    "content": "# s07: Task System（任务系统）\r\n\r\n`s01 > s02 > s03 > s04 > s05 > s06 > [ s07 ] | s08 > s09 > s10 > s11 > s12`\r\n\r\n> *\"将大目标分解为小任务，排序，持久化到磁盘\"*\r\n\r\n## 问题\r\n\r\ns03 的 TodoManager 是内存中的平面检查清单：没有顺序、没有依赖、除了完成/未完成外没有其他状态。真实的目标有结构 —— 任务 B 依赖任务 A，任务 C 和 D 可以并行运行，任务 E 等待 C 和 D 都完成。\r\n\r\n没有明确的关系，智能体无法判断：\r\n- 哪些任务可以执行？\r\n- 哪些任务被阻塞？\r\n- 哪些任务可以并行？\r\n\r\n而且因为列表只存在于内存中，上下文压缩（s06）会将其清空。\r\n\r\n## 解决方案\r\n\r\n![Task System Overview](/agent/task-system-overview.svg)\r\n\r\n<div style=\"background:#1a1a2e;padding:12px;border-radius:8px\">\r\n<img src=\"/agent/task-dag.svg\" alt=\"Task DAG\" />\r\n</div>\r\n\r\n将检查清单提升为**持久化到磁盘的 DAG 任务图**。每个任务是一个 JSON 文件，包含状态、依赖（`blockedBy`）和被依赖（`blocks`）。任务图在任何时刻都能回答三个问题：\r\n\r\n- **哪些任务准备就绪？** — `pending` 状态且 `blockedBy` 为空\r\n- **哪些任务被阻塞？** — `blockedBy` 非空，等待未完成依赖\r\n- **哪些任务已完成？** — `completed` 状态，完成时自动解除依赖\r\n\r\n```\r\n                任务图 (DAG)\r\n┌──────────────────────────────────────────────┐\r\n│                                              │\r\n│  task_1 (completed)                          │\r\n│     ├── task_2 (pending, blockedBy: [])      │  ← 可执行\r\n│     └── task_3 (pending, blockedBy: [])      │  ← 可执行\r\n│           ├── task_4 (blocked by: [2, 3])    │  ← 等待 2 和 3\r\n│           └── ...                            │\r\n│                                              │\r\n│  存储格式: .tasks/task_{id}.json             │\r\n│  字段: id, subject, description, status,     │\r\n│        blockedBy, blocks, owner              │\r\n└──────────────────────────────────────────────┘\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. TaskManager — 任务管理器\r\n\r\n```java\r\npublic class TaskManager {\r\n    private final Path tasksDir;  // .tasks/ 目录\r\n    private int nextId;           // 自增 ID\r\n\r\n    public TaskManager() {\r\n        this.tasksDir = Paths.get(AIConstants.ROOT, \".tasks\");\r\n        this.nextId = findMaxId() + 1;  // 扫描已有文件，保证 ID 连续\r\n    }\r\n}\r\n```\r\n\r\n**核心方法：**\r\n\r\n```java\r\n// 创建任务 — 自增 ID，默认 pending\r\npublic String createTask(String subject, String description) {\r\n    TaskInput task = new TaskInput();\r\n    task.setId(nextId++);\r\n    task.setStatus(\"pending\");\r\n    saveTask(task);\r\n    return taskToJson(task);\r\n}\r\n\r\n// 更新任务 — 完成时自动清除依赖\r\npublic String updateTask(int taskId, String status,\r\n                         List<Integer> addBlockedBy,\r\n                         List<Integer> addBlocks) {\r\n    TaskInput task = loadTask(taskId);\r\n    if (\"completed\".equals(status)) {\r\n        clearDependency(taskId);  // 自动解锁后续任务\r\n    }\r\n    // addBlocks 时双向同步被阻塞任务的 blockedBy\r\n    if (addBlocks != null) {\r\n        for (Integer blockedId : addBlocks) {\r\n            TaskInput blockedTask = loadTask(blockedId);\r\n            blockedTask.getBlockedBy().add(taskId);\r\n            saveTask(blockedTask);\r\n        }\r\n    }\r\n    saveTask(task);\r\n    return taskToJson(task);\r\n}\r\n\r\n// 清除依赖 — 遍历所有任务，移除已完成任务的 ID\r\nprivate void clearDependency(int completedId) {\r\n    Files.list(tasksDir).forEach(p -> {\r\n        TaskInput task = loadTaskFromFile(p);\r\n        if (task.getBlockedBy().contains(completedId)) {\r\n            task.getBlockedBy().remove(Integer.valueOf(completedId));\r\n            saveTask(task);\r\n        }\r\n    });\r\n}\r\n```\r\n\r\n### 2. TaskInput — 任务数据类\r\n\r\n```java\r\npublic class TaskInput {\r\n    private int id;\r\n    private String subject;\r\n    private String description;\r\n    private String status;          // pending, in_progress, completed\r\n    private List<Integer> blockedBy; // 被哪些任务阻塞\r\n    private List<Integer> blocks;    // 阻塞哪些任务\r\n    private String owner;\r\n\r\n    // 显示格式: [x] #1: Parse code (blocked by: [2, 3])\r\n    public String getDisplayString() { ... }\r\n}\r\n```\r\n\r\n### 3. 四个任务工具\r\n\r\n```java\r\ntask_create(subject=\"实现登录\", description=\"JWT + Spring Security\")\r\ntask_update(taskId=2, status=\"in_progress\")\r\ntask_update(taskId=3, addBlockedBy=[1, 2])  // 设置依赖\r\ntask_list()   // 列出所有任务\r\ntask_get(taskId=1)  // 获取详情\r\n```\r\n\r\n### 4. 静态字段桥接\r\n\r\n```java\r\npublic class Agent07 extends ZQAgent {\r\n    public static TaskManager taskManager;    // ToolDefinition 引用\r\n    public static ContextCompactor compactor;\r\n    public static boolean manualCompactRequested = false;\r\n    public static TodoManager todoManager;\r\n    public static SkillLoader skillLoader;\r\n}\r\n```\r\n\r\n## 工具数量\r\n\r\n| 版本 | 工具数 | 新增工具 |\r\n|------|--------|----------|\r\n| s06 | 10 | — |\r\n| s07 | 14 | task_create, task_update, task_list, task_get |\r\n\r\n## 相对于 s06 的变更\r\n\r\n| 组件 | s06 | s07 |\r\n|------|-----|-----|\r\n| 工具数 | 10 | 14 (+4 任务工具) |\r\n| 任务模型 | 内存平面清单（TodoManager） | DAG 任务图（磁盘持久化） |\r\n| 依赖关系 | 无 | blockedBy + blocks |\r\n| 状态管理 | done/not | pending → in_progress → completed |\r\n| 持久化 | 压缩后丢失 | `.tasks/` 磁盘文件，压缩后保留 |\r\n| 新增类 | — | TaskManager, TaskInput, TaskCreateInput, TaskUpdateInput, TaskListInput, TaskGetInput |\r\n| Agent 字段 | 4 个 public static | 5 个 public static (+taskManager) |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-07\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent07\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `创建 3 个任务：\"搭建项目\"、\"编写代码\"、\"编写测试\"，让它们按顺序依赖`\r\n2. `创建重构任务板：parse -> transform & emit (并行) -> test`\r\n3. `完成任务 1，然后列出所有任务，看看哪些被解锁了`\r\n4. `创建 5 个任务，让 task_5 依赖 task_1 到 task_4，然后完成 task_3 看看 task_5 状态`\r\n5. `列出所有任务，看看有没有可以并行执行的`\r\n6. `给任务 2 添加 blocks 指向任务 3，然后查看任务 3 的 blockedBy`\r\n7. `完成任务 1，完成任务 2，再列出任务看 task_3 是否解锁`\r\n8. `清空 .tasks 目录，重新创建一个完整的 CI/CD 流水线任务图`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s08",
    "locale": "zh",
    "title": "s08: Background Tasks (后台任务)",
    "content": "# s08: Background Tasks（后台任务系统）\r\n\r\n`s01 > s02 > s03 > s04 > s05 > s06 > s07 > [ s08 ] | s09 > s10 > s11 > s12`\r\n\r\n> *\"在后台运行慢操作；智能体持续思考\"* — 守护线程运行命令，完成后注入通知\r\n\r\n## 问题\r\n\r\ns07 的 `bash` 工具是阻塞的：模型调用 bash → 等待命令完成 → 返回结果 → 继续思考。如果命令需要几分钟（`npm install`、`pytest`、`docker build`），模型就闲置等待。用户说\"安装依赖，同时帮我创建配置文件\"，智能体只能顺序执行，无法并行。\r\n\r\n**核心矛盾：**\r\n- ❌ bash 阻塞主线程，模型在等待时无法思考\r\n- ❌ 无法并行执行多个耗时任务\r\n- ❌ 长时间命令导致对话超时或体验差\r\n\r\n## 解决方案\r\n\r\n![Background Tasks Overview](/agent/background-tasks-overview.svg)\r\n\r\n引入 **BackgroundManager** — 一个基于守护线程池 + 通知队列的后台任务管理器。模型调用 `background_run` 启动任务后**立即返回**，任务在后台线程运行。完成后结果进入 `ConcurrentLinkedQueue` 通知队列，在**下次 LLM 调用前自动注入**到对话中。\r\n\r\n```\r\nMain thread                         Background threads\r\n+-------------------+               +-------------------+\r\n| agent loop        |               |                   |\r\n|                   |   spawn       | thread-1: bash    |\r\n| background_run --+------>+------->| npm install       |\r\n| (returns immediately)             | ...               |\r\n|                   |               |        done!      |\r\n| [other tools]    |               | enqueue(result) --+---> notificationQueue\r\n| [LLM call] <-----+------- drain  |                   |     (ConcurrentLinkedQueue)\r\n|  ^inject results |       queue   +-------------------+\r\n+-------------------+               +-------------------+\r\n                                     thread-2: bash\r\n                                     | pytest\r\n                                     | ...              |\r\n                                     | enqueue(result) -+---> notificationQueue\r\n                                     +-------------------+\r\n\r\nTimeline:\r\nt0: background_run(\"npm install\")     → 立即返回 \"task 00000001 started\"\r\nt0: background_run(\"pytest\")          → 立即返回 \"task 00000002 started\"\r\nt0: write_file(\"config.yaml\", ...)    → 同步执行，完成\r\nt1: [下次 LLM 调用前]\r\n    drainNotifications()              → 注入 task_00000001 和 task_00000002 的结果\r\n    LLM 看到结果 → 继续思考下一步\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. BackgroundManager — 后台任务管理器\r\n\r\n```java\r\npublic class BackgroundManager {\r\n    private final Map<String, BackgroundTaskInput> tasks;          // ConcurrentHashMap\r\n    private final Queue<BackgroundNotification> notificationQueue; // ConcurrentLinkedQueue\r\n    private final ExecutorService executor;                        // CachedThreadPool\r\n    private final AtomicInteger taskIdCounter;                     // 8位十六进制ID\r\n\r\n    public BackgroundManager() {\r\n        this.tasks = new ConcurrentHashMap<>();\r\n        this.notificationQueue = new ConcurrentLinkedQueue<>();\r\n        this.executor = Executors.newCachedThreadPool(r -> {\r\n            Thread t = new Thread(r, \"bg-task-\" + r.hashCode());\r\n            t.setDaemon(true);\r\n            return t;\r\n        });\r\n        this.taskIdCounter = new AtomicInteger(0);\r\n    }\r\n}\r\n```\r\n\r\n**核心方法：**\r\n\r\n```java\r\n// 启动后台任务 — 立即返回，不阻塞\r\npublic String runInBackground(String command) {\r\n    String taskId = generateTaskId();  // 8位十六进制: 00000001\r\n    BackgroundTaskInput task = new BackgroundTaskInput();\r\n    task.setTaskId(taskId);\r\n    task.setCommand(command);\r\n    task.setStatus(\"running\");\r\n    task.setStartTime(LocalDateTime.now().toString());\r\n    tasks.put(taskId, task);\r\n    executor.submit(() -> executeTask(task));\r\n    return String.format(\"后台任务 %s 已启动，命令: %s\", taskId, command);\r\n}\r\n\r\n// 执行任务 — 带超时和OS适配\r\nprivate void executeTask(BackgroundTaskInput task) {\r\n    String os = System.getProperty(\"os.name\").toLowerCase();\r\n    ProcessBuilder pb;\r\n    if (os.contains(\"win\")) {\r\n        pb = new ProcessBuilder(\"cmd.exe\", \"/c\", task.getCommand());\r\n    } else {\r\n        pb = new ProcessBuilder(\"bash\", \"-c\", task.getCommand());\r\n    }\r\n    pb.directory(new File(AIConstants.ROOT));\r\n    pb.redirectErrorStream(false);\r\n    Process process = pb.start();\r\n\r\n    boolean finished = process.waitFor(300, TimeUnit.SECONDS);\r\n    if (!finished) {\r\n        process.destroyForcibly();\r\n        task.setStatus(\"timeout\");\r\n    } else {\r\n        task.setStatus(process.exitValue() == 0 ? \"completed\" : \"error\");\r\n    }\r\n    // 结果截断到 50000 字符\r\n    task.setResult(truncateResult(output, 50000));\r\n    tasks.put(task.getTaskId(), task);\r\n    enqueueNotification(task);\r\n}\r\n\r\n// 生成任务ID — 8位十六进制\r\nprivate String generateTaskId() {\r\n    return String.format(\"%08x\", taskIdCounter.incrementAndGet() & 0xFFFFFFFFL);\r\n}\r\n```\r\n\r\n### 2. 通知队列 — 结果注入\r\n\r\n```java\r\n// 排空通知队列 — 每次LLM调用前调用\r\npublic List<BackgroundNotification> drainNotifications() {\r\n    List<BackgroundNotification> notifications = new ArrayList<>();\r\n    BackgroundNotification notif;\r\n    while ((notif = notificationQueue.poll()) != null) {\r\n        notifications.add(notif);\r\n    }\r\n    return notifications;\r\n}\r\n\r\n// 静态注入方法 — 在 chatMessage 中调用\r\npublic static void injectBackgroundNotifications(\r\n        List<MessageParam> messageParams,\r\n        BackgroundManager backgroundManager) {\r\n    List<BackgroundNotification> notifications = backgroundManager.drainNotifications();\r\n    if (!notifications.isEmpty()) {\r\n        String notifText = notifications.stream()\r\n            .map(n -> String.format(\"[后台任务:%s] %s: %s\",\r\n                n.getTaskId(), getStatusChinese(n.getStatus()), n.getResult()))\r\n            .collect(Collectors.joining(\"\\n\"));\r\n        messageParams.add(MessageParam.builder().role(MessageParam.Role.USER)\r\n            .content(\"<后台任务结果>\\n\" + notifText + \"\\n</后台任务结果>\").build());\r\n        messageParams.add(MessageParam.builder().role(MessageParam.Role.ASSISTANT)\r\n            .content(\"已记录后台任务结果。\").build());\r\n    }\r\n}\r\n```\r\n\r\n### 3. chatMessage 覆写 — 注入 + 三层压缩\r\n\r\n```java\r\n@Override\r\nprotected Message chatMessage(List<MessageParam> messageParams) {\r\n    BackgroundManager.injectBackgroundNotifications(messageParams, backgroundManager);\r\n    if (compactor != null) {\r\n        compactor.microCompact(messageParams);\r\n        compactor.autoCompact(messageParams);\r\n    }\r\n    return super.chatMessage(messageParams);\r\n}\r\n```\r\n\r\n### 4. BackgroundTaskInput — 任务数据模型\r\n\r\n```java\r\npublic class BackgroundTaskInput {\r\n    private String taskId;    // 8位十六进制: \"00000001\"\r\n    private String command;   // 原始命令\r\n    private String status;    // running, completed, timeout, error\r\n    private String result;    // 截断到 50000 字符\r\n    private String startTime; // 启动时间\r\n\r\n    public String getDisplayString() {\r\n        String statusChinese;\r\n        switch (status) {\r\n            case \"running\": statusChinese = \"运行中\"; break;\r\n            case \"completed\": statusChinese = \"已完成\"; break;\r\n            case \"timeout\": statusChinese = \"超时\"; break;\r\n            case \"error\": statusChinese = \"错误\"; break;\r\n            default: statusChinese = status;\r\n        }\r\n        return String.format(\"[%s] %s: %s\", statusChinese, taskId, command);\r\n    }\r\n}\r\n```\r\n\r\n### 5. 静态字段桥接\r\n\r\n```java\r\npublic class Agent08 extends ZQAgent {\r\n    public static BackgroundManager backgroundManager;\r\n    public static TaskManager taskManager;\r\n    public static ContextCompactor compactor;\r\n    public static TodoManager todoManager;\r\n    public static SkillLoader skillLoader;\r\n    public static boolean manualCompactRequested = false;\r\n}\r\n```\r\n\r\n### 6. 两个后台任务工具\r\n\r\n```java\r\n// 启动后台任务\r\nbackground_run(command=\"npm install\")\r\n// → \"后台任务 00000001 已启动，命令: npm install\"\r\n\r\n// 查询状态\r\ncheck_background(taskId=\"00000001\")  // 查询特定任务\r\ncheck_background()                   // 列出所有任务\r\n```\r\n\r\n### 7. 资源保护\r\n\r\n```java\r\n// 自动清理 — 保留最近 100 个任务\r\npublic void cleanupOldTasks() {\r\n    if (tasks.size() > 100) {\r\n        List<BackgroundTaskInput> sorted = tasks.values().stream()\r\n            .filter(t -> !\"running\".equals(t.getStatus()))\r\n            .sorted(Comparator.comparing(BackgroundTaskInput::getStartTime))\r\n            .collect(Collectors.toList());\r\n        int removeCount = tasks.size() - 100;\r\n        for (int i = 0; i < removeCount && i < sorted.size(); i++) {\r\n            tasks.remove(sorted.get(i).getTaskId());\r\n        }\r\n    }\r\n}\r\n\r\n// 优雅关闭 — 5秒宽限期\r\npublic void shutdown() {\r\n    executor.shutdown();\r\n    if (!executor.awaitTermination(5, TimeUnit.SECONDS)) {\r\n        executor.shutdownNow();\r\n    }\r\n}\r\n```\r\n\r\n## 工具数量\r\n\r\n| 版本 | 工具数 | 新增工具 |\r\n|------|--------|----------|\r\n| s07 | 14 | — |\r\n| s08 | 16 | background_run, check_background |\r\n\r\n## 相对于 s07 的变更\r\n\r\n| 组件 | s07 | s08 |\r\n|------|-----|-----|\r\n| 工具数 | 14 | 16 (+2 后台工具) |\r\n| 执行方式 | 仅阻塞（bash） | 阻塞 + 后台线程 |\r\n| 通知机制 | 无 | ConcurrentLinkedQueue + 自动注入 |\r\n| 并发能力 | 无 | CachedThreadPool 守护线程 |\r\n| 任务模型 | DAG 任务图（TaskInput） | + BackgroundTaskInput（4 状态） |\r\n| ID 格式 | 自增整数 | 8位十六进制（AtomicInteger） |\r\n| 超时控制 | 无 | 300 秒 + destroyForcibly |\r\n| 结果处理 | 直接返回 | 截断 50000 字符 + 队列通知 |\r\n| 资源保护 | 无 | cleanupOldTasks（100 上限）+ shutdown（5s 宽限） |\r\n| OS 适配 | 无 | cmd.exe /c vs bash -c |\r\n| 新增类 | — | BackgroundManager, BackgroundNotification, BackgroundRunInput, BackgroundTaskInput, CheckBackgroundInput |\r\n| Agent 字段 | 5 个 public static | 6 个 public static (+backgroundManager) |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-08\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent08\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `在后台运行 \"sleep 5 && echo done\"，同时帮我创建一个 hello.txt 文件`\r\n2. `同时启动 3 个后台任务：\"sleep 2\"、\"sleep 4\"、\"sleep 6\"，然后检查它们的状态`\r\n3. `在后台运行 npm install，等它完成后告诉我结果`\r\n4. `启动一个后台任务运行 pytest，同时帮我搜索代码中的 TODO 注释`\r\n5. `检查所有后台任务的状态，清理已完成的任务`\r\n6. `启动一个超长运行的后台任务，看看超时机制是否生效（sleep 999）`\r\n7. `并行运行代码编译和测试，完成后分析测试结果`\r\n8. `创建一个任务图，用后台任务执行其中的可并行部分`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s09",
    "locale": "zh",
    "title": "s09: Permission System (权限系统)",
    "content": "# s09: Permission System（权限系统）\r\n\r\n`s01 > s02 > s03 > s04 > s05 > s06 > s07 > s08 > [ s09 ] s10 > s11 > s12 ...`\r\n\r\n> *\"三道闸门：黑名单直接拒、规则匹配拒、破坏性命令问用户\"* — 在工具真正执行前拦截。\r\n\r\n## 问题\r\n\r\ns08 的 agent 已经能 bash、写文件、跑后台任务。但模型偶尔会做出危险动作：`rm -rf /`、`sudo xxx`、`git push --force`、把文件写到工作区之外。这些动作一旦执行就是不可逆的真实后果。\r\n\r\n**核心矛盾：**\r\n- ❌ 没有任何拦截层，工具调用直达 shell / 文件系统\r\n- ❌ 危险命令（`rm -rf`、`format`）一旦执行无法回滚\r\n- ❌ 路径越界（`../escape.txt`）会写到 workspace 之外\r\n- ❌ 破坏性但合理的命令（`rm temp.log`）一刀切禁止又太死板\r\n\r\n## 解决方案\r\n\r\n![Permission Overview](/agent/permission-overview.svg)\r\n\r\n引入 **PermissionChecker** — 一条三道闸门的流水线，所有工具调用在 `invokeTool` 之前必须先过闸：\r\n\r\n```\r\n工具调用 (toolName, input)\r\n   │\r\n   ├─ 闸门 1: denyList    （rm -rf /、sudo、format C:、del /f /s /q C:\\）  → 命中 → DENY\r\n   ├─ 闸门 2: rules       （write_file 路径越出 ROOT）                     → 命中 → DENY\r\n   └─ 闸门 3: askUser     （rm xxx、del xxx、git push --force）            → y/N → ALLOW/DENY\r\n   │\r\n   全部通过 → 执行工具\r\n```\r\n\r\n`PermissionChecker.check()` 返回 `Decision`（ALLOW / DENY / ASK），DENY 时把拒绝原因回灌给模型，让模型自己改方案；ASK 时通过 `Scanner` 弹出 `[权限确认]` 让用户决定。\r\n\r\n![Permission Pipeline](/agent/permission-pipeline.svg)\r\n\r\n## 工作原理\r\n\r\n### 1. Decision — 决策类型 + 拒绝原因\r\n\r\n```java\r\npublic class Decision {\r\n    public enum Type { ALLOW, DENY, ASK }\r\n    public final Type type;\r\n    public final String reason;   // 拒绝/询问时给模型和用户看的解释\r\n\r\n    public static Decision allow()              { return new Decision(Type.ALLOW, null); }\r\n    public static Decision deny(String reason)  { return new Decision(Type.DENY, reason); }\r\n    public static Decision ask(String reason)   { return new Decision(Type.ASK, reason); }\r\n}\r\n```\r\n\r\n### 2. PermissionRules — 静态黑名单 + 路径越界\r\n\r\n```java\r\npublic class PermissionRules {\r\n    // 闸门 1：denyList，命中即拒\r\n    private static final List<String> DENY = List.of(\r\n        \"rm -rf /\", \"rm -rf ~\", \"rm -rf /*\",\r\n        \"sudo \", \"shutdown\", \"reboot\",\r\n        \"format C:\", \"format c:\",\r\n        \"del /f /s /q C:\\\\\", \"mkfs\"\r\n    );\r\n\r\n    public static Decision checkDenyList(String tool, String input) {\r\n        if (\"bash\".equals(tool)) {\r\n            for (String bad : DENY) {\r\n                if (input.contains(bad)) return Decision.deny(\"命中黑名单: \" + bad);\r\n            }\r\n        }\r\n        return Decision.allow();\r\n    }\r\n\r\n    // 闸门 2：路径越界规则\r\n    public static Decision checkPathEscape(String tool, String input) {\r\n        if (\"write_file\".equals(tool) || \"edit_file\".equals(tool)) {\r\n            String path = parsePath(input);\r\n            Path resolved = ROOT.resolve(path).normalize();\r\n            if (!resolved.startsWith(ROOT)) {\r\n                return Decision.deny(\"路径越界: \" + path);\r\n            }\r\n        }\r\n        return Decision.allow();\r\n    }\r\n}\r\n```\r\n\r\n### 3. PermissionChecker — 三闸门流水线\r\n\r\n```java\r\npublic class PermissionChecker {\r\n    private final Scanner stdin;   // 用于 ASK 阶段\r\n\r\n    public Decision checkPermission(String toolName, String input) {\r\n        // 闸门 1\r\n        Decision d1 = PermissionRules.checkDenyList(toolName, input);\r\n        if (d1.type == Decision.Type.DENY) return d1;\r\n\r\n        // 闸门 2\r\n        Decision d2 = PermissionRules.checkPathEscape(toolName, input);\r\n        if (d2.type == Decision.Type.DENY) return d2;\r\n\r\n        // 闸门 3：破坏性命令交互式确认\r\n        if (isDestructive(toolName, input)) {\r\n            System.out.print(\"[权限确认] \" + input + \" (y/N) \");\r\n            String line = stdin.nextLine().trim().toLowerCase();\r\n            return \"y\".equals(line)\r\n                ? Decision.allow()\r\n                : Decision.deny(\"用户拒绝\");\r\n        }\r\n        return Decision.allow();\r\n    }\r\n\r\n    private boolean isDestructive(String tool, String input) {\r\n        if (!\"bash\".equals(tool)) return false;\r\n        return input.matches(\".*(\\\\brm\\\\b|\\\\bdel\\\\b|git push --force|chmod -R).*\");\r\n    }\r\n}\r\n```\r\n\r\n### 4. ZQAgent 集成 — 工具执行前先过闸\r\n\r\n`ZQAgent` 新增 `permissionChecker` 字段，在分发工具前调用：\r\n\r\n```java\r\nfor (ToolUseBlock use : toolUses) {\r\n    Decision d = permissionChecker.checkPermission(use.name(), inputJson);\r\n    if (d.type == Decision.Type.DENY) {\r\n        // 把拒绝原因作为 isError=true 的 ToolResult 回灌给模型\r\n        results.add(toolResult(use.id(), \"[permission denied] \" + d.reason, true));\r\n        continue;\r\n    }\r\n    Object output = invokeTool(use.name(), inputJson);  // 真正执行\r\n    results.add(toolResult(use.id(), output));\r\n}\r\n```\r\n\r\n模型看到拒绝原因后会自己换方案：被 `rm -rf` 拒绝 → 改用 `rm` 单文件；路径越界 → 改写到 workspace 内。\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s08 相同 |\r\n\r\n权限检查不是工具，是工具调用前的拦截层。\r\n\r\n## 相对 s08 的变更\r\n\r\n| 组件 | s08 | s09 |\r\n|------|-----|-----|\r\n| 拦截层 | 无 | denyList → rules → askUser 三闸门 |\r\n| 新增类 | — | Decision、PermissionRules、PermissionChecker |\r\n| 危险命令 | 直达 shell | 命中黑名单直接拒，路径越界直接拒 |\r\n| 破坏性命令 | 直达 shell | 弹 y/N 交互确认 |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-09-permission -am compile\r\nmvn -pl hoppinzq-module-agent-09-permission exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentPermission\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `请执行 rm -rf /tmp/x` — 期望：denyList 拦截，模型改方案\r\n2. `请执行 sudo apt update` — 期望：denyList 拦截\r\n3. `把内容写到 ../escape.txt` — 期望：路径越界规则拦截\r\n4. `用 del 删除 old.log` — 期望：弹出 `[权限确认]` 提示\r\n5. `读 README.md` — 期望：正常执行\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s10",
    "locale": "zh",
    "title": "s10: Hooks (钩子机制)",
    "content": "# s10: Hooks（钩子机制）\r\n\r\n`s01 > ... > s08 > s09 > [ s10 ] s11 > s12 ...`\r\n\r\n> *\"把权限检查从硬编码抽成可注册的钩子\"* — 4 个事件点 + HookRegistry，扩展点不再写死。\r\n\r\n## 问题\r\n\r\ns09 的 `PermissionChecker` 是硬编码进 `ZQAgent` 的：每加一种检查（日志、大输出警告、统计）都要改 `ZQAgent` 主循环。一个 agent 想要审计日志，另一个想要输入重写，第三个想要工具统计 — 这些需求彼此正交，但都得改同一份循环代码。\r\n\r\n**核心矛盾：**\r\n- ❌ 权限、日志、统计、改写都堆在主循环里\r\n- ❌ 加一个扩展点要改 ZQAgent 源码\r\n- ❌ 无法按需启用/禁用功能（每次都全量编译）\r\n\r\n## 解决方案\r\n\r\n![Hooks Overview](/agent/hooks-overview.svg)\r\n\r\n引入 **HookRegistry** — 把扩展点抽成 4 个固定事件，每个事件挂一串 `HookCallback`。主循环只负责在固定时机 `trigger(event, ctx)`，由注册的回调决定做什么：\r\n\r\n```\r\n事件点                          非-null 返回值的影响\r\n─────────────────────────────────────────────────────────\r\nUSER_PROMPT_SUBMIT  (读入输入后)  → 用返回值替换原 prompt\r\nPRE_TOOL_USE        (工具执行前)  → 跳过执行，返回值作为 ToolResult\r\nPOST_TOOL_USE       (工具执行后)  → 仅副作用（不影响主流程）\r\nSTOP                (本轮将退出)  → 仅副作用\r\n```\r\n\r\n权限检查从硬编码 if 改成 `register(PRE_TOOL_USE, new PermissionHook())`，要审计就 `register(POST_TOOL_USE, new LogHook())`，互不影响。\r\n\r\n## 工作原理\r\n\r\n### 1. HookEvent + HookContext — 事件枚举 + 上下文\r\n\r\n```java\r\npublic enum HookEvent {\r\n    USER_PROMPT_SUBMIT, PRE_TOOL_USE, POST_TOOL_USE, STOP\r\n}\r\n\r\npublic class HookContext {\r\n    public String userInput;       // USER_PROMPT_SUBMIT\r\n    public String toolName;        // PRE/POST_TOOL_USE\r\n    public String toolInput;\r\n    public String toolResult;      // POST_TOOL_USE\r\n    public Map<String, Object> extra = new HashMap<>();\r\n}\r\n```\r\n\r\n### 2. HookCallback — 函数式接口\r\n\r\n```java\r\n@FunctionalInterface\r\npublic interface HookCallback {\r\n    /** 返回非 null 表示拦截/改写；返回 null 表示仅副作用或放行。 */\r\n    String apply(HookContext ctx);\r\n}\r\n```\r\n\r\n### 3. HookRegistry — 注册 + 触发\r\n\r\n```java\r\npublic class HookRegistry {\r\n    private final Map<HookEvent, List<HookCallback>> hooks = new EnumMap<>(HookEvent.class);\r\n\r\n    public void register(HookEvent event, HookCallback cb) {\r\n        hooks.computeIfAbsent(event, k -> new ArrayList<>()).add(cb);\r\n    }\r\n\r\n    /** 顺序触发，返回第一个非 null 结果；全部返回 null 时返回 null。 */\r\n    public String trigger(HookEvent event, HookContext ctx) {\r\n        List<HookCallback> list = hooks.get(event);\r\n        if (list == null) return null;\r\n        for (HookCallback cb : list) {\r\n            String result = cb.apply(ctx);\r\n            if (result != null) return result;\r\n        }\r\n        return null;\r\n    }\r\n}\r\n```\r\n\r\n### 4. 内置四个钩子示例\r\n\r\n```java\r\n// PermissionHook：把 s09 的检查改写为 PRE_TOOL_USE 钩子\r\npublic class PermissionHook implements HookCallback {\r\n    public String apply(HookContext ctx) {\r\n        Decision d = PermissionChecker.check(ctx.toolName, ctx.toolInput);\r\n        return d.type == Decision.Type.DENY\r\n            ? \"[hook 阻断] \" + d.reason\r\n            : null;\r\n    }\r\n}\r\n\r\n// LogHook：所有 4 个事件都挂，打审计日志\r\npublic class LogHook implements HookCallback {\r\n    public String apply(HookContext ctx) { /* print audit */ return null; }\r\n}\r\n\r\n// LargeOutputHook：POST_TOOL_USE 时检查长度\r\npublic class LargeOutputHook implements HookCallback {\r\n    public String apply(HookContext ctx) {\r\n        if (ctx.toolResult != null && ctx.toolResult.length() > 4000) {\r\n            System.out.println(\"[warn] 输出超过 4000 字符\");\r\n        }\r\n        return null;\r\n    }\r\n}\r\n\r\n// SummaryHook：STOP 时统计本轮工具调用次数\r\npublic class SummaryHook implements HookCallback {\r\n    public String apply(HookContext ctx) { /* print count */ return null; }\r\n}\r\n```\r\n\r\n### 5. ZQAgent 集成 — 4 个事件点的触发位置\r\n\r\n```java\r\n// USER_PROMPT_SUBMIT\r\nString rewritten = hookRegistry.trigger(USER_PROMPT_SUBMIT, ctxWith(userInput));\r\nif (rewritten != null) userInput = rewritten;\r\n\r\nwhile (true) {\r\n    // ... LLM call ...\r\n    for (ToolUseBlock use : toolUses) {\r\n        // PRE_TOOL_USE\r\n        String blocked = hookRegistry.trigger(PRE_TOOL_USE, ctxWith(use));\r\n        if (blocked != null) {\r\n            results.add(toolResult(use.id(), blocked));   // 跳过 invokeTool\r\n            continue;\r\n        }\r\n        Object out = invokeTool(use.name(), input);\r\n        // POST_TOOL_USE\r\n        hookRegistry.trigger(POST_TOOL_USE, ctxWith(use, out));\r\n        results.add(toolResult(use.id(), out));\r\n    }\r\n    if (stopReason != \"tool_use\") {\r\n        hookRegistry.trigger(STOP, ctx);\r\n        return;\r\n    }\r\n}\r\n```\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s09 相同 |\r\n\r\n钩子是事件扩展点，不是工具。\r\n\r\n## 相对 s09 的变更\r\n\r\n| 组件 | s09 | s10 |\r\n|------|-----|-----|\r\n| 权限检查 | 硬编码进 ZQAgent | 改写成 PRE_TOOL_USE 钩子，可插拔 |\r\n| 扩展点 | 改源码 | 4 个事件 × N 个回调 |\r\n| 新增类 | — | HookEvent、HookContext、HookCallback、HookRegistry + 4 个内置钩子 |\r\n| 可组合性 | 一份代码一种行为 | 同一份代码按需挂不同钩子组合 |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-10-hooks -am compile\r\nmvn -pl hoppinzq-module-agent-10-hooks exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentHooks\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `执行 rm -rf /tmp/x` — 期望：`[hook 阻断]` 由 PermissionHook 触发\r\n2. `列出当前目录文件` — 期望：LogHook 打印 pre/post 审计；SummaryHook 末尾输出本轮次数\r\n3. `读取一个大文件` — 期望：LargeOutputHook 在 >4000 字符时打印警告\r\n4. `读 README.md 然后总结` — 期望：STOP 时 SummaryHook 输出本轮工具调用次数\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s11",
    "locale": "zh",
    "title": "s11: Persistent Memory (持久化记忆)",
    "content": "# s11: Memory（持久化记忆）\r\n\r\n`s01 > ... > s09 > s10 > [ s11 ] s12 ...`\r\n\r\n> *\"压缩会丢细节，要有一层不丢的\"* — 文件仓库 + 索引 + 按需加载，跨压缩、跨会话。\r\n\r\n## 问题\r\n\r\ns06 的上下文压缩会把\"用户偏好 tab 缩进\"压成\"用户有代码风格偏好\"，细节被吃掉。新开一个会话，连摘要也没了 — agent 不知道用户上次说过什么。LLM 没有持久状态，所有信息都在上下文窗口里；上下文满了要压缩，压缩就有损。\r\n\r\n需要一层**不参与压缩、跨会话保留**的存储。\r\n\r\n## 解决方案\r\n\r\n![Memory Overview](/agent/memory-overview.svg)\r\n\r\n存储选文件系统：`.memory/` 目录下每个记忆一个 `.md`，YAML frontmatter 记录 `type/name/tags`。文件多了用索引 `MEMORY.md` 一行一条，注入到 SYSTEM prompt。\r\n\r\n```\r\n每轮 LLM 调用前              每轮工具循环退出后\r\n       │                              │\r\n       ▼                              ▼\r\n MemorySelector              MemoryExtractor\r\n 读 MEMORY.md 索引            从对话中抽事实\r\n   ↓ 调 LLM 选相关项           ↓ 调 LLM 输出 TYPE|name|tags|body\r\n 读 .memory/*.md 正文          ↓ 写入 .memory/{type}_{name}.md\r\n   ↓ 注入 systemPrompt          ↓ 更新 MEMORY.md 索引\r\n                                ↓ 文件数 ≥10 触发 consolidate\r\n```\r\n\r\n四类记忆，各有用途：\r\n\r\n| type | 回答什么 | 示例 |\r\n|------|---------|------|\r\n| user | 你是谁 | \"偏好 tab 缩进\" |\r\n| feedback | 怎么做事 | \"别 mock 数据库\" |\r\n| project | 正在发生什么 | \"auth 重写是合规驱动\" |\r\n| reference | 东西在哪找 | \"pipeline bug 在 Linear INGEST\" |\r\n\r\n## 工作原理\r\n\r\n![Memory Subsystems](/agent/memory-subsystems.svg)\r\n\r\n### 1. 存储格式 — Markdown + YAML frontmatter\r\n\r\n`.memory/user_prefers_vim.md`：\r\n\r\n```markdown\r\n---\r\ntype: user\r\nname: prefers_vim\r\ntags: [editor, vim]\r\ncreated: 2026-06-19T10:15:30Z\r\n---\r\n\r\n用户偏好使用 vim 编辑器。\r\n```\r\n\r\n`MEMORY.md` 是索引，一行一个链接：\r\n\r\n```markdown\r\n# Memory Index\r\n\r\n- [user] prefers_vim #editor #vim — 用户偏好使用 vim 编辑器\r\n- [feedback] no_emoji #style — 不要在回复中使用 emoji\r\n```\r\n\r\n写入新记忆时自动重建索引：\r\n\r\n```java\r\npublic void writeMemoryFile(String name, String type, String tags, String body) {\r\n    String slug = type + \"_\" + name.replace(\" \", \"_\");\r\n    Path file = MEMORY_DIR.resolve(slug + \".md\");\r\n    String content = \"---\\ntype: \" + type + \"\\nname: \" + name + \"\\ntags: \" + tags + \"\\n---\\n\\n\" + body;\r\n    Files.writeString(file, content);\r\n    rebuildIndex();\r\n}\r\n```\r\n\r\n### 2. 加载 — MemorySelector 每轮选相关记忆\r\n\r\n索引常驻 SYSTEM（可被 prompt cache 缓存），文件内容按需注入。每次用户请求开始时，`MemorySelector` 把最近对话和记忆清单（name + tags）一起发给 LLM 做一次轻量 side-query，选出相关的文件名，再读文件内容临时注入到当前 user turn。最多 5 条，控制开销：\r\n\r\n```java\r\npublic List<Path> selectRelevant(String userInput, List<MemoryRecord> catalog) {\r\n    if (catalog.isEmpty()) return List.of();\r\n    String list = formatCatalog(catalog);   // \"0: prefers_vim — 用户偏好使用 vim 编辑器\"\r\n    String resp = askLlm(\r\n        \"Select relevant memory indices. Return JSON array.\\n\\n\" +\r\n        \"User input:\\n\" + userInput + \"\\n\\nMemory catalog:\\n\" + list);\r\n    List<Integer> indices = parseJsonArray(resp);\r\n    return indices.stream().map(catalog::get).map(MemoryRecord::path).toList();\r\n}\r\n```\r\n\r\nside-query 失败（API 错误、JSON 解析失败）时降级到关键词匹配 name + tags。\r\n\r\n### 3. 写入 — MemoryExtractor 每轮结束后抽记忆\r\n\r\n用户不会每次都说\"记住这个\"。偏好散落在正常对话中：\"用 tab 比空格好\"、\"以后都用单引号\"。`extractAndSave` 在每轮工具循环退出后（说明对话告一段落）触发：\r\n\r\n```java\r\n// ZQAgent 主循环退出后\r\nif (stopReason != TOOL_USE) {\r\n    memoryExtractor.extractAndSave(history);\r\n    memoryStore.consolidateIfNeeded();\r\n}\r\n```\r\n\r\n提取前先检查已有记忆避免重复，prompt 要求 LLM 返回 `{type, name, tags, body}` 的 JSON 数组，只有确实有新信息时才写文件。\r\n\r\n### 4. 整理 — 文件数 ≥10 触发 consolidate\r\n\r\n记忆文件会积累。`consolidateIfNeeded()` 在文件数达到阈值（默认 10）时让 LLM 去重、合并矛盾、淘汰过时记忆：\r\n\r\n```java\r\nprivate static final int CONSOLIDATE_THRESHOLD = 10;\r\n\r\npublic void consolidateIfNeeded() {\r\n    List<Path> files = listMemoryFiles();\r\n    if (files.size() < CONSOLIDATE_THRESHOLD) return;\r\n    // 把所有记忆发给 LLM，拿回去重后的列表\r\n    List<MemoryRecord> merged = askLlmToMerge(files);\r\n    rewriteMemoryDir(merged);\r\n    rebuildIndex();\r\n}\r\n```\r\n\r\nClaude Code 把这个过程叫 Dream，实际有四层门控（时间间隔、扫描节流、会话数、文件锁）；教学版简化为文件数阈值。\r\n\r\n### 5. Memory 适合保存什么\r\n\r\n跨会话仍然有用的信息：用户偏好、反复出现的反馈、项目背景、常用入口和排查线索。它关注\"以后还会用到什么\"，通过索引 + 按需加载把这些信息带回当前对话。session memory（s06）关注同一会话内的连续性，两者配合使用。\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s10 相同 |\r\n\r\n记忆系统不是工具，是注入和提取的两侧管线。\r\n\r\n## 相对 s10 的变更\r\n\r\n| 组件 | s10 | s11 |\r\n|------|-----|-----|\r\n| 记忆能力 | 无 | 存储 + 加载 + 提取 + 整理 |\r\n| 新增类 | — | MemoryRecord、MemoryStore、MemorySelector、MemoryExtractor |\r\n| 存储 | — | `.memory/MEMORY.md` 索引 + `.memory/*.md` 文件 |\r\n| ZQAgent | 硬编码 system | 每轮允许传不同 `effectiveSystem`，注入相关记忆 |\r\n| 循环 | — | 入口注入 + 退出提取 + 定期整理 |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-11-memory -am compile\r\nmvn -pl hoppinzq-module-agent-11-memory exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentMemory\r\n```\r\n\r\n试试这些 prompt（分多轮、跨会话输入）：\r\n\r\n1. 第一次会话：`我喜欢用 vim 编辑器` — 退出后检查 `.memory/user_prefers_vim.md` 与 `MEMORY.md` 是否生成\r\n2. 重启程序：`我喜欢用什么编辑器？` — 期望：看到 `[memory] 已注入相关记忆`，agent 从记忆读出 vim\r\n3. 持续交互 10+ 次 — 期望：触发 `[memory] 文件数已到 N，建议 consolidate`\r\n4. `别在回复里加 emoji` — 期望：生成 `feedback_no_emoji.md`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s12",
    "locale": "zh",
    "title": "s12: Error Recovery (错误恢复)",
    "content": "# s12: Error Recovery（错误恢复）\r\n\r\n`s01 > ... > s10 > s11 > [ s12 ] > s13 ...`\r\n\r\n> *\"输出被截断就升级、上下文溢出就压缩、限流就退避重试\"* — 三条恢复路径让 agent 在异常下继续运行。\r\n\r\n## 问题\r\n\r\n到 s11 为止，agent 一旦 LLM 调用失败就崩：输出超长被截断（`stop_reason == max_tokens`）、上下文超限（`prompt_too_long`）、API 限流（429 / 529）、网络抖动。这些异常在生产中很常见，但教学版之前要么直接抛出，要么简单重试。\r\n\r\n**核心矛盾：**\r\n- ❌ 输出截断 → 模型话没说完，结果残缺\r\n- ❌ 上下文溢出 → 直接崩，无法继续\r\n- ❌ 限流/抖动 → 没有退避策略，连续失败\r\n- ❌ 高峰过载（529）→ 不知道切 fallback 模型\r\n\r\n## 解决方案\r\n\r\n![Error Recovery Overview](/agent/error-recovery-overview.svg)\r\n\r\n引入 **RetryWrapper** — 包装 LLM 调用，把异常分类后路由到三条恢复路径：\r\n\r\n| 触发条件 | 恢复动作 |\r\n|---------|---------|\r\n| `stop_reason == max_tokens`（输出截断） | 升级 maxTokens 8K → 64K，让模型续写 |\r\n| `prompt_too_long`（上下文溢出） | 触发 `ReactiveCompactor.compact`：保留首条 user + 最近 6 条，中间摘要替代 |\r\n| 429 / 529 / 网络抖动 | 指数退避 `min(500×2^attempt, 32000) + jitter`；连续 3 次 529 切 fallback model |\r\n\r\n每条路径用 `RecoveryState` 跟踪尝试次数、当前 maxTokens、当前模型。\r\n\r\n## 工作原理\r\n\r\n### 1. RecoveryState — 跟踪恢复进度\r\n\r\n```java\r\npublic class RecoveryState {\r\n    public int attempt = 0;\r\n    public int consecutive529 = 0;\r\n    public int maxTokens;          // 当前 maxTokens（可能被升级）\r\n    public String model;            // 当前 model（可能切到 fallback）\r\n    public String fallbackModel;    // 可为 null\r\n}\r\n```\r\n\r\n### 2. ErrorClassifier — 异常分类\r\n\r\n把原始异常/响应映射到 5 种类型，决定走哪条路径：\r\n\r\n```java\r\npublic enum ErrorType {\r\n    PROMPT_TOO_LONG,   // → ReactiveCompactor\r\n    RATE_LIMIT,        // → 退避\r\n    OVERLOAD,          // 529 → 退避 + 可能切 fallback\r\n    TRANSIENT,         // 网络抖动 → 退避\r\n    FATAL              // 无法恢复，抛出\r\n}\r\n\r\npublic static ErrorType classify(Throwable t, String stopReason) {\r\n    if (stopReason != null && \"max_tokens\".equals(stopReason)) return MAX_TOKENS;\r\n    if (t.getMessage().contains(\"prompt_too_long\"))             return PROMPT_TOO_LONG;\r\n    if (status(t) == 429)                                       return RATE_LIMIT;\r\n    if (status(t) == 529)                                       return OVERLOAD;\r\n    if (t instanceof IOException)                               return TRANSIENT;\r\n    return FATAL;\r\n}\r\n```\r\n\r\n### 3. ReactiveCompactor — 紧急压缩\r\n\r\n`prompt_too_long` 触发，不动文件，只在内存压缩 `messageParams`：保留首条 user + 最近 6 条，中间用一次性 LLM 调用生成摘要替代：\r\n\r\n```java\r\npublic List<MessageParam> compact(List<MessageParam> msgs) {\r\n    if (msgs.size() <= 8) return msgs;\r\n    MessageParam head = msgs.get(0);\r\n    List<MessageParam> middle = msgs.subList(1, msgs.size() - 6);\r\n    List<MessageParam> tail = msgs.subList(msgs.size() - 6, msgs.size());\r\n    String summary = summarizeOnce(middle);   // 一次单独 LLM 调用\r\n    List<MessageParam> out = new ArrayList<>();\r\n    out.add(head);\r\n    out.add(new MessageParam(\"user\", \"[earlier conversation summary]\\n\" + summary));\r\n    out.addAll(tail);\r\n    return out;\r\n}\r\n```\r\n\r\n### 4. RetryWrapper — 三路径调度\r\n\r\n```java\r\npublic Response call(Function<RecoveryState, Response> paramBuilder,\r\n                     RecoveryState state,\r\n                     List<MessageParam> history) {\r\n    while (true) {\r\n        try {\r\n            Response resp = paramBuilder.apply(state).send();\r\n            state.consecutive529 = 0;\r\n            if (\"max_tokens\".equals(resp.stopReason)) {\r\n                // 路径 1：升级 maxTokens 续写\r\n                System.out.println(\"[recovery] 升级 maxTokens → 64000 并续写\");\r\n                state.maxTokens = 64000;\r\n                continue;\r\n            }\r\n            return resp;\r\n        } catch (Exception e) {\r\n            ErrorType t = ErrorClassifier.classify(e, null);\r\n            switch (t) {\r\n                case PROMPT_TOO_LONG:\r\n                    System.out.println(\"[reactive compact] 保留首条 + 最近 6 条\");\r\n                    history = ReactiveCompactor.compact(history);\r\n                    break;\r\n                case RATE_LIMIT:\r\n                case TRANSIENT:\r\n                case OVERLOAD:\r\n                    state.attempt++;\r\n                    if (t == OVERLOAD) state.consecutive529++;\r\n                    if (state.consecutive529 >= 3 && state.fallbackModel != null) {\r\n                        System.out.println(\"[recovery] 连续 529，切到 fallback 模型\");\r\n                        state.model = state.fallbackModel;\r\n                    }\r\n                    long backoff = Math.min(500L * (1L << state.attempt), 32000L);\r\n                    long jitter = (long)(Math.random() * 100);\r\n                    Thread.sleep(backoff + jitter);\r\n                    break;\r\n                case FATAL:\r\n                    throw e;\r\n            }\r\n        }\r\n    }\r\n}\r\n```\r\n\r\n### 5. ZQAgent 集成\r\n\r\n`ZQAgent` 新增 `retryWrapper / recoveryState / fallbackModel` 三字段。`chatMessage` 内部判断：有 `retryWrapper` 时走 `RetryWrapper.call(...)`，paramBuilder 接收 `RecoveryState` 从中取当前 model / maxTokens。RecoveryState 在首次调用时按 model / fallbackModel 懒初始化。\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s11 相同 |\r\n\r\n恢复逻辑包装在 LLM 调用外侧，不影响工具集。\r\n\r\n## 相对 s11 的变更\r\n\r\n| 组件 | s11 | s12 |\r\n|------|-----|-----|\r\n| 异常处理 | 直接抛 / 简单重试 | 三路径分类恢复 |\r\n| 新增类 | — | RecoveryState、ErrorClassifier、ReactiveCompactor、RetryWrapper |\r\n| max_tokens 截断 | 输出残缺 | 升级续写 |\r\n| prompt_too_long | 崩 | 内存压缩后重试 |\r\n| 429/529/抖动 | 崩 | 指数退避 + 可能切 fallback |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-12-errorrecovery -am compile\r\nmvn -pl hoppinzq-module-agent-12-errorrecovery exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentErrorRecovery\r\n```\r\n\r\n四个场景：\r\n\r\n1. **触发输出截断**：临时把 `AIConstants.MAX_TOKENS` 改到很小（如 64），问一个需要长回复的问题，看 `[recovery] 升级 maxTokens → 64000 并续写`\r\n2. **触发上下文压缩**：进行 10+ 轮长对话，下一轮命中 `prompt_too_long`，看 `[reactive compact]` 日志\r\n3. **触发退避**：临时把 `AIConstants.BASE_URL` 改到无效地址，看 `[recovery] 退避 Nms（base=X, jitter=Y）`\r\n4. **连续 529 切 fallback**：设置 `FALLBACK_MODEL` 非 null 后模拟连续 529，看 `[recovery] 连续 529，切到 fallback 模型 xxx`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s13",
    "locale": "zh",
    "title": "s13: MCP Protocol (MCP 协议)",
    "content": "# s13: MCP 工具集成（Model Context Protocol）\r\n\r\n`s01 > s02 > s03 > s04 > s05 > s06 > s07 > s08 > [ s09 ] > s10 > s11 > s12`\r\n\r\n> *\"让智能体接入外部世界\"* — 通过 MCP 协议，AI 不再局限于本地工具，可以调用任意 MCP 服务器提供的专业能力\r\n\r\n## 问题\r\n\r\ns01~s08 的工具都是**硬编码**的 6 个静态工具（bash、read_file、write_file、edit_file、list_files、content_search）。如果需要接入外部系统（数据库、论坛 API、搜索引擎、专业计算引擎），就得在 `ToolDefinition` 里一行行写 Java 代码，重新编译部署。每接入一个新系统，都是一次开发周期。\r\n\r\n**核心矛盾：**\r\n- ❌ 工具集固定，无法动态扩展\r\n- ❌ 接入外部系统需要改代码、重新编译\r\n- ❌ 不同系统有各自的认证、传输、协议细节\r\n- ❌ 没有标准化的工具发现和调用机制\r\n\r\n## 解决方案\r\n\r\n![MCP Architecture](/agent/mcp-architecture.svg)\r\n\r\n引入 **MCP（Model Context Protocol）** — Anthropic 提出的开放协议，标准化 AI 与外部工具的通信。Agent13 只需编辑 `mcp.json` 配置文件，即可动态加载任意 MCP 服务器提供的工具，无需改代码。\r\n\r\n```\r\n                    mcp.json (配置文件)\r\n                    +------------------+\r\n                    | mcpServers: {    |\r\n                    |   \"论坛MCP\": {   |\r\n                    |     command: ... |\r\n                    |     args: ...    |\r\n                    |   }              |\r\n                    |   \"搜索MCP\": {   |\r\n                    |     url: ...     |\r\n                    |   }              |\r\n                    | }                |\r\n                    +--------+---------+\r\n                             |\r\n                    +--------v---------+\r\n                    | McpConfigLoader  |\r\n                    | 解析配置          |\r\n                    | 自动判断传输类型   |\r\n                    +--------+---------+\r\n                             |\r\n                    +--------v---------+\r\n                    |    MCPAgent      |\r\n                    | 初始化客户端       |\r\n                    | (STDIO/SSE/HTTP) |\r\n                    +--------+---------+\r\n                             |\r\n              +--------------+--------------+\r\n              |              |              |\r\n     +--------v---+  +------v----+  +------v----+\r\n     | STDIO      |  | SSE       |  | Streamable |\r\n     | 进程通信    |  | (已废弃)   |  | HTTP       |\r\n     | 本地jar/脚本|  |           |  | 远程服务    |\r\n     +--------+---+  +-----------+  +------+----+\r\n              |                             |\r\n              +--------------+--------------+\r\n                             |\r\n                    +--------v---------+\r\n                    |   McpLoader      |\r\n                    | listTools()      |\r\n                    | convertToTool()  |\r\n                    | createExecute()  |\r\n                    +--------+---------+\r\n                             |\r\n                    +--------v---------+\r\n                    |  ZQAgent         |\r\n                    | 6 内置 + N MCP   |\r\n                    | 统一工具调用      |\r\n                    +------------------+\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. McpConfigLoader — 配置解析与传输类型自动检测\r\n\r\n从 `resources/mcp.json` 读取配置，自动判断传输方式：\r\n\r\n```java\r\npublic List<McpSetting> loadMcpSettings() {\r\n    InputStream inputStream = getClass().getResourceAsStream(\"/mcp.json\");\r\n    JsonNode rootNode = objectMapper.readTree(inputStream);\r\n    JsonNode serversNode = rootNode.get(\"mcpServers\");  // 对象，非数组\r\n\r\n    serversNode.fields().forEachRemaining(entry -> {\r\n        String serverName = entry.getKey();       // 键 = 服务器名称\r\n        JsonNode serverConfig = entry.getValue();  // 值 = 服务器配置\r\n\r\n        if (!isEnabled(serverConfig)) return;      // 支持 \"enabled\" 字段\r\n\r\n        McpSetting setting = parseMcpSetting(serverName, serverConfig);\r\n        settings.add(setting);\r\n    });\r\n    return settings;\r\n}\r\n```\r\n\r\n**自动传输检测规则：**\r\n\r\n```java\r\nif (serverConfig.has(\"command\")) {\r\n    // STDIO — 启动本地进程，通过标准输入输出通信\r\n    builder.transportType(TransportType.STDIO);\r\n    configureStdio(builder, serverConfig);  // 解析 command + args + env\r\n} else if (serverConfig.has(\"url\")) {\r\n    String url = serverConfig.get(\"url\").asText();\r\n    if (url.contains(\"/sse\")) {\r\n        builder.transportType(TransportType.SSE);          // 0.9.0 已废弃\r\n    } else {\r\n        builder.transportType(TransportType.Streamable_HTTP);  // 推荐的 HTTP 方式\r\n    }\r\n    configureHttp(builder, serverConfig);  // 解析 url + headers + authorization\r\n}\r\n```\r\n\r\n**mcp.json 配置格式（Claude 标准格式）：**\r\n\r\n```json\r\n{\r\n  \"mcpServers\": {\r\n    \"潍柴论坛MCP\": {\r\n      \"command\": \"java\",\r\n      \"args\": [\r\n        \"-Dfile.encoding=UTF-8\",\r\n        \"-jar\",\r\n        \"D:\\\\mcp\\\\hoppinzq-module-mcp-wcbbs-stdio-1.0.1-SNAPSHOT.jar\"\r\n      ],\r\n      \"env\": {\r\n        \"wc_bbs_token\": \"wc_bbs_Me4M9gPSwKpNlvziF37fBds8GuScfiTE\"\r\n      }\r\n    },\r\n    \"远程搜索服务\": {\r\n      \"url\": \"https://search-api.example.com/mcp\",\r\n      \"headers\": {\r\n        \"Authorization\": \"Bearer your-token-here\"\r\n      }\r\n    }\r\n  }\r\n}\r\n```\r\n\r\n### 2. MCPAgent — 客户端生命周期管理\r\n\r\n根据传输类型创建对应的 MCP 客户端，支持同步/异步两种模式：\r\n\r\n```java\r\npublic void initializeClient() {\r\n    mcpSettings.forEach(setting -> {\r\n        McpClientTransport transport;\r\n        switch (setting.getTransportType()) {\r\n            case STDIO:\r\n                transport = new StdioClientTransport(setting.getServerParameters());\r\n                break;\r\n            case Streamable_HTTP:\r\n                transport = new WebFluxSseClientTransport(\r\n                    WebClient.builder()\r\n                        .baseUrl(setting.getSseUrl())\r\n                        .defaultHeaders(h -> h.add(\"Authorization\", setting.getAuthorization())),\r\n                    OBJECT_MAPPER, McpSetting.sseEndpoint);\r\n                break;\r\n            default:\r\n                throw new RuntimeException(\"不支持的传输方式\");\r\n        }\r\n\r\n        if (setting.isSync()) {\r\n            McpSyncClient syncClient = McpClient.sync(transport)\r\n                .requestTimeout(setting.getTimeout())  // 默认 10 秒\r\n                .build();\r\n            syncClient.initialize();\r\n            setting.setMcpSyncClient(syncClient);\r\n        }\r\n        // 异步模式类似，使用 McpClient.async()...\r\n    });\r\n}\r\n```\r\n\r\n### 3. McpLoader — MCP Schema → Anthropic ToolDefinition 桥接\r\n\r\n核心转换逻辑：将 MCP 协议的工具描述转为 Anthropic SDK 可用的格式：\r\n\r\n```java\r\npublic List<ToolDefinition> loadTools() {\r\n    List<ToolDefinition> tools = new ArrayList<>();\r\n    for (McpSetting setting : mcpSettings) {\r\n        McpSchema.ListToolsResult mcpTools = setting.isSync()\r\n            ? setting.getMcpSyncClient().listTools()\r\n            : setting.getMcpAsyncClient().listTools().block();\r\n\r\n        for (McpSchema.Tool mcpTool : mcpTools.tools()) {\r\n            ToolDefinition toolDef = convertToToolDefinition(mcpTool, setting);\r\n            tools.add(toolDef);\r\n        }\r\n    }\r\n    return tools;\r\n}\r\n```\r\n\r\n**Schema 转换（DeepSeek 兼容版本）：**\r\n\r\n```java\r\nprivate ToolDefinition convertToToolDefinition(McpSchema.Tool mcpTool, McpSetting setting) {\r\n    McpSchema.JsonSchema jsonSchema = mcpTool.inputSchema();\r\n    ObjectNode propertiesNode = OBJECT_MAPPER.valueToTree(jsonSchema.properties());\r\n    List<String> requiredList = jsonSchema.required();\r\n\r\n    Tool.InputSchema.Builder schemaBuilder = Tool.InputSchema.builder()\r\n        .properties(com.anthropic.core.JsonValue.fromJsonNode(propertiesNode));\r\n    if (requiredList != null && !requiredList.isEmpty()) {\r\n        schemaBuilder.required(requiredList);\r\n    }\r\n\r\n    return ToolDefinition.builder()\r\n        .name(mcpTool.name())\r\n        .description(mcpTool.description())\r\n        .inputSchema(schemaBuilder.build())\r\n        .function(createExecuteFunction(setting))\r\n        .build();\r\n}\r\n```\r\n\r\n### 4. createExecuteFunction — MCP 工具执行闭包\r\n\r\n每个 MCP 工具的执行函数是一个闭包，捕获了对应的 `McpSetting`，通过 JSON 中转调用：\r\n\r\n```java\r\nprivate Function<String, String> createExecuteFunction(McpSetting setting) {\r\n    return new Function<String, String>() {\r\n        @Override\r\n        public String apply(String inputJson) {\r\n            AtomicReference<String> result = new AtomicReference<>(\"\");\r\n\r\n            // 1. 解析输入 — 从 JSON 中提取 tool_name 和 input\r\n            JsonNode jsonNode = OBJECT_MAPPER.readTree(inputJson);\r\n            String toolName = jsonNode.get(\"tool_name\").asText();\r\n            Map<String, Object> arguments = OBJECT_MAPPER.convertValue(\r\n                jsonNode.get(\"input\"),\r\n                new TypeReference<Map<String, Object>>() {}\r\n            );\r\n\r\n            // 2. 调用 MCP 服务器上的工具\r\n            McpSchema.CallToolResult callResult = setting.isSync()\r\n                ? setting.getMcpSyncClient().callTool(\r\n                    new McpSchema.CallToolRequest(toolName, arguments))\r\n                : setting.getMcpAsyncClient().callTool(\r\n                    new McpSchema.CallToolRequest(toolName, arguments)).block();\r\n\r\n            // 3. 提取文本结果\r\n            if (callResult != null && callResult.content() != null) {\r\n                for (McpSchema.Content content : callResult.content()) {\r\n                    if (content instanceof McpSchema.TextContent) {\r\n                        result.set(((McpSchema.TextContent) content).text());\r\n                    }\r\n                }\r\n            }\r\n            return result.get();\r\n        }\r\n    };\r\n}\r\n```\r\n\r\n### 5. Agent13 — 组合模式启动\r\n\r\nAgent13 使用**组合模式**（与 Agent01 相同），不继承 ZQAgent：\r\n\r\n```java\r\npublic class Agent13 {\r\n    public static void main(String[] args) {\r\n        AnthropicClient client = AnthropicOkHttpClient.builder()\r\n            .apiKey(API_KEY).baseUrl(BASE_URL).build();\r\n\r\n        // 1. 加载 MCP 配置\r\n        List<McpSetting> settings = new McpConfigLoader().loadMcpSettings();\r\n\r\n        // 2. 初始化 MCP 客户端\r\n        MCPAgent mcpAgent = new MCPAgent(settings);\r\n        mcpAgent.initializeClient();\r\n\r\n        // 3. 加载 MCP 工具\r\n        McpLoader mcpLoader = new McpLoader(settings);\r\n        System.out.println(mcpLoader.getServerInfo());\r\n        System.out.println(mcpLoader.getToolDescriptions());\r\n\r\n        // 4. 组装工具列表：6 内置 + N MCP\r\n        List<ToolDefinition> tools = new ArrayList<>();\r\n        tools.add(BashDefinition);\r\n        tools.add(ReadFileDefinition);\r\n        tools.add(WriteFileDefinition);\r\n        tools.add(EditFileDefinition);\r\n        tools.add(ListFilesDefinition);\r\n        tools.add(ContentSearchDefinition);\r\n        tools.addAll(mcpLoader.loadTools());\r\n\r\n        // 5. 创建 ZQAgent 并注入动态系统提示\r\n        ZQAgent agent = new ZQAgent(client, MODEL, tools);\r\n        agent.setSystemPrompt(buildSystemPrompt(mcpLoader));\r\n        agent.run();\r\n\r\n        // 6. 优雅关闭 MCP 连接\r\n        mcpAgent.closeClient();\r\n    }\r\n}\r\n```\r\n\r\n### 6. McpSetting — 配置数据模型\r\n\r\n```java\r\npublic class McpSetting {\r\n    public static final String sseEndpoint = \"/sse\";\r\n\r\n    private String mcpId;           // UUID\r\n    private String name;            // 服务器名称（mcp.json 中的 key）\r\n    private String description;     // 描述\r\n    private ClientType clientType;  // SYNC / ASYNC\r\n    private TransportType transportType;  // STDIO / SSE / Streamable_HTTP\r\n    private McpSyncClient mcpSyncClient;  // 同步客户端实例\r\n    private McpAsyncClient mcpAsyncClient; // 异步客户端实例\r\n    private Duration timeout;       // 默认 10 秒\r\n    private ServerParameters serverParameters;  // STDIO 配置\r\n    private String sseUrl;          // HTTP/SSE 地址\r\n    private String authorization;   // 认证令牌\r\n    private Map<String, String> headers;  // 自定义请求头\r\n    private List<McpSchema.Root> roots;   // 客户端能力配置\r\n}\r\n```\r\n\r\n## 工具数量\r\n\r\n| 版本 | 工具数 | 说明 |\r\n|------|--------|------|\r\n| s08 | 16 | 16 个硬编码工具 |\r\n| s13 | 6 + N | 6 个内置工具 + N 个 MCP 动态工具 |\r\n\r\n## 相对于 s08 的变更\r\n\r\n| 组件 | s08 | s13 |\r\n|------|-----|-----|\r\n| 架构模式 | 继承 ZQAgent（Agent08 extends ZQAgent） | 组合模式（不继承，创建 ZQAgent 实例） |\r\n| 工具数量 | 16（固定硬编码） | 6 + N（动态加载 MCP 工具） |\r\n| 工具扩展 | 修改 ToolDefinition.java，重新编译 | 编辑 mcp.json，无需改代码 |\r\n| 外部系统集成 | 不支持 | 通过 MCP 协议标准化接入 |\r\n| 传输方式 | 无 | STDIO / SSE / Streamable_HTTP |\r\n| 客户端类型 | 无 | 同步（SYNC）/ 异步（ASYNC） |\r\n| 配置方式 | 代码内硬编码 | mcp.json 配置文件 |\r\n| 工具发现 | 无 | listTools() 自动发现 |\r\n| 资源管理 | 无 | listResources() + readResource() |\r\n| 提示词模板 | 无 | listPrompts() |\r\n| 系统提示 | 静态字符串 | 动态生成（含 MCP 工具描述） |\r\n| 后台任务 | BackgroundManager + CachedThreadPool | 无（回归 s01 级别基础能力） |\r\n| 子智能体 | SubAgentTool | 无 |\r\n| Todo 管理 | TodoManager | 无 |\r\n| Skill 系统 | SkillLoader | 无 |\r\n| 上下文压缩 | ContextCompactor | 无 |\r\n| 新增类 | — | McpLoader, MCPAgent, McpConfigLoader, McpSetting |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-13-mcp\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent13\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `你有哪些 MCP 工具可用？列出所有工具名称和描述`\r\n2. `用 MCP 工具搜索潍柴论坛上关于发动机故障的帖子`\r\n3. `读取本地文件 config.yaml 的内容，然后通过 MCP 工具发布到论坛`\r\n4. `同时使用 bash 工具和 MCP 工具完成任务`\r\n5. `列出当前已连接的 MCP 服务器及其传输类型`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s14",
    "locale": "zh",
    "title": "s14: ReAct Framework (ReAct 行为模式)",
    "content": "# s14: The ReAct Pattern (ReAct推理行动模式)\r\n\r\n`s01 > s02 > s03 > s04 > s05 > s06 > s07 > s08 > s09 > [ s10 ] > s11 > s12`\r\n\r\n> *\"Think before you act, observe before you think again\"* -- 先思考再行动，观察之后再思考。\r\n\r\n## 问题\r\n\r\n标准的 `tool_use` 模式把思考过程藏在结构化响应里 -- LLM 的推理链对开发者不可见。更关键的是，并非所有 LLM API 都支持 tool_use，如果模型只能输出纯文本，整个工具调用机制就失效了。我们需要一种**不依赖 API 特定能力**的工具调用方式，同时让推理过程透明可见。\r\n\r\n## 解决方案\r\n\r\n![System Architecture](/agent/system-architecture.svg)\r\n\r\n```\r\n            REACT_ENABLE = true                 REACT_ENABLE = false\r\n          ┌──────────────────┐               ┌──────────────────┐\r\n          │  不发送 tools     │               │  发送 tools      │\r\n          │  给 LLM API      │               │  给 LLM API      │\r\n          └────────┬─────────┘               └────────┬─────────┘\r\n                   │                                  │\r\n                   ▼                                  ▼\r\n          LLM 输出纯文本:                     LLM 输出结构化:\r\n          ┌──────────────────┐               ┌──────────────────┐\r\n          │ Thought: 分析..  │               │ ToolUseBlock {   │\r\n          │ Action: bash     │               │   name: \"bash\"   │\r\n          │ Action Input: {} │               │   input: {...}   │\r\n          └────────┬─────────┘               │ }                │\r\n                   │                          └────────┬─────────┘\r\n                   ▼                                   │\r\n          正则表达式解析:                               │\r\n          ┌──────────────────┐                        │\r\n          │ Pattern.compile  │                        │\r\n          │ (\"Action:...\")   │                        │\r\n          │ (\"Action Input..\")│                        │\r\n          └────────┬─────────┘                        │\r\n                   │                                  │\r\n                   └──────────┬───────────────────────┘\r\n                              ▼\r\n                     invokeTool()\r\n                              │\r\n                              ▼\r\n                     工具执行结果\r\n                              │\r\n               ┌──────────────┴──────────────┐\r\n               ▼                             ▼\r\n     \"Observation: ...\"             ToolResultBlockParam\r\n     (注入为 USER 消息)              (追加为 tool_result)\r\n               │                             │\r\n               └──────────────┬──────────────┘\r\n                              ▼\r\n                     继续循环 (直到无 Action)\r\n```\r\n\r\n一个 `REACT_ENABLE` 开关控制两条完全不同的执行路径。ReAct 模式下工具不发给 API，LLM 以纯文本输出 Thought/Action/Action Input，ZQAgent 用正则解析后执行工具，再将 Observation 注入为用户消息。\r\n\r\n## 工作原理\r\n\r\n### 1. 双模式入口 -- Agent14\r\n\r\nAgent14 是入口类，根据 `REACT_ENABLE` 选择不同的系统提示词和执行模式：\r\n\r\n```java\r\npublic class Agent14 {\r\n    public static void main(String[] args) {\r\n        AnthropicClient client = AnthropicOkHttpClient.builder()\r\n                .apiKey(API_KEY).baseUrl(BASE_URL).build();\r\n\r\n        List<ToolDefinition> tools = new ArrayList<>();\r\n        tools.add(BashDefinition);\r\n        tools.add(EditFileDefinition);\r\n        tools.add(WriteFileDefinition);\r\n        tools.add(ReadFileDefinition);\r\n        tools.add(ListFilesDefinition);\r\n        tools.add(ContentSearchDefinition);\r\n\r\n        ZQAgent agent = new ZQAgent(client, MODEL, tools);\r\n\r\n        String systemPrompt;\r\n        if (REACT_ENABLE) {\r\n            systemPrompt = buildReActSystemPrompt(tools);\r\n        } else {\r\n            systemPrompt = buildNormalSystemPrompt(tools);\r\n        }\r\n        agent.setSystemPrompt(systemPrompt);\r\n        agent.run();\r\n    }\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- `REACT_ENABLE`: 来自 `AIConstants` 的布尔开关，决定整个执行模式\r\n- `buildReActSystemPrompt(tools)`: 构建 ReAct 模式提示词，强制 Thought/Action/Action Input/Observation 格式\r\n- `buildNormalSystemPrompt(tools)`: 构建标准模式提示词，列出工具描述\r\n- 组合模式：创建 `ZQAgent` 实例而非继承，与 Agent01、Agent02、Agent13 一致\r\n\r\n### 2. ReAct 系统提示词 -- buildReActSystemPrompt()\r\n\r\nReAct 提示词严格约束 LLM 的输出格式，确保正则解析能正确工作：\r\n\r\n```java\r\nprivate static String buildReActSystemPrompt(List<ToolDefinition> tools) {\r\n    StringBuilder sb = new StringBuilder();\r\n    sb.append(\"你是一个使用 ReAct 模式的 AI 助手。ReAct 模式结合了推理和行动。\\n\\n\");\r\n    sb.append(\"当需要使用工具时，请按照以下格式思考和行动：\\n\\n\");\r\n    sb.append(\"Thought: 思考并确定下一步的最佳行动方案\\n\");\r\n    sb.append(\"Action: 工具名称\\n\");\r\n    sb.append(\"Action Input: 工具参数（JSON 对象）\\n\");\r\n    sb.append(\"Observation: 工具执行结果\\n\\n\");\r\n    sb.append(\"重要规则：\\n\");\r\n    sb.append(\"- 不使用工具时，回复中不要出现 Thought、Action、Action Input\\n\");\r\n    sb.append(\"- 使用工具前，检查是否缺少必要参数\\n\");\r\n    sb.append(\"- 一次只能返回一个 Thought/Action/Action Input/Observation\\n\");\r\n    sb.append(\"- 不能私自给 Observation 赋值\\n\\n\");\r\n    sb.append(\"可用工具：\\n\");\r\n    for (ToolDefinition tool : tools) {\r\n        sb.append(\"- \").append(tool.getName())\r\n          .append(\": \").append(tool.getDescription()).append(\"\\n\");\r\n    }\r\n    return sb.toString();\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- 四元素格式：Thought（思考）/ Action（行动）/ Action Input（参数）/ Observation（结果）\r\n- 严格约束：一次只返回一组，不私自赋值 Observation，缺参数时向用户提问\r\n- 动态工具列表：遍历 `tools` 生成可用工具描述，LLM 据此选择 Action\r\n\r\n### 3. ReAct 模式下不发送工具 -- chatMessage()\r\n\r\n这是 ReAct 模式的关键：**工具不发送给 API**。LLM 只看到提示词中的工具列表，以纯文本输出 Action：\r\n\r\n```java\r\nprotected Message chatMessage(List<MessageParam> messageParams) {\r\n    List<ToolUnion> anthropicTools = new ArrayList<>();\r\n    for (ToolDefinition tool : tools) {\r\n        anthropicTools.add(ToolUnion.ofTool(\r\n                Tool.builder()\r\n                        .name(tool.getName())\r\n                        .description(tool.getDescription())\r\n                        .inputSchema(tool.getInputSchema())\r\n                        .build()\r\n        ));\r\n    }\r\n\r\n    MessageCreateParams.Builder messageBuilder = MessageCreateParams.builder()\r\n            .model(model)\r\n            .messages(messageParams);\r\n\r\n    if (!REACT_ENABLE && tools != null && !tools.isEmpty()) {\r\n        messageBuilder.tools(anthropicTools);\r\n    }\r\n\r\n    if (systemPrompt != null && !systemPrompt.isEmpty()) {\r\n        messageBuilder.system(systemPrompt);\r\n    }\r\n    messageBuilder.maxTokens(MAX_TOKENS);\r\n    messageBuilder.temperature(TEMPERATURE);\r\n\r\n    return client.messages().create(messageBuilder.build());\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- `!REACT_ENABLE`: 只有标准模式才发送工具给 API\r\n- ReAct 模式下 `messageBuilder.tools()` 不被调用，API 只收到纯文本请求\r\n- LLM 通过系统提示词了解可用工具，以纯文本格式输出 Action\r\n\r\n### 4. 正则解析引擎 -- run()\r\n\r\nReAct 模式的核心逻辑在 `ZQAgent.run()` 中，用正则从 LLM 文本输出中提取工具调用信息：\r\n\r\n```java\r\nif (REACT_ENABLE) {\r\n    String resultText = \"\";\r\n    for (ContentBlock content : message.content()) {\r\n        if (content.isText()) {\r\n            resultText = content.text().map(TextBlock::text).orElse(\"\");\r\n        }\r\n    }\r\n\r\n    messageParams.add(MessageParam.builder()\r\n            .role(MessageParam.Role.ASSISTANT)\r\n            .content(resultText).build());\r\n\r\n    if (resultText.contains(\"Action:\")) {\r\n        Pattern actionPattern = Pattern.compile(\"Action:\\\\s*([^\\\\n]+)\");\r\n        Pattern inputPattern = Pattern.compile(\r\n            \"Action Input:\\\\s*(\\\\{.*?\\\\})(?=\\\\s*\\n|$)\", Pattern.DOTALL);\r\n\r\n        Matcher actionMatcher = actionPattern.matcher(resultText);\r\n        Matcher inputMatcher = inputPattern.matcher(resultText);\r\n\r\n        if (actionMatcher.find()) {\r\n            String targetToolName = actionMatcher.group(1).trim();\r\n            String inputJson = inputMatcher.find() ? inputMatcher.group(1) : \"{}\";\r\n\r\n            ToolDefinition targetTool = null;\r\n            for (ToolDefinition tool : tools) {\r\n                if (tool.getName().equals(targetToolName)) {\r\n                    targetTool = tool;\r\n                    break;\r\n                }\r\n            }\r\n\r\n            String observation = \"\";\r\n            if (targetTool != null) {\r\n                observation = invokeTool(targetTool, inputJson);\r\n            } else {\r\n                observation = \"错误: 未找到工具 \" + targetToolName;\r\n            }\r\n\r\n            messageParams.add(MessageParam.builder()\r\n                    .role(MessageParam.Role.USER)\r\n                    .content(\"Observation: \" + observation).build());\r\n\r\n            message = chatMessage(messageParams);\r\n            messageParams.add(message.toParam());\r\n        }\r\n    } else {\r\n        System.out.printf(\"AI: %s%n\", resultText);\r\n        break;\r\n    }\r\n} else {\r\n    // 标准 tool_use 流程\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- `resultText.contains(\"Action:\")`: 检测 LLM 是否请求调用工具\r\n- `Pattern.compile(\"Action:\\\\s*([^\\\\n]+)\")`: 提取 Action 后面的工具名称\r\n- `Pattern.compile(\"Action Input:\\\\s*(\\\\{.*?\\\\})(?=\\\\s*\\\\n|$)\", Pattern.DOTALL)`: 提取 JSON 格式的参数，`DOTALL` 支持跨行匹配\r\n- `invokeTool(targetTool, inputJson)`: 直接调用工具执行\r\n- `\"Observation: \"` 前缀：将结果注入为用户消息，LLM 据此继续推理\r\n- 无 `Action:` 则视为最终答案，`break` 退出内层循环\r\n\r\n### 5. ReActInput 数据模型\r\n\r\n`ReActInput` 定义了 ReAct 模式的四个核心字段，但**不作为工具注册**：\r\n\r\n```java\r\npublic class ReActInput {\r\n    @JsonProperty(\"thought\")\r\n    private String thought;\r\n\r\n    @JsonProperty(\"action\")\r\n    private String action;\r\n\r\n    @JsonProperty(\"actionInput\")\r\n    private String actionInput;\r\n\r\n    @JsonProperty(\"observation\")\r\n    private String observation;\r\n}\r\n```\r\n\r\n**代码解释**:\r\n- 四个字段对应 ReAct 的 Thought / Action / Action Input / Observation\r\n- `@JsonProperty` 注解用于 JSON 序列化/反序列化\r\n- 这个类仅作为数据模型存在，未在 `ToolDefinition` 中注册为工具\r\n- ReAct 是**执行模式**，不是**可调用工具**\r\n\r\n### 6. ZQAgent 本地副本\r\n\r\nAgent14 拥有自己的 `ZQAgent` 副本，与标准版本的关键差异：\r\n\r\n| 方法 | 标准 ZQAgent | Agent14 的 ZQAgent |\r\n|------|-------------|-------------------|\r\n| `chatMessage()` | 始终发送 tools | `REACT_ENABLE` 为 false 时才发送 |\r\n| `run()` | 仅处理 tool_use block | 额外处理 ReAct 文本解析分支 |\r\n| `invokeTool()` | 仅处理 TypedToolInvoker | 额外支持 `Function<String,String>` 模式（MCP 兼容） |\r\n\r\n**代码解释**:\r\n- 本地副本确保 ReAct 分支不影响其他模块的标准行为\r\n- `invokeTool()` 的 MCP 兼容分支：当 `tool.getType() == null` 时，将输入包装为 `{\"tool_name\": ..., \"input\": ...}` 传入 `Function<String,String>`\r\n\r\n## 工具数量表\r\n\r\n| 工具名 | 类型 | 输入参数 | 说明 |\r\n|--------|------|----------|------|\r\n| bash | Function | command, type | 执行 Shell 命令（cmd/powershell/bash） |\r\n| read_file | Function | path | 读取文件内容 |\r\n| write_file | Function | path, content | 写入文件 |\r\n| edit_file | Function | path, oldStr, newStr | 编辑文件（字符串替换） |\r\n| list_files | Function | path, fileType | 列出目录文件 |\r\n| content_search | Function | pattern, path, fileType, caseSensitive | ripgrep 搜索文件内容 |\r\n\r\n**总计：6 个工具**（与 Agent01-08 相同的基础工具集）\r\n\r\n> 注：ReAct 不是工具，而是执行模式。`ReActInput` 仅作为数据模型存在。\r\n\r\n## 相对于 s01 的变更\r\n\r\n| 组件 | s01 (Agent Loop) | s14 (ReAct Pattern) | 变更说明 |\r\n|------|-----------------|---------------------|----------|\r\n| 架构模式 | 组合模式 | 组合模式 | 不变 |\r\n| ZQAgent | 标准版本 | **本地副本** | 新增 `REACT_ENABLE` 分支 |\r\n| chatMessage() | 始终发送 tools | 条件发送 | `!REACT_ENABLE` 时才发送 |\r\n| run() | tool_use block 解析 | **正则文本解析** | ReAct 模式用正则提取 Action |\r\n| 工具结果注入 | ToolResultBlockParam | **Observation 用户消息** | `role=USER` + `\"Observation: \"` 前缀 |\r\n| 系统提示词 | 单一提示词 | **双提示词** | `buildReActSystemPrompt()` + `buildNormalSystemPrompt()` |\r\n| 工具数量 | 1 (bash) | 6 | 增加了文件操作和搜索工具 |\r\n| invokeTool() | TypedToolInvoker | **双模式** | 额外支持 `Function<String,String>` |\r\n| 推理可见性 | 不可见 | **完全可见** | Thought 过程以纯文本输出 |\r\n| API 兼容性 | 依赖 tool_use | **不依赖** | ReAct 模式仅需文本生成能力 |\r\n\r\n## 试一试\r\n\r\n```bash\r\n# 编译项目\r\nmvn clean install\r\n\r\n# 运行 ReAct 智能体\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent14\"\r\n```\r\n\r\n试试这些 prompt (ReAct 模式下可以观察到完整的推理链):\r\n\r\n1. `查看当前目录有哪些文件`\r\n2. `读取 README.md 的前10行内容`\r\n3. `创建一个 hello.txt 文件，写入 \"Hello from ReAct!\"`\r\n4. `搜索项目中所有包含 \"REACT_ENABLE\" 的文件`\r\n5. `查看当前系统时间，然后创建一个以时间戳命名的日志文件`\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s15",
    "locale": "zh",
    "title": "s15: Dynamic System Prompt (动态系统提示)",
    "content": "# s15: Dynamic System Prompt（动态系统提示）\r\n\r\n`s01 > ... > s13 > s14 > [ s15 ] > s16 > s17 ...`\r\n\r\n> *\"prompt 是组装出来的，不是写死的\"* — 分段 + 按需拼接 + context-hash 缓存。\r\n\r\n## 问题\r\n\r\ns01 到 s14 的 system prompt 都是一行硬编码：\r\n\r\n```java\r\nString SYSTEM = \"You are a coding agent at \" + WORKDIR + \". Use tools to solve tasks.\";\r\n```\r\n\r\ns01 够用，只有 bash、read、write 三个工具。但到 s14，Agent 已经有 todo、子智能体、技能、压缩、任务、后台、权限、钩子、记忆、MCP、ReAct。prompt 该提的能力越来越多。三个问题：\r\n\r\n1. **换项目要重写整个 prompt**，不知道哪些该改、哪些该留\r\n2. **修改一处可能影响全局**，加一段工具描述可能跟前面的指令冲突\r\n3. **每次请求都带全部内容**，即使当前对话用不到某些段落也浪费 token\r\n\r\nsystem prompt 应该是运行时根据当前状态组装的配置：哪些工具启用、哪些上下文可见、哪些记忆相关、哪些内容必须保持稳定。\r\n\r\n## 解决方案\r\n\r\n![System Prompt Overview](/agent/system-prompt-overview.svg)\r\n\r\ns15 聚焦 prompt 组装机制。把硬编码的 `SYSTEM` 拆成独立段落（section），运行时根据 `AgentContext` 按需拼接，缓存结果避免重复组装。\r\n\r\n四个 section，两种加载策略：\r\n\r\n| Section | 加载策略 | 内容 | 判断依据 |\r\n|---------|---------|------|---------|\r\n| identity | 始终 | 你是谁、怎么做事 | 始终存在 |\r\n| tools | 始终 | 可用工具列表 | `toolNames` 非空 |\r\n| workspace | 始终 | 工作目录、操作系统 | 始终存在 |\r\n| memory | 按需 | 相关记忆内容 | `MEMORY.md` 是否存在 |\r\n\r\n关键设计：section 是否加载取决于真实状态（工具是否注册、文件是否存在），不是消息里的关键词。\r\n\r\n## 工作原理\r\n\r\n### 1. AgentContext — 装配上下文\r\n\r\n```java\r\n@Data @Builder\r\npublic class AgentContext {\r\n    private String workspace;            // 工作目录绝对路径\r\n    private String osName;               // 操作系统名（小写）\r\n    private List<String> toolNames;      // 当前已注册的工具名列表\r\n    private boolean memoryEnabled;       // MEMORY.md 是否存在\r\n    private String userPrompt;           // 用户当前 prompt（不进 system）\r\n\r\n    /** 上下文 hash —— 决定是否重新装配（缓存键）。userPrompt 不算。 */\r\n    public String cacheKey() {\r\n        return workspace + \"|\" + osName + \"|\"\r\n             + (toolNames == null ? \"\" : String.join(\",\", toolNames)) + \"|\"\r\n             + memoryEnabled;\r\n    }\r\n}\r\n```\r\n\r\n### 2. PromptAssembler.Section — 单段定义\r\n\r\n每个 section 是 `(name, enabled 谓词, render 函数)` 三元组：\r\n\r\n```java\r\n@Data @AllArgsConstructor\r\npublic static class Section {\r\n    private final String name;\r\n    private final Function<AgentContext, Boolean> enabled;\r\n    private final Function<AgentContext, String> render;\r\n}\r\n```\r\n\r\n构造时注册 4 个默认 section：\r\n\r\n```java\r\npublic PromptAssembler() {\r\n    register(\"identity\",  ctx -> true,                                  this::renderIdentity);\r\n    register(\"tools\",     ctx -> ctx.getToolNames() != null && !ctx.getToolNames().isEmpty(),\r\n                                                                            this::renderTools);\r\n    register(\"workspace\", ctx -> true,                                  this::renderWorkspace);\r\n    register(\"memory\",    AgentContext::isMemoryEnabled,                this::renderMemory);\r\n}\r\n```\r\n\r\n### 3. get(ctx) — 装配 + context-hash 缓存\r\n\r\n```java\r\npublic String get(AgentContext ctx) {\r\n    String key = ctx.cacheKey();\r\n    if (key.equals(lastKey)) return lastValue;          // 命中 LRU 末位\r\n    if (cache.containsKey(key)) { lastKey = key; lastValue = cache.get(key); return lastValue; }\r\n\r\n    StringBuilder sb = new StringBuilder();\r\n    for (Section s : sections) {\r\n        if (Boolean.TRUE.equals(s.getEnabled().apply(ctx))) {\r\n            String block = s.getRender().apply(ctx);\r\n            if (block != null && !block.isBlank()) {\r\n                if (sb.length() > 0) sb.append(\"\\n\\n\");\r\n                sb.append(block);\r\n            }\r\n        }\r\n    }\r\n    lastKey = key; lastValue = sb.toString();\r\n    cache.put(key, lastValue);\r\n    return lastValue;\r\n}\r\n```\r\n\r\n为什么不全加载？token 有成本（system prompt 每轮计费），信息越少 LLM 越专注。\r\n\r\n### 4. memory section 的按需加载\r\n\r\n`renderMemory` 检查 `MEMORY.md` 是否存在，存在才把前 1500 字符挂上：\r\n\r\n```java\r\nprivate String renderMemory(AgentContext ctx) {\r\n    Path memoryFile = Paths.get(ctx.getWorkspace(), \"MEMORY.md\");\r\n    if (!Files.isRegularFile(memoryFile)) return null;\r\n    String text = Files.readString(memoryFile).trim();\r\n    if (text.isEmpty()) return null;\r\n    if (text.length() > 1500) text = text.substring(0, 1500) + \"\\n... (MEMORY.md 已截断)\";\r\n    return \"## 持久记忆索引\\n\" + text;\r\n}\r\n```\r\n\r\n### 5. ZQAgent 集成\r\n\r\n每轮 while 入口前，按当前状态 build context、`assembler.get(ctx)` 拿到 effective system：\r\n\r\n```java\r\nAgentContext ctx = AgentContext.builder()\r\n    .workspace(WORKDIR.toString())\r\n    .osName(System.getProperty(\"os.name\").toLowerCase())\r\n    .toolNames(registeredToolNames())\r\n    .memoryEnabled(Files.exists(WORKDIR.resolve(\"MEMORY.md\")))\r\n    .userPrompt(userInput)\r\n    .build();\r\nString effectiveSystem = promptAssembler.get(ctx);\r\nchatMessage(messageParams, effectiveSystem);\r\n```\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s14 相同 |\r\n\r\nprompt 装配是注入层，不影响工具集。\r\n\r\n## 相对 s14 的变更\r\n\r\n| 组件 | s14 | s15 |\r\n|------|-----|-----|\r\n| prompt | 硬编码 `SYSTEM` 字符串 | `PromptAssembler` 分段装配 |\r\n| 缓存 | 无 | `get(ctx)` 用 `cacheKey` 命中缓存 |\r\n| 新增类 | — | `AgentContext`、`PromptAssembler`、`PromptAssembler.Section`（PromptSection） |\r\n| 循环 | 用固定 `SYSTEM` | 用 `assembler.get(ctx)`，每轮按状态重装 |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-sysprompt -am compile\r\nmvn -pl hoppinzq-module-agent-sysprompt exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentSysPrompt\r\n```\r\n\r\n观察重点：\r\n\r\n1. 输出中能看到 `[prompt] sections: identity, tools, workspace`（无 memory）\r\n2. 让 agent 用 write_file 写一个 MEMORY.md，下一轮 system 自动追加 memory 段\r\n3. bash 删掉 MEMORY.md，再下一轮 memory 段消失\r\n4. 连续相同 context 的对话，缓存命中\r\n\r\n试试这些 prompt：\r\n\r\n1. `读 README.md` — 观察始终加载的 3 段\r\n2. `用 write_file 创建 MEMORY.md，内容是 \"# Memory\\n- [test] test — test memory\"` — 写入记忆索引\r\n3. `读 README.md` — 观察 memory section 是否出现\r\n4. `bash 删掉 MEMORY.md` — 观察下一轮 memory 段消失\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s16",
    "locale": "zh",
    "title": "s16: Cron Scheduler (定时调度)",
    "content": "# s16: Cron Scheduler（定时调度）\r\n\r\n`s01 > ... > s14 > s15 > [ s16 ] > s17 > s18 ...`\r\n\r\n> *\"按时间表生产工作，调度与执行解耦\"* — cron 表达式 + 双 daemon 线程 + 队列交付。\r\n\r\n## 问题\r\n\r\ns15 让 Agent 能动态组装 prompt、能后台执行慢操作，但所有操作仍然是你手动触发的。你说一句，Agent 动一下。\"每天早上 9 点跑测试\"、\"每 30 分钟检查 CI 状态\"、\"2 分钟后提醒我\"，这些周期性/延时任务不该需要人每次来推。\r\n\r\n闹钟不需要你盯着它才会响 — 你设好 7:00，到点它自己响。\r\n\r\n## 解决方案\r\n\r\n![Cron Scheduler Overview](/agent/cron-scheduler-overview.svg)\r\n\r\ns16 新增独立的 cron 调度系统：两条 daemon 线程 + 一个 `cron_queue`。调度线程每秒检查一次，时间到了把任务塞进队列；另一条 queue processor 线程在 Agent 空闲时把队列里的任务交付给 agent_loop。\r\n\r\n| | 手动触发 (s15) | 定时触发 (s16) |\r\n|---|---|---|\r\n| 触发者 | 用户输入 | 调度线程 |\r\n| 触发时机 | 随时 | cron 表达式指定 |\r\n| 需要人参与 | 是 | 否（调度器自动入队，空闲时自动交付） |\r\n| 持久性 | — | durable 跨重启 |\r\n\r\n## 工作原理\r\n\r\n### 1. CronJob — 数据结构\r\n\r\n```java\r\n@Data @AllArgsConstructor\r\npublic class CronJob {\r\n    private String id;          // 8 位 hex\r\n    private String cron;        // \"0 9 * * *\" — 五段式 cron 表达式\r\n    private String prompt;      // 触发时注入给 Agent 的消息\r\n    private boolean recurring;  // true=周期性，false=一次性\r\n    private boolean durable;    // true=写磁盘，跨会话保留\r\n    private String lastFired;   // \"yyyy-MM-dd HH:mm\" 防同分钟重复触发\r\n}\r\n```\r\n\r\n五段式 cron，Unix 用了 50 年：\r\n\r\n```\r\n分钟  小时  日  月  星期\r\n  *    *   *   *   *      每分钟\r\n  0    9   *   *   *      每天早上 9:00\r\n */5    *   *   *   *      每 5 分钟\r\n  0    9   *   *  1-5     工作日早上 9:00\r\n```\r\n\r\n支持 `*`、`*/N`、`N`、`N-M`、`N,M,...`。\r\n\r\n### 2. CronExpression — 五段式匹配\r\n\r\n标准 cron 语义：分钟、小时、月必须全部匹配；日（DOM）和星期（DOW）同时被约束时任一匹配即可（OR）：\r\n\r\n```java\r\npublic boolean matches(LocalDateTime dt) {\r\n    int dow = dt.getDayOfWeek().getValue() % 7;   // 周一=1 → cron 周日=0\r\n    if (!matchMinute(dt) || !matchHour(dt) || !matchMonth(dt)) return false;\r\n    boolean domUnconstrained = domField.equals(\"*\");\r\n    boolean dowUnconstrained = dowField.equals(\"*\");\r\n    if (domUnconstrained && dowUnconstrained) return true;\r\n    if (domUnconstrained) return matchDow(dow);\r\n    if (dowUnconstrained) return matchDom(dt);\r\n    return matchDom(dt) || matchDow(dow);\r\n}\r\n```\r\n\r\n### 3. CronScheduler — 两条 daemon 线程\r\n\r\n调度线程跑在 daemon 里，独立于 agent_loop 是否在执行。单个 job 异常不会杀掉整个线程：\r\n\r\n```java\r\n// 调度线程：每秒轮询，时间到了塞进 queue\r\nprivate void tickLoop() {\r\n    while (!Thread.currentThread().isInterrupted()) {\r\n        try { Thread.sleep(1000); } catch (InterruptedException e) { break; }\r\n        LocalDateTime now = LocalDateTime.now();\r\n        String marker = now.format(DateTimeFormatter.ofPattern(\"yyyy-MM-dd HH:mm\"));\r\n        synchronized (jobs) {\r\n            for (CronJob job : new ArrayList<>(jobs.values())) {\r\n                try {\r\n                    if (expressionOf(job).matches(now)\r\n                        && !marker.equals(job.getLastFired())) {\r\n                        queue.offer(job);\r\n                        job.setLastFired(marker);\r\n                        if (!job.isRecurring()) {\r\n                            jobs.remove(job.getId());\r\n                            if (job.isDurable()) CronStore.persist(jobs);\r\n                        }\r\n                    }\r\n                } catch (Exception e) {\r\n                    System.out.println(\"[cron error] \" + job.getId() + \": \" + e.getMessage());\r\n                }\r\n            }\r\n        }\r\n    }\r\n}\r\n```\r\n\r\nqueue processor 线程不检查时间，只负责在队列有任务且 Agent 空闲时拉起一轮执行：\r\n\r\n```java\r\nprivate void dispatchLoop() {\r\n    while (!Thread.currentThread().isInterrupted()) {\r\n        try { Thread.sleep(200); } catch (InterruptedException e) { break; }\r\n        if (queue.isEmpty()) continue;\r\n        if (!agentLock.tryLock()) continue;\r\n        try {\r\n            if (!queue.isEmpty()) runAgentTurnLocked();\r\n        } finally { agentLock.unlock(); }\r\n    }\r\n}\r\n```\r\n\r\n生产者（调度线程）、交付者（queue processor）和消费者（agent_loop）通过 `queue`、`jobs` 锁、`agentLock` 解耦。\r\n\r\n### 4. consumeQueue — agent_loop 消费端\r\n\r\nagent_loop 不负责检查时间，只从 queue 拿已触发的任务，注入到 messages：\r\n\r\n```java\r\npublic String consumeQueue() {\r\n    StringBuilder sb = new StringBuilder();\r\n    CronJob job;\r\n    while ((job = queue.poll()) != null) {\r\n        sb.append(\"[Scheduled] \").append(job.getPrompt()).append(\"\\n\");\r\n    }\r\n    return sb.toString().trim();\r\n}\r\n\r\n// ZQAgent 主循环入口：\r\nString scheduled = cronScheduler.consumeQueue();\r\nif (!scheduled.isEmpty()) {\r\n    messages.add(new MessageParam(\"user\", scheduled));\r\n}\r\n```\r\n\r\n### 5. Durable vs Session-only\r\n\r\n- **Durable**：`CronStore.persist(jobs)` 写入 `.scheduled_tasks.json`。Agent 重启后 `CronStore.load()` 恢复任务\r\n- **Session-only**：只在内存里。Agent 关闭就没了\r\n\r\n> cron 调度器必须在 Agent 进程内跑。进程关闭，调度也停。Durable 只意味着任务定义跨重启保留，下次启动时调度器才会发现\"该触发了\"并触发。\r\n\r\n### 6. 三个 cron 工具\r\n\r\n```java\r\npublic static String schedule(String input) {  // schedule_cron\r\n    CronScheduleInput in = parse(input);\r\n    String err = CronExpression.validate(in.cron);\r\n    if (err != null) return err;\r\n    CronJob job = cronScheduler.schedule(in.cron, in.prompt, in.recurring, in.durable);\r\n    return \"已注册 cron 任务 \" + job.getId();\r\n}\r\npublic static String list(String input)   { ... }  // list_crons\r\npublic static String cancel(String input) { ... }  // cancel_cron\r\n```\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s15 相同 |\r\n| **schedule_cron, list_crons, cancel_cron** | 新增 3 个 cron 工具 |\r\n\r\n## 相对 s15 的变更\r\n\r\n| 组件 | s15 | s16 |\r\n|------|-----|-----|\r\n| 触发方式 | 用户手动 | 调度线程自动入队 |\r\n| 新增类 | — | CronJob、CronExpression、CronScheduler、CronStore |\r\n| 新存储 | — | `.scheduled_tasks.json`（durable）+ 内存（session-only） |\r\n| 线程 | — | + 调度线程（daemon, 1s 轮询）+ queue processor 线程 |\r\n| 队列 | — | `cron_queue`：调度写、processor 交付、agent_loop 消费 |\r\n| 新工具 | — | schedule_cron, list_crons, cancel_cron |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-cron -am compile\r\nmvn -pl hoppinzq-module-agent-cron exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentCron\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `每 2 分钟打印当前日期` — 期望：`已注册 cron 任务 xxxxxxxx`，2 分钟后看到 `[Scheduled]` 自动执行\r\n2. `列出所有 cron 任务` — 期望：list_crons 返回注册的任务列表\r\n3. `1 分钟后提醒我检查构建状态` — 期望：一次性任务，触发后自动从列表删除\r\n4. `取消任务 abc12345` — 期望：cancel_cron 移除任务，list_crons 验证\r\n5. `每天早上 9 点跑测试` — 期望：注册 durable 任务，重启后仍在\r\n\r\n观察重点：调度线程是否独立运行？不输入新 prompt 时，是否也出现 `[Scheduled]` 并自动执行？durable job 是否写入了 `.scheduled_tasks.json`？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s17",
    "locale": "zh",
    "title": "s17: Agent Teams (Agent 团队)",
    "content": "# s17: Agent Teams（Agent 团队）\r\n\r\n`s01 > ... > s15 > s16 > [ s17 ] > s18 > s19 ...`\r\n\r\n> *\"一个搞不定，组队来\"* — 文件邮箱 + 守护线程，多 Agent 协作。\r\n\r\n## 问题\r\n\r\n\"重构整个后端\"涉及认证模块、数据库层、API 路由、测试。一个 Agent 在修 API 路由时，认证模块的细节已经不在上下文里了。上下文窗口就那么大，单个 Agent 的注意力覆盖不了所有模块。\r\n\r\ns04 的子 Agent 是临时工，叫来干一件事就走了。但有些任务需要能通信、能协作、生命周期跨越多个任务的队友。\r\n\r\n## 解决方案\r\n\r\n![Agent Teams Overview](/agent/agent-teams-overview.svg)\r\n\r\ns17 新增三样：**MessageBus**（文件邮箱）、**TeammateRunner**（teammate 守护线程主循环）、**inbox 注入**（Lead 接收队友消息并注入 history）。\r\n\r\n| | s04 子 Agent | s17 队友 |\r\n|---|---|---|\r\n| 生命周期 | 一次性，用完销毁 | 多轮（守护线程，跑 N 轮直到 idle） |\r\n| 通信 | 只回传结论 | 异步收件箱，随时通信 |\r\n| 上下文 | 完全隔离 | 通过消息共享信息 |\r\n| 数量 | 一个主 Agent + 偶尔子 Agent | 一个 Lead + 多个 teammate |\r\n\r\n## 工作原理\r\n\r\n![Team Topology](/agent/team-topology.svg)\r\n\r\n### 1. MessageBus — 文件邮箱\r\n\r\n每个 Agent（包括 Lead 和 teammate）有一个 `.jsonl` 邮箱。发消息 = 往对方的文件里 append 一行 JSON。读消息 = 读文件 + 删除（消费式）：\r\n\r\n```java\r\npublic class MessageBus {\r\n    public void send(String from, String to, String content, String type) {\r\n        MailboxMessage msg = new MailboxMessage(from, to, content, type, System.currentTimeMillis());\r\n        Path inbox = MAILBOX_DIR.resolve(to + \".jsonl\");\r\n        Files.writeString(inbox, JSON.write(msg) + \"\\n\",\r\n                          StandardOpenOption.CREATE, StandardOpenOption.APPEND);\r\n    }\r\n\r\n    public List<MailboxMessage> readInbox(String agent) {\r\n        Path inbox = MAILBOX_DIR.resolve(agent + \".jsonl\");\r\n        if (!Files.exists(inbox)) return List.of();\r\n        List<MailboxMessage> msgs = Files.readAllLines(inbox).stream()\r\n            .map(line -> JSON.read(line, MailboxMessage.class)).toList();\r\n        Files.delete(inbox);   // 消费式：读完删除\r\n        return msgs;\r\n    }\r\n}\r\n```\r\n\r\n为什么用文件而不是内存队列？直观、跨线程可观察。真实 CC 也用文件收件箱（`~/.claude/teams/{team}/inboxes/`），但加了 `proper-lockfile` 防并发写冲突。\r\n\r\n### 2. TeammateRunner — teammate 守护线程主循环\r\n\r\nLead 调用 `spawn_teammate` 工具启动一个 teammate。teammate 跑在自己的 daemon 线程里，有自己的 system prompt、自己的 messages、自己的简化工具集：\r\n\r\n```java\r\npublic class TeammateRunner implements Runnable {\r\n    private final String name, role;\r\n    private final List<MessageParam> messages = new ArrayList<>();\r\n\r\n    public void run() {\r\n        messages.add(new MessageParam(\"user\", initialPrompt));\r\n        List<ToolDefinition> subTools = List.of(bash, read_file, write_file, send_message);\r\n        for (int i = 0; i < MAX_TURNS; i++) {            // 教学版限 10 轮\r\n            drainInboxIntoMessages();                    // 每轮先消费 inbox\r\n            Response resp = client.chat(model, systemFor(name, role), messages, subTools);\r\n            messages.add(resp.assistant());\r\n            if (resp.stopReason() != TOOL_USE) break;\r\n            messages.addAll(executeTools(resp.toolUses()));\r\n        }\r\n        BUS.send(name, \"lead\", summary(), \"result\");     // 完成后发 summary 给 Lead\r\n    }\r\n\r\n    private String systemFor(String name, String role) {\r\n        return \"你是 '\" + name + \"', 一个 \" + role + \". 使用工具完成任务.\";\r\n    }\r\n}\r\n```\r\n\r\n关键设计：\r\n- **简化工具集**：bash、read_file、write_file、send_message。教学版省略了任务和 cron，聚焦通信机制\r\n- **限 10 轮**：防止 teammate 无限循环。真实 CC 用 idle loop：跑完一轮后发 `idle_notification`，等 inbox 消息，收到后继续\r\n- **完成后自动汇报**：`BUS.send(name, \"lead\", summary)` 把最终结果发到 Lead 的收件箱\r\n\r\n### 3. TeammateSpawner — 启动入口\r\n\r\n```java\r\npublic static String spawnTeammate(String input) {\r\n    SpawnInput in = parse(input);\r\n    Thread t = new Thread(new TeammateRunner(in.name, in.role, in.prompt), \"teammate-\" + in.name);\r\n    t.setDaemon(true);\r\n    t.start();\r\n    activeTeammates.put(in.name, t);\r\n    return \"teammate \" + in.name + \" 已启动 (\" + in.role + \")\";\r\n}\r\n```\r\n\r\n### 4. Lead 的 inbox 注入\r\n\r\nLead 在每轮主循环结束后检查收件箱。teammate 发来的消息注入到 history 里，让 LLM 能看到并做出反应：\r\n\r\n```java\r\n// ZQAgent 主循环每轮末：\r\nList<MailboxMessage> inbox = BUS.readInbox(\"lead\");\r\nif (!inbox.isEmpty()) {\r\n    String text = inbox.stream()\r\n        .map(m -> \"From \" + m.getFrom() + \": \" + truncate(m.getContent(), 200))\r\n        .collect(joining(\"\\n\"));\r\n    messages.add(new MessageParam(\"user\", \"[Inbox]\\n\" + text));\r\n}\r\n```\r\n\r\n教学版在用户输入循环外注入。CC 更精细，Lead 的 `useInboxPoller` 每 1 秒检查一次，有消息就提交为新的 turn。\r\n\r\n### 5. 三个 team 工具\r\n\r\n```java\r\nspawn_teammate  → TeammateSpawner.spawnTeammate(input)\r\nsend_message    → MessageBus.send(from, to, content, type)\r\ncheck_inbox     → MessageBus.readInbox(currentAgent)\r\n```\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| bash, read_file, write_file, edit_file, list_files, content_search | 与 s16 相同 |\r\n| schedule_cron, list_crons, cancel_cron | 与 s16 相同 |\r\n| **spawn_teammate, send_message, check_inbox** | 新增 3 个团队工具 |\r\n\r\nteammate 自己只用 4 个简化工具（bash / read_file / write_file / send_message）。\r\n\r\n## 相对 s16 的变更\r\n\r\n| 组件 | s16 | s17 |\r\n|------|-----|-----|\r\n| Agent 数量 | 1 | 1 Lead + N teammate 线程 |\r\n| 通信 | 无 | MessageBus + `.mailboxes/*.jsonl` |\r\n| 新增类 | — | MessageBus、TeammateRunner、TeammateSpawner、MailboxMessage |\r\n| 新工具 | — | spawn_teammate, send_message, check_inbox |\r\n| teammate 工具 | — | bash, read_file, write_file, send_message（4 个） |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-teams -am compile\r\nmvn -pl hoppinzq-module-agent-teams exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentTeams\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `Spawn alice 作为后端开发，让她创建一个 schema.sql 文件，包含 users 表` — 期望：alice 线程启动，写文件，发 result 给 Lead\r\n2. `检查 inbox 看 alice 的结果` — 期望：check_inbox 返回 alice 的 summary\r\n3. `Spawn bob 作为测试员，让他检查 schema.sql 是否存在并列出内容` — 期望：bob 并行工作，两个 teammate 互不干扰\r\n4. `同时 spawn alice 和 bob，让 alice 写后端、bob 写前端 API 客户端` — 期望：两条 daemon 线程并发执行\r\n\r\n观察重点：`.mailboxes/` 目录下的 `.jsonl` 文件长什么样？teammate 完成后 Lead 的 inbox 是否注入到 history？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s18",
    "locale": "zh",
    "title": "s18: Team Protocols (团队协议)",
    "content": "# s18: Team Protocols（团队协议）\r\n\r\n`s01 > ... > s16 > s17 > [ s18 ] > s19 > s20`\r\n\r\n> *\"队友之间要有约定\"* — request-response 模式 + 状态机驱动协商。\r\n\r\n## 问题\r\n\r\ns17 的 teammate 能干活了，但协调是松散的：Lead 发消息，teammate 回复，没有结构化的协议。两个场景暴露了问题：\r\n\r\n**关机**：Lead 想让 Alice 关机。直接杀线程，Alice 写了一半的文件留在磁盘上。需要握手：Lead 发请求，Alice 确认收尾后关机。\r\n\r\n**计划审批**：Bob 想重构认证模块，属于高风险操作。应该先让 Lead 看 Bob 的计划，审批通过后再动手。\r\n\r\n这两个场景结构完全一样：一方发请求，另一方给回复，请求和回复通过同一个 ID 关联。有状态机追踪：`pending → approved / rejected`。\r\n\r\n## 解决方案\r\n\r\n![Team Protocols Overview](/agent/team-protocols-overview.svg)\r\n\r\ns18 在 s17 的文件邮箱基础上加入结构化协议。新增三样：**ProtocolState**（请求状态追踪）、**ProtocolDispatcher**（按消息类型路由到处理器）、**ProtocolRegistry**（match_response 通过 request_id 关联回复与请求，含类型校验）。\r\n\r\n两种协议，一套机制：\r\n\r\n| 协议 | 方向 | 用途 |\r\n|------|------|------|\r\n| shutdown_request / response | Lead → teammate | 体面关机握手 |\r\n| plan_approval_request / response | teammate → Lead | 计划审批 |\r\n\r\n## 工作原理\r\n\r\n### 1. ProtocolState — 请求状态\r\n\r\n每个协议请求创建一条状态记录：\r\n\r\n```java\r\n@Data @AllArgsConstructor\r\npublic class ProtocolState {\r\n    private String requestId;    // 唯一 ID，如 \"req_004281\"\r\n    private String type;         // \"shutdown\" | \"plan_approval\"\r\n    private String sender;       // 发起方\r\n    private String target;       // 接收方\r\n    private String status;       // pending | approved | rejected\r\n    private String payload;      // 计划文本或关机原因\r\n    private long createdAt;\r\n}\r\n\r\nprivate final Map<String, ProtocolState> pending = new ConcurrentHashMap<>();\r\n```\r\n\r\n发请求时创建记录，收回复时通过 `requestId` 找到对应记录，更新状态。\r\n\r\n### 2. 四步协议流程\r\n\r\n以关机为例：\r\n\r\n```\r\n① Lead 发请求\r\n   String reqId = newRequestId();           // \"req_004281\"\r\n   pending.put(reqId, new ProtocolState(reqId, \"shutdown\", \"lead\", \"alice\", \"pending\", ...));\r\n   BUS.send(\"lead\", \"alice\", \"shutdown_request\", metadata={requestId: reqId});\r\n\r\n② teammate 收到 → dispatch\r\n   inbox = BUS.readInbox(\"alice\");\r\n   String type = msg.type;                   // \"shutdown_request\"\r\n   → 路由到 handleShutdownRequest()\r\n\r\n③ teammate 回复\r\n   BUS.send(\"alice\", \"lead\", \"shutdown_response\",\r\n            metadata={requestId: reqId, approve: true});\r\n\r\n④ Lead 收响应 → match\r\n   registry.match(\"shutdown_response\", reqId, true);\r\n   pending.get(reqId).setStatus(\"approved\");\r\n```\r\n\r\n`requestId` 是贯穿全链路的关联键，请求带着它出去，回复带着它回来。\r\n\r\n### 3. ProtocolDispatcher — 按类型路由\r\n\r\nteammate 的 inbox 不只收普通消息，还收协议消息。dispatcher 按消息类型分发：\r\n\r\n```java\r\npublic class ProtocolDispatcher {\r\n    public DispatchOutcome dispatch(String agent, MailboxMessage msg, List<MessageParam> messages) {\r\n        String type = msg.getType();\r\n        String reqId = msg.getMetadata().get(\"requestId\");\r\n        if (\"shutdown_request\".equals(type)) {\r\n            BUS.send(agent, \"lead\", \"Shutting down.\", \"shutdown_response\",\r\n                     Map.of(\"requestId\", reqId, \"approve\", \"true\"));\r\n            return DispatchOutcome.stopLoop();    // 停止循环\r\n        }\r\n        if (\"plan_approval_response\".equals(type)) {\r\n            boolean approve = Boolean.parseBoolean(msg.getMetadata().get(\"approve\"));\r\n            messages.add(new MessageParam(\"user\",\r\n                approve ? \"[Plan approved]\" : \"[Plan rejected]\"));\r\n        }\r\n        return DispatchOutcome.continueLoop();\r\n    }\r\n}\r\n```\r\n\r\n新增协议类型只需注册新 handler。\r\n\r\n### 4. ProtocolRegistry — 类型校验\r\n\r\n`match` 不只按 `requestId` 找状态，还会校验响应类型是否匹配请求类型：\r\n\r\n```java\r\npublic void match(String responseType, String requestId, boolean approve) {\r\n    ProtocolState s = pending.get(requestId);\r\n    if (s == null) return;\r\n    if (\"shutdown\".equals(s.getType()) && !\"shutdown_response\".equals(responseType)) return;\r\n    if (\"plan_approval\".equals(s.getType()) && !\"plan_approval_response\".equals(responseType)) return;\r\n    if (!\"pending\".equals(s.getStatus())) return;     // 已解决，跳过重复\r\n    s.setStatus(approve ? \"approved\" : \"rejected\");\r\n}\r\n```\r\n\r\n一个 shutdown_response 不会意外 approve 一个 plan_approval 请求。\r\n\r\n### 5. 统一 inbox 消费\r\n\r\n`check_inbox` 工具和主循环末尾都调用同一个 `consumeLeadInbox()`，先路由协议消息再返回剩余内容：\r\n\r\n```java\r\npublic List<MailboxMessage> consumeLeadInbox(boolean routeProtocol) {\r\n    List<MailboxMessage> msgs = BUS.readInbox(\"lead\");\r\n    if (routeProtocol) {\r\n        for (MailboxMessage msg : msgs) {\r\n            String reqId = msg.getMetadata().get(\"requestId\");\r\n            String type = msg.getType();\r\n            if (reqId != null && type.endsWith(\"_response\")) {\r\n                boolean approve = Boolean.parseBoolean(msg.getMetadata().get(\"approve\"));\r\n                registry.match(type, reqId, approve);\r\n            }\r\n        }\r\n    }\r\n    return msgs;\r\n}\r\n```\r\n\r\n### 6. teammate idle loop — 等待而不是退出\r\n\r\ns17 的 teammate 跑完 N 轮就退出。s18 的 teammate 在 LLM 返回非 tool_use 后进入 idle 等待：每秒轮询 inbox，收到 shutdown_request 就响应退出，收到新消息就继续工作：\r\n\r\n```\r\nLLM 返回非 tool_use\r\n  → idle: 每秒轮询 inbox\r\n  → 收到 shutdown_request → 回复 shutdown_response → 退出\r\n  → 收到新消息 → 注入 messages → 继续 LLM turn\r\n```\r\n\r\n### 7. 三个新工具\r\n\r\n```java\r\nrequest_shutdown  → Lead 发 shutdown_request\r\nrequest_plan      → teammate 发 plan_approval_request\r\nreview_plan       → Lead 回 plan_approval_response\r\n```\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| 上一版（s17）全部工具 | 不变 |\r\n| **request_shutdown, request_plan, review_plan** | 新增 3 个协议工具 |\r\n\r\n## 相对 s17 的变更\r\n\r\n| 组件 | s17 | s18 |\r\n|------|-----|-----|\r\n| teammate 退出 | 跑完 N 轮直接退 | idle loop，等 shutdown_request |\r\n| 协调方式 | 松散消息 | request-response + 状态机 |\r\n| 新增类 | — | ProtocolState、ProtocolDispatcher、ProtocolRegistry、DispatchOutcome |\r\n| 新工具 | — | request_shutdown, request_plan, review_plan |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-protocols -am compile\r\nmvn -pl hoppinzq-module-agent-protocols exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentProtocols\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `Spawn alice 让她创建 config.py，然后 request_shutdown(alice)` — 期望：alice 收尾后回 shutdown_response，状态 approved\r\n2. `Spawn bob 让他先提交 plan，等他 plan_approval_request 进 inbox 后 review approve` — 期望：bob 收到 `[Plan approved]` 继续干活\r\n3. `Spawn alice 做事 → request_shutdown → alice 拒绝（仍在写文件）` — 期望：alice 回 shutdown_response approve=false，继续工作\r\n4. `同时 spawn 两个 teammate，分别走 shutdown 和 plan_approval 协议` — 期望：requestId 互不混淆\r\n\r\n观察重点：`requestId` 是否贯穿全链路？`pending_requests` 状态是否正确从 pending → approved/rejected？teammate 在 idle loop 时是否每秒轮询？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s19",
    "locale": "zh",
    "title": "s19: Autonomous Agents (自主 Agent)",
    "content": "# s19: Autonomous Agents（自主 Agent）\r\n\r\n`s01 > ... > s17 > s18 > [ s19 ] > s20`\r\n\r\n> *\"自己看板，自己认领\"* — 空闲时轮询任务板，有活就干，无需 Lead 分配。\r\n\r\n## 问题\r\n\r\ns18 的 teammate 能通信、能握手关机。但每个 teammate 等 Lead 分配任务 — 如果任务看板上有 10 个未认领任务，Lead 得手动 assign 10 次。这不能扩展。\r\n\r\nteammate 应该自己看任务看板，发现没人做的任务就认领，做完再找下一个。\r\n\r\n## 解决方案\r\n\r\n![Autonomous Agents Overview](/agent/autonomous-agents-overview.svg)\r\n\r\ns19 沿用 s18 的 MessageBus 和协议工具，新增：**IdlePoller**（空闲时每 5 秒轮询）、**AutoClaimer**（扫描看板上可认领的任务并自动认领）。\r\n\r\nteammate 生命周期从两阶段变成三阶段：\r\n\r\n| 阶段 | 行为 | 退出条件 |\r\n|------|------|---------|\r\n| WORK | inbox → LLM → 工具循环 | `stop_reason != tool_use` |\r\n| IDLE | 每 5s 轮询 inbox + 任务板 | 60s 超时 |\r\n| SHUTDOWN | 发 summary，退出 | — |\r\n\r\n## 工作原理\r\n\r\n### 1. IdlePoller — 空闲轮询\r\n\r\nteammate 完成当前任务后不退出，进入 IDLE 阶段 — 每 5 秒检查一次有没有新工作：\r\n\r\n```java\r\npublic class IdlePoller {\r\n    private static final long POLL_INTERVAL_MS = 5000;\r\n    private static final long IDLE_TIMEOUT_MS = 60000;\r\n\r\n    /** 返回 \"work\" / \"shutdown\" / \"timeout\" */\r\n    public String poll(String agent, List<MessageParam> messages, String name, String role) {\r\n        long deadline = System.currentTimeMillis() + IDLE_TIMEOUT_MS;\r\n        while (System.currentTimeMillis() < deadline) {\r\n            Thread.sleep(POLL_INTERVAL_MS);\r\n\r\n            // ① 检查收件箱（优先）\r\n            List<MailboxMessage> inbox = BUS.readInbox(agent);\r\n            if (!inbox.isEmpty()) {\r\n                for (MailboxMessage msg : inbox) {\r\n                    if (\"shutdown_request\".equals(msg.getType())) {\r\n                        // 回复 shutdown_response\r\n                        return \"shutdown\";\r\n                    }\r\n                }\r\n                // 普通消息注入上下文，回到 WORK\r\n                injectInbox(messages, inbox);\r\n                return \"work\";\r\n            }\r\n\r\n            // ② 扫描任务看板\r\n            List<Task> unclaimed = AutoClaimer.scanUnclaimed();\r\n            if (!unclaimed.isEmpty()) {\r\n                Task task = unclaimed.get(0);\r\n                String result = AutoClaimer.claim(task.getId(), agent);\r\n                if (result.contains(\"Claimed\")) {\r\n                    messages.add(new MessageParam(\"user\",\r\n                        \"[Auto-claimed] \" + task.getSubject()));\r\n                    return \"work\";\r\n                }\r\n            }\r\n        }\r\n        return \"timeout\";\r\n    }\r\n}\r\n```\r\n\r\ninbox 优先（可能包含 shutdown_request 等协议消息），任务板其次。IDLE 阶段收到 shutdown_request 直接回复并退出，不等到下一轮 WORK。\r\n\r\n### 2. AutoClaimer — 扫描 + 认领\r\n\r\n找 pending 状态、无 owner、所有依赖已完成（`canStart`）的任务：\r\n\r\n```java\r\npublic class AutoClaimer {\r\n    public static List<Task> scanUnclaimed() {\r\n        return TaskManager.allTasks().stream()\r\n            .filter(t -> \"pending\".equals(t.getStatus()))\r\n            .filter(t -> t.getOwner() == null)\r\n            .filter(t -> TaskManager.canStart(t.getId()))\r\n            .sorted(Comparator.comparing(Task::getId))\r\n            .toList();\r\n    }\r\n\r\n    public static String claim(String taskId, String owner) {\r\n        Task task = TaskManager.load(taskId);\r\n        if (!\"pending\".equals(task.getStatus()))\r\n            return \"Task \" + taskId + \" is \" + task.getStatus() + \", cannot claim\";\r\n        if (task.getOwner() != null)\r\n            return \"Task \" + taskId + \" already owned by \" + task.getOwner();\r\n        if (!TaskManager.canStart(taskId))\r\n            return \"Blocked by: \" + task.getBlockedBy();\r\n        task.setOwner(owner);\r\n        task.setStatus(\"in_progress\");\r\n        TaskManager.save(task);\r\n        return \"Claimed \" + task.getId() + \" (\" + task.getSubject() + \")\";\r\n    }\r\n}\r\n```\r\n\r\n三个条件：必须 pending、没 owner、所有 blockedBy 依赖已完成。教学版按文件名排序取第一个；CC 用 `proper-lockfile` 防止多个 teammate 同时认领同一任务。\r\n\r\n### 3. teammate 生命周期：WORK → IDLE → SHUTDOWN\r\n\r\n外层循环让 WORK 和 IDLE 交替进行：\r\n\r\n```java\r\n// TeammateRunner 外层循环\r\nwhile (true) {\r\n    // WORK phase：内层最多 10 轮 LLM 调用\r\n    for (int i = 0; i < MAX_TURNS; i++) {\r\n        drainInbox(messages);\r\n        Response resp = client.chat(...);\r\n        messages.add(resp.assistant());\r\n        if (!\"tool_use\".equals(resp.stopReason())) break;\r\n        messages.addAll(executeTools(resp.toolUses()));\r\n    }\r\n\r\n    // IDLE phase\r\n    String result = idlePoller.poll(name, messages, name, role);\r\n    if (\"shutdown\".equals(result) || \"timeout\".equals(result)) break;\r\n}\r\n\r\n// SHUTDOWN：发 summary 给 Lead\r\nBUS.send(name, \"lead\", summary, \"result\");\r\n```\r\n\r\n关键设计：\r\n- **外层 while**：WORK 和 IDLE 交替，直到超时或收到关机请求\r\n- **内层 for 10 轮**：WORK 阶段最多 10 轮 LLM 调用（防无限循环）\r\n- **IDLE 超时 60 秒**：12 次轮询 × 5 秒 = 60 秒，超时后发 summary 退出\r\n- **shutdown 两阶段都响应**：WORK 阶段通过 dispatcher 分发；IDLE 阶段 poller 直接检查并回复\r\n\r\n### 4. 身份重注入\r\n\r\nautoCompact（s06）之后，teammate 的 messages 列表可能被压成一段摘要。每次进入新的 WORK 阶段时检查：\r\n\r\n```java\r\nif (messages.size() <= 3) {\r\n    messages.add(0, new MessageParam(\"user\",\r\n        \"<identity>你是 '\" + name + \"', 角色: \" + role + \". 继续你的工作.</identity>\"));\r\n}\r\n```\r\n\r\n消息过短说明发生了压缩，此时重新注入身份信息。\r\n\r\n### 5. 新增的 task 工具\r\n\r\n```java\r\ncreate_task    → Lead 创建任务，写到 .tasks/task_xxx.json\r\nlist_tasks     → 列出所有任务\r\nget_task       → 查单个任务详情\r\nclaim_task     → 显式认领（手动模式，与 AutoClaimer 自动模式互补）\r\ncomplete_task  → 标记完成，触发依赖解锁\r\n```\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| 上一版（s18）全部工具 | 不变 |\r\n| **create_task, list_tasks, get_task, claim_task, complete_task** | 新增 5 个任务工具 |\r\n\r\n## 相对 s18 的变更\r\n\r\n| 组件 | s18 | s19 |\r\n|------|-----|-----|\r\n| teammate 退出 | idle 等手动消息 | idle 时自动扫描任务板认领 |\r\n| 生命周期 | 两阶段（WORK → SHUTDOWN） | 三阶段（WORK → IDLE → SHUTDOWN） |\r\n| 新增类 | — | IdlePoller、AutoClaimer、TaskManager |\r\n| 新工具 | — | create_task, list_tasks, get_task, claim_task, complete_task |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-autonomous -am compile\r\nmvn -pl hoppinzq-module-agent-autonomous exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentAutonomous\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `创建 3 个任务：写 schema、写 API 路由、写测试；然后 spawn alice 和 bob，看他们自动认领` — 期望：alice 和 bob 各自从看板拿一个任务，做完后再找下一个\r\n2. `创建带依赖的任务：task2 blockedBy task1，看 task2 在 task1 完成前不会被扫出` — 期望：AutoClaimer 检查 canStart，跳过被阻塞的\r\n3. `spawn alice，60 秒不分配新任务，看 alice 因 IDLE 超时自动退出` — 期望：超时 SHUTDOWN，发 summary\r\n4. `spawn alice 做事 → request_shutdown → 看 alice 即使在 IDLE 也立即响应` — 期望：IdlePoller 第一时间检查 inbox，收到 shutdown_request 立即回复\r\n\r\n观察重点：teammate 在 IDLE 阶段是否每 5 秒扫一次任务板？多个 teammate 是否会重复认领？依赖任务是否正确阻塞？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s20",
    "locale": "zh",
    "title": "s20: Worktree Isolation (Worktree 隔离)",
    "content": "# s20: Worktree Isolation（Worktree 隔离）\r\n\r\n`s01 > ... > s18 > s19 > [ s20 ]`\r\n\r\n> *\"各干各的目录，互不干扰\"* — 任务管目标，worktree 管目录，按 ID 绑定。\r\n\r\n## 问题\r\n\r\ns19 中，Alice 和 Bob 都在同一个目录下工作。Alice 的任务是\"重构认证模块\"，Bob 的任务是\"重构 UI 登录页\"。\r\n\r\nAlice `write_file(\"config.py\", ...)`。Bob 也 `write_file(\"config.py\", ...)`。两个人改同一个文件，互相覆盖。而且无法干净地回滚 — 分不清哪些改动是谁的。\r\n\r\ns15-s19 解决了\"谁干什么\"（任务系统）和\"怎么通信\"（消息总线），但没解决\"在哪干\"。\r\n\r\n## 解决方案\r\n\r\n![Worktree Overview](/agent/worktree-overview.svg)\r\n\r\nGit worktree 让你在同一仓库中创建多个独立的工作目录，每个有自己的分支。Alice 在 `.worktrees/auth-refactor/` 下工作，Bob 在 `.worktrees/ui-login/` 下工作 — 互不干扰。\r\n\r\ns20 沿用 s19 的 MessageBus、协议、自治认领机制，新增：\r\n\r\n| 能力 | 作用 |\r\n|------|------|\r\n| create_worktree | 为任务创建独立目录 + 独立分支 |\r\n| bind_task_to_worktree | 把任务和工作目录绑定（不改状态） |\r\n| remove_worktree / keep_worktree | 完成后清理或保留 |\r\n| validate_worktree_name | 拒绝路径穿越和非法字符 |\r\n\r\n![Concurrency Comparison](/agent/concurrency-comparison.svg)\r\n\r\n## 工作原理\r\n\r\n### 1. WorktreeManager — 创建 + 任务绑定\r\n\r\n```java\r\npublic class WorktreeManager {\r\n    public String create(String name, String taskId) {\r\n        validateName(name);                            // 只允许 [A-Za-z0-9._-]{1,64}\r\n        Path path = WORKTREES_DIR.resolve(name);\r\n        GitResult r = GitRunner.run(\"worktree\", \"add\", path.toString(), \"-b\", \"wt/\" + name, \"HEAD\");\r\n        if (!r.ok) return \"Git error: \" + r.output;\r\n        if (taskId != null && !taskId.isEmpty()) {\r\n            bindTaskToWorktree(taskId, name);\r\n        }\r\n        logEvent(\"create\", name, taskId);\r\n        return \"Worktree '\" + name + \"' created at \" + path;\r\n    }\r\n\r\n    public void bindTaskToWorktree(String taskId, String worktreeName) {\r\n        Task task = TaskManager.load(taskId);\r\n        task.setWorktree(worktreeName);                // 只写 worktree 字段\r\n        TaskManager.save(task);                        // 状态保持 pending，等 teammate claim\r\n    }\r\n}\r\n```\r\n\r\n绑定规则：一个任务绑定一个 worktree。绑定**不改任务状态** — 任务仍是 `pending`，teammate 自动认领时才推进到 `in_progress`。这样 Lead 可以提前创建任务和 worktree，teammate idle 时自然认领带 worktree 的任务。\r\n\r\n### 2. WorktreeContext — ThreadLocal 路由\r\n\r\n每个 teammate 线程通过 ThreadLocal 持有自己的 worktree 路径。teammate 认领带 worktree 的任务时，`WorktreeContext` 自动设置路径；teammate 的 `bash`、`read_file`、`write_file` 通过 `effectiveRoot()` 路由：\r\n\r\n```java\r\npublic class WorktreeContext {\r\n    private static final ThreadLocal<Path> CURRENT = new ThreadLocal<>();\r\n\r\n    public static void set(Path worktreePath) { CURRENT.set(worktreePath); }\r\n    public static void clear()                 { CURRENT.remove(); }\r\n\r\n    public static Path effectiveRoot() {\r\n        Path wt = CURRENT.get();\r\n        return wt != null ? wt : ROOT;                // 没绑定时回退到主仓库\r\n    }\r\n}\r\n\r\n// 工具实现里调用：\r\npublic static String runBash(String command) {\r\n    return shellExec(command, WorktreeContext.effectiveRoot());\r\n}\r\npublic static String runWrite(String path, String content) {\r\n    Path resolved = WorktreeContext.effectiveRoot().resolve(path).normalize();\r\n    // ... 写文件\r\n}\r\n```\r\n\r\nThreadLocal 的好处是多 teammate 并发时各自隔离，文件/bash 操作天然路由到自己的 worktree，无需在每个工具调用处手动传 cwd。\r\n\r\n### 3. 认领时自动切换\r\n\r\nteammate 认领任务时，如果任务带 worktree，自动进入：\r\n\r\n```java\r\npublic static String claimTask(String input) {\r\n    String result = AutoClaimer.claim(taskId, owner);\r\n    if (result.contains(\"Claimed\")) {\r\n        Task task = TaskManager.load(taskId);\r\n        if (task.getWorktree() != null) {\r\n            Path wtPath = WORKTREES_DIR.resolve(task.getWorktree());\r\n            WorktreeContext.set(wtPath);\r\n        }\r\n    }\r\n    return result;\r\n}\r\n```\r\n\r\n### 4. GitRunner — 封装 git 命令\r\n\r\n```java\r\npublic class GitRunner {\r\n    public static GitResult run(String... args) {\r\n        ProcessBuilder pb = new ProcessBuilder(\"git\", args);\r\n        pb.directory(ROOT.toFile());\r\n        pb.redirectErrorStream(true);\r\n        Process p = pb.start();\r\n        String output = new String(p.getInputStream().readAllBytes());\r\n        boolean ok = p.waitFor() == 0;\r\n        return new GitResult(ok, output);\r\n    }\r\n}\r\n```\r\n\r\n`create` 和 `remove` 只在 git 命令成功后才写事件日志，保证日志反映真实状态。\r\n\r\n### 5. 收尾：Keep 还是 Remove\r\n\r\n任务完成后，两个选择：\r\n\r\n```java\r\npublic String remove(String name, boolean discardChanges) {\r\n    if (!discardChanges) {\r\n        int[] changes = countChanges(name);     // [files, commits]\r\n        if (changes[0] > 0 || changes[1] > 0) {\r\n            return \"有未提交改动，使用 discard_changes=true 强制删除，或 keep_worktree 保留\";\r\n        }\r\n    }\r\n    GitRunner.run(\"worktree\", \"remove\", path.toString(), \"--force\");\r\n    GitRunner.run(\"branch\", \"-D\", \"wt/\" + name);\r\n    logEvent(\"remove\", name);\r\n    return \"已删除 \" + name;\r\n}\r\n\r\npublic String keep(String name) {\r\n    logEvent(\"keep\", name);\r\n    return \"Worktree '\" + name + \"' kept for review (branch: wt/\" + name + \")\";\r\n}\r\n```\r\n\r\nKeep = 留着分支，等人工 review 后合并到主分支。Remove = 有改动时默认拒绝，需要 `discard_changes=true` 确认。**不自动 complete task** — 任务完成由 teammate 的 `complete_task` 显式触发。\r\n\r\n### 6. 事件流 — 可审计\r\n\r\n每次生命周期操作写入 `.worktrees/events.jsonl`：\r\n\r\n```java\r\nprivate void logEvent(String type, String name, String taskId) {\r\n    Map<String, Object> event = Map.of(\r\n        \"type\", type, \"worktree\", name,\r\n        \"task_id\", taskId == null ? \"\" : taskId,\r\n        \"ts\", System.currentTimeMillis());\r\n    Files.writeString(EVENTS_LOG, JSON.write(event) + \"\\n\",\r\n                      StandardOpenOption.CREATE, StandardOpenOption.APPEND);\r\n}\r\n```\r\n\r\n事件类型：`create` / `remove` / `keep`。教学版只记录事件用于人工排查。\r\n\r\n## 工具数量\r\n\r\n| 工具 | 说明 |\r\n|------|------|\r\n| 上一版（s19）全部工具 | 不变 |\r\n| **create_worktree, remove_worktree, keep_worktree** | 新增 3 个 worktree 工具 |\r\n\r\nteammate 工具数不变 — bash/read/write/edit 自动通过 `WorktreeContext.effectiveRoot()` 路由到 worktree。\r\n\r\n## 相对 s19 的变更\r\n\r\n| 组件 | s19 | s20 |\r\n|------|-----|-----|\r\n| 工作目录 | 所有 Agent 共享 ROOT | 每个任务可绑定独立 git worktree |\r\n| Task 数据 | id/subject/status/owner/blockedBy | + worktree 字段 |\r\n| teammate 工具 cwd | 始终 ROOT | ThreadLocal 自动路由到绑定的 worktree |\r\n| 新增类 | — | WorktreeManager、WorktreeContext（ThreadLocal）、GitRunner |\r\n| 新工具 | — | create_worktree, remove_worktree, keep_worktree |\r\n| worktree 安全 | 无 | name 校验（拒绝路径穿越）+ 有改动时拒绝删除 |\r\n| 事件日志 | 无 | `.worktrees/events.jsonl` 生命周期审计 |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-worktree -am compile\r\nmvn -pl hoppinzq-module-agent-worktree exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentWorktree\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `创建 2 个 task，每个 task 各绑一个 worktree；spawn alice 和 bob 观察他们隔离干活` — 期望：alice 写文件到 `.worktrees/auth/`，bob 写到 `.worktrees/ui/`，互不覆盖\r\n2. `让 alice 在 worktree 里改文件、commit；观察 git status 输出分支是 wt/auth 而不是 main` — 期望：worktree 有自己的分支\r\n3. `remove_worktree(name) 对有改动的 worktree 调用` — 期望：拒绝删除，提示用 discard_changes=true 或 keep_worktree\r\n4. `keep_worktree(name) 标记任务完成后保留 worktree` — 期望：分支 wt/{name} 保留，等人工 review\r\n5. `create_worktree(\"../escape\", task_id)` — 期望：name 校验拒绝路径穿越\r\n\r\n观察重点：两个 worktree 的 `git status` 是否显示不同分支？teammate 认领带 worktree 的任务后，bash 命令是否在 worktree 目录下执行？绑定 worktree 后任务状态是否仍为 `pending`？多个 teammate 并发时 `ThreadLocal` 是否各自隔离？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s21",
    "locale": "zh",
    "title": "s21: Comprehensive (综合集成)",
    "content": "# s21: Comprehensive（综合集成）\r\n\r\n`s01 > ... > s19 > s20 > [ s21 ] > s22 > s23`\r\n\r\n> *\"机制可以很多，循环只有一个\"* — 27 个工具 + 全部基础设施，塞进同一个 agent loop。\r\n\r\n## 问题\r\n\r\ns01-s20 讲了一堆机制：工具分发、todo 规划、子智能体、技能加载、上下文压缩、任务系统、后台任务、权限、钩子、记忆、错误恢复、MCP、ReAct、动态系统提示、cron 调度、Agent 团队、团队协议、自主 Agent、worktree 隔离。\r\n\r\n每个机制都独立运行。但生产环境需要所有机制同时工作：一个 agent 循环同时处理 cron 触发、权限检查、钩子注入、上下文压缩、后台通知。\r\n\r\n## 解决方案\r\n\r\ns21 把 s07-s20 的所有基础设施集成到同一个 agent 循环中。循环变成一条 7 阶段流水线：\r\n\r\n```\r\n用户输入\r\n    ↓\r\n① 微型压缩（每轮执行，裁剪 role+system message）\r\n    ↓\r\n② PromptAssembler 组装（identity/tools/workspace/memory 动态装配）\r\n    ↓\r\n③ CronScheduler 注入（检查定时任务是否到期）\r\n    ↓\r\n④ RetryWrapper 重试（max_tokens→prompt_too_long→rate_limit→非重试异常）\r\n    ↓\r\n⑤ HookRegistry 注入（PreToolUse 钩子：权限检查等）\r\n    ↓\r\n⑥ PermissionChecker 过滤（denyList→permissionRules→askUser）\r\n    ↓\r\n⑦ 工具执行 + 落账日志（ToolLogger 记录每次调用）\r\n    ↓\r\n返回结果\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. AgentComprehensive 主类\r\n\r\nAgentComprehensive 继承 ZQAgent，集成所有基础设施：\r\n\r\n```java\r\npublic class AgentComprehensive extends ZQAgent {\r\n    // 7 个基础设施组件\r\n    private final ContextCompactor compactor;\r\n    private final PromptAssembler promptAssembler;\r\n    private final CronScheduler cronScheduler;\r\n    private final RetryWrapper retryWrapper;\r\n    private final HookRegistry hookRegistry;\r\n    private final PermissionChecker permissionChecker;\r\n    private final BackgroundManager backgroundManager;\r\n    private final TaskManager taskManager;\r\n    private final SkillLoader skillLoader;\r\n    private final TodoManager todoManager;\r\n    private final MessageBus messageBus;\r\n    private final WorktreeManager worktreeManager;\r\n    private final ToolLogger toolLogger;\r\n\r\n    public AgentComprehensive(...) {\r\n        super(client, model, buildTools());\r\n        // 初始化 13 个组件\r\n    }\r\n}\r\n```\r\n\r\n### 2. 7 阶段流水线\r\n\r\n`runAgentTurn()` 是单次用户输入的完整处理管道：\r\n\r\n```java\r\npublic String runAgentTurn(String userInput) {\r\n    // 阶段 0：cron 到期注入\r\n    List<String> due = cronScheduler.pollDue();\r\n    if (!due.isEmpty()) {\r\n        messages.add(userMsg(\"[Cron triggered] \" + String.join(\"; \", due)));\r\n    }\r\n\r\n    // 阶段 1：微型压缩\r\n    compactor.microCompact(messages);\r\n\r\n    // 阶段 2：动态 system prompt\r\n    systemPrompt = promptAssembler.assemble(AgentContext.builder()\r\n        .workdir(ROOT).tools(tools).messages(messages).build());\r\n\r\n    while (true) {\r\n        // 阶段 3：发送消息（含重试）\r\n        Message message = retryWrapper.call(() -> chatMessage(messages));\r\n\r\n        // 阶段 4：处理响应\r\n        boolean hasTool = false;\r\n        for (ContentBlock block : message.content()) {\r\n            if (block.isToolUse()) {\r\n                hasTool = true;\r\n                ToolUseBlock tool = block.asToolUse();\r\n\r\n                // 阶段 5：PreToolUse 钩子\r\n                hookRegistry.firePreHook(tool.name(), tool._input());\r\n\r\n                // 阶段 6：权限检查\r\n                PermissionChecker.Result pr = permissionChecker.check(tool.name());\r\n                if (pr.isBlocked()) {\r\n                    toolResults.add(errorResult(tool.id(), pr.reason()));\r\n                    continue;\r\n                }\r\n\r\n                // 阶段 7：执行 + 落账\r\n                String result = invokeTool(tool);\r\n                toolLogger.log(tool.name(), tool._input(), result);\r\n                toolResults.add(successResult(tool.id(), result));\r\n            }\r\n        }\r\n        if (!hasTool) break;\r\n        messages.add(toolResultMsg(toolResults));\r\n    }\r\n    return extractText(message);\r\n}\r\n```\r\n\r\n### 3. 27 个工具\r\n\r\n包括之前所有版本的工具，加上 5 个 MCP 工具：\r\n\r\n```\r\n基础工具（6）：bash, read_file, write_file, edit_file, list_files, content_search\r\n规划工具（2）：todo, sub_agent\r\n技能工具（1）：load_skill\r\n记忆工具（1）：compact\r\n任务工具（5）：create_task, list_tasks, get_task, claim_task, complete_task\r\ncron 工具（3）：schedule_cron, list_crons, cancel_cron\r\n团队工具（4）：spawn_teammate, send_message, check_inbox, request_shutdown\r\n协议工具（2）：request_plan, review_plan\r\nworktree 工具（3）：create_worktree, remove_worktree, keep_worktree\r\nMCP 工具（5）：connect_mcp, mcp__docs__search, mcp__docs__get_version, mcp__deploy__status, mcp__deploy__run\r\n```\r\n\r\n## 变更内容\r\n\r\n| 组件 | s20 | s21 |\r\n|------|-----|-----|\r\n| 基础设施集成 | 各版本独立 | 13 个组件统一编排 |\r\n| Agent 循环 | 基础 tool_use 循环 | 7 阶段流水线 |\r\n| 工具数量 | 27 | 27（不变，继承 s07-s20 全部工具） |\r\n| 异常处理 | 无 | RetryWrapper 四类重试策略 |\r\n| 审计 | 无 | ToolLogger 落账日志 |\r\n| MCP 协议 | 无 | 5 个 MCP 工具 |\r\n\r\n## 试一试\r\n\r\n```bash\r\nmvn -pl hoppinzq-module-agent-comprehensive -am compile\r\nmvn -pl hoppinzq-module-agent-comprehensive exec:java -Dexec.mainClass=com.hoppinzq.agent.AgentComprehensive\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `创建一个任务，然后 spawn 一个 teammate 去执行它` — 期望：cron 和 task 系统协同工作\r\n2. `连接 MCP 文档服务，搜索 spring boot 相关文档` — 期望：MCP 工具正常工作\r\n3. `查看后台任务状态` — 期望：backgroundManager 正常运行\r\n4. `执行一个 bash 命令，查看 toolLogger 是否记录了日志` — 期望：每次工具调用都有落账\r\n\r\n观察重点：7 阶段流水线是否正确执行？cron 到期是否自动注入？权限检查和钩子是否按顺序触发？MCP 工具是否能正常连接外部服务？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s22",
    "locale": "zh",
    "title": "s22: Multimodal Input (多模态输入)",
    "content": "# s22: Multimodal Input（多模态输入）\r\n\r\n`s01 > ... > s21 > [ s22 ] > s23`\r\n\r\n> *\"让 AI 看见世界\"* — MessageParam 从纯文本升级为 text/image/file 多块结构。\r\n\r\n## 问题\r\n\r\n标准 agent 只能处理文本。read_file 读取图片返回的是乱码字节流，AI 无法理解图像内容。想让 AI 看图，你只能手动把图片 base64 编码，粘贴到提示词里。\r\n\r\n## 解决方案\r\n\r\n![Multimodal Overview](/agent/multimodal-overview.svg)\r\n\r\ns22 引入 MessageContent 多类型消息管道，让 Agent 能自动检测并发送图片：\r\n\r\n```\r\n+--------+      +------------------+      +-------+      +---------+\r\n|  User  | ---> | MessageContent   | ---> |  LLM  | ---> |  Tools  |\r\n| prompt |      | text / image     |      |       |      |         |\r\n| /img   |      | / file           |      +---+---+      +----+----+\r\n+--------+      +------------------+          ^               |\r\n                     |                       |               |\r\n                     | read_file auto-detect +---------------+ \r\n                     | (image/* → base64)    tool_result\r\n                     v\r\n              ContentBlockParam.image()\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. MessageContent — 多类型消息块\r\n\r\n```java\r\n@Data @Builder\r\npublic class MessageContent {\r\n    private String type;         // \"text\" | \"image\" | \"file\"\r\n    private String text;         // type=text 时\r\n    private String mediaType;    // type=image 时，如 \"image/png\"\r\n    private String data;         // type=image 时，base64 编码\r\n    private String filename;     // type=file 时\r\n\r\n    public static MessageContent text(String t) { ... }\r\n    public static MessageContent image(String mediaType, String base64Data) { ... }\r\n    public static MessageContent file(String filename, String extractedText) { ... }\r\n}\r\n```\r\n\r\n三种类型：\r\n- **text**：普通文本内容\r\n- **image**：mediaType + base64 数据\r\n- **file**：文件名 + 提取的文本内容\r\n\r\n### 2. read_file 增强 — 自动检测 MIME\r\n\r\nread_file 工具升级，用 `Files.probeContentType()` 检测文件类型：\r\n\r\n```java\r\npublic static String readFile(ReadFileInput input) {\r\n    Path path = Path.of(ROOT).resolve(input.getPath()).normalize();\r\n    byte[] bytes = Files.readAllBytes(path);\r\n    String mimeType = Files.probeContentType(path);\r\n\r\n    if (mimeType != null && mimeType.startsWith(\"image/\")) {\r\n        // 图片文件 → base64 编码\r\n        String base64 = Base64.getEncoder().encodeToString(bytes);\r\n        String size = bytes.length > 1024 * 1024\r\n            ? (bytes.length / (1024 * 1024)) + \"MB\"\r\n            : (bytes.length / 1024) + \"KB\";\r\n\r\n        if (AIConstants.SUPPORTS_VISION) {\r\n            // 支持视觉：返回 ContentBlockParam.image()\r\n            return MessageContent.image(mimeType, base64).toJson();\r\n        } else {\r\n            // 不支持视觉：降级为文本描述\r\n            return String.format(\"[图片: %s, %s, 路径: %s]\", mimeType, size, input.getPath());\r\n        }\r\n    }\r\n\r\n    // 文本或其他文件：原有逻辑\r\n    return new String(bytes, StandardCharsets.UTF_8);\r\n}\r\n```\r\n\r\n### 3. screenshot 工具 — 截屏\r\n\r\n新增 screenshot 工具，使用 Java AWT Robot 截取屏幕：\r\n\r\n```java\r\npublic static String executeScreenshot() {\r\n    if (GraphicsEnvironment.isHeadless()) {\r\n        return \"错误: 当前环境不支持图形界面（headless）\";\r\n    }\r\n    try {\r\n        Robot robot = new Robot();\r\n        Rectangle screen = new Rectangle(Toolkit.getDefaultToolkit().getScreenSize());\r\n        BufferedImage img = robot.createScreenCapture(screen);\r\n        ByteArrayOutputStream baos = new ByteArrayOutputStream();\r\n        ImageIO.write(img, \"png\", baos);\r\n        String base64 = Base64.getEncoder().encodeToString(baos.toByteArray());\r\n        return MessageContent.image(\"image/png\", base64).toJson();\r\n    } catch (AWTException e) {\r\n        return \"截屏失败: \" + e.getMessage();\r\n    }\r\n}\r\n```\r\n\r\nscreenshot 在 headless 环境自动返回友好错误，非 headless 环境返回 base64 PNG。\r\n\r\n### 4. 视觉 API 适配\r\n\r\nAIConstants 新增 `SUPPORTS_VISION` 标志：\r\n\r\n```java\r\npublic static final boolean SUPPORTS_VISION = false;  // deepseek-chat 不支持 vision\r\n```\r\n\r\nZQAgent 改造为区分两种模式：\r\n\r\n```java\r\nprotected List<ContentBlockParam> buildContentBlocks(List<MessageContent> contents) {\r\n    List<ContentBlockParam> blocks = new ArrayList<>();\r\n    for (MessageContent mc : contents) {\r\n        if (\"image\".equals(mc.getType()) && SUPPORTS_VISION) {\r\n            blocks.add(ContentBlockParam.ofImage(\r\n                ImageBlockParam.builder()\r\n                    .source(ImageBlockParam.Source.ofBase64(\r\n                        ImageBlockParam.Source.Source.ofBase64(mc.getData()),\r\n                        ImageBlockParam.Source.MediaType.of(mc.getMediaType())))\r\n                    .build()));\r\n        } else if (\"text\".equals(mc.getType())) {\r\n            blocks.add(ContentBlockParam.ofText(\r\n                TextBlockParam.builder().text(mc.getText()).build()));\r\n        }\r\n    }\r\n    return blocks;\r\n}\r\n```\r\n\r\n不支持视觉时，所有图片降级为 `[图片: type, size]` 文本描述。\r\n\r\n## 变更内容\r\n\r\n| 组件 | s21 | s22 |\r\n|------|-----|-----|\r\n| 消息类型 | 纯文本 | text/image/file 多块 |\r\n| read_file | 返回文本 | 自动检测 MIME，图片 base64 |\r\n| 新工具 | — | screenshot |\r\n| 视觉支持 | 无 | SUPPORTS_VISION 开关 + 降级 |\r\n| 新增类 | — | MessageContent |\r\n| 跨平台 | — | headless 检测，优雅降级 |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-22-multimodal\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent22\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `读取当前目录下的 test.png 图片` — 期望：read_file 检测到 image/png，返回 base64 编码的图片内容\r\n2. `截个屏` — 期望：screenshot 工具执行 Robot.createScreenCapture，返回 base64 PNG\r\n3. `帮我描述一下这张图片的内容` — 期望：图片被发送到 LLM（如果 SUPPORTS_VISION=true）\r\n4. `读取一个文本文件` — 期望：非图片文件走原有逻辑，正常返回文本\r\n\r\n观察重点：read_file 是否能正确检测图片 MIME？screenshot 在无头环境是否返回友好错误？SUPPORTS_VISION=false 时图片是否降级为文本描述？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  },
  {
    "version": "s23",
    "locale": "zh",
    "title": "s23: Security Sandbox (安全沙箱)",
    "content": "# s23: Security Sandbox（安全沙箱）\r\n\r\n`s01 > ... > s22 > [ s23 ]`\r\n\r\n> *\"信任但验证，执行前隔离\"* — 四层沙箱让 bash 命令从不可控变为可控。\r\n\r\n## 问题\r\n\r\nbash 工具是整个 Agent 最强大的工具，也是最危险的。一个 `rm -rf /` 或 `dd if=/dev/zero of=/dev/sda` 就能摧毁整个系统。s09 的权限系统可以拒绝特定工具，s10 的钩子可以在工具执行前注入检查，但它们都不能防止 bash 本身的破坏性。\r\n\r\n## 解决方案\r\n\r\n![Sandbox Overview](/agent/sandbox-overview.svg)\r\n\r\ns23 引入 SandboxRunner，按 L1 → L2 → L3 → L4 → 降级的顺序执行命令。每一层都是一种不同的防御策略：\r\n\r\n```\r\n命令输入\r\n    ↓\r\nL1 静态分析（黑名单 regex）→ 拒绝高危命令\r\n    ↓\r\nL2 目录监禁（Path.normalize）→ 拒绝路径越界\r\n    ↓\r\nL3 OS 沙箱（bwrap）→ Linux 命名空间隔离\r\n    ↓\r\nL4 容器隔离（docker）→ 完整容器沙箱\r\n    ↓\r\n降级 ProcessBuilder（带超时，cwd 锁定）\r\n    ↓\r\n命令输出\r\n```\r\n\r\n## 工作原理\r\n\r\n### 1. SandboxRunner — 四层编排器\r\n\r\n使用 Builder 模式配置：\r\n\r\n```java\r\nSandboxRunner sandbox = new SandboxRunner.Builder()\r\n        .workDir(Path.of(ROOT))\r\n        .enableOsSandbox(isLinux)      // L3 仅 Linux\r\n        .enableContainer(false)        // L4 默认关闭\r\n        .timeoutSec(30)\r\n        .build();\r\nString result = sandbox.run(\"ls -la\");\r\n```\r\n\r\n### 2. L1 StaticAnalyzer — 静态分析\r\n\r\n基于正则黑名单，命令发送前就拒绝：\r\n\r\n```java\r\npublic class StaticAnalyzer {\r\n    private static final List<Rule> RULES = List.of(\r\n        new Rule(\"rm-rf-root\", \"rm\\\\s+-rf\\\\s+/\", \"危险：递归删除根目录\"),\r\n        new Rule(\"mkfs\", \"\\\\bmkfs\\\\b\", \"危险：格式化文件系统\"),\r\n        new Rule(\"fork-bomb\", \":\\\\(\\\\)\\\\{.*\\\\}\", \"危险：fork 炸弹\"),\r\n        new Rule(\"curl-pipe-sh\", \"curl.*\\\\|.*sh\", \"危险：远程执行\"),\r\n        new Rule(\"dd-write-device\", \"dd\\\\s+if=.*of=.*\\\\bdev\\\\b\", \"危险：写入块设备\"),\r\n        new Rule(\"shutdown\", \"\\\\b(shutdown|reboot|halt|poweroff)\\\\b\", \"危险：系统关机/重启\"),\r\n        new Rule(\"raw-block-write\", \">.*/dev/sd\", \"危险：直接写入磁盘\"),\r\n        new Rule(\"chmod-root\", \"chmod.*777\\\\s+/\", \"危险：根目录权限修改\"),\r\n        new Rule(\"etc-passwd-overwrite\", \">.*/etc/passwd\", \"危险：覆盖密码文件\"),\r\n        new Rule(\"rm-system-dirs\", \"rm\\\\s+-rf\\\\s+/(etc|boot|dev|proc|sys)\\\\b\", \"危险：删除系统目录\"),\r\n        new Rule(\"wget-pipe-sh\", \"wget.*-O-.*\\\\|.*sh\", \"危险：远程执行\"),\r\n        new Rule(\"sudo-rm\", \"sudo.*rm\\\\s+-rf\", \"危险：提权删除\")\r\n    );\r\n\r\n    @Data @AllArgsConstructor\r\n    public static class Result {\r\n        private final boolean blocked;\r\n        private final String reason;\r\n        private final String ruleName;\r\n    }\r\n\r\n    public static Result check(String command) {\r\n        for (Rule rule : RULES) {\r\n            if (rule.pattern.matcher(command).find()) {\r\n                return new Result(true, rule.reason, rule.name);\r\n            }\r\n        }\r\n        return new Result(false, \"\", \"\");\r\n    }\r\n}\r\n```\r\n\r\n12 条规则覆盖最常见的危险操作：删除系统文件、格式化磁盘、fork 炸弹、远程下载执行、关机重启、修改系统密码文件等。\r\n\r\n### 3. L2 DirectoryJail — 目录监禁\r\n\r\n把命令限制在工作目录内，阻止路径穿越：\r\n\r\n```java\r\npublic class DirectoryJail {\r\n    private final Path jailRoot;\r\n\r\n    public String validate(String command) {\r\n        java.util.regex.Matcher m = PATH_TOKEN.matcher(command);\r\n        while (m.find()) {\r\n            String token = m.group();\r\n            Path resolved = jailRoot.resolve(token).normalize();\r\n            if (!resolved.startsWith(jailRoot)) {\r\n                return \"路径 '\" + token + \"' 越界 (解析为 \" + resolved + \")\";\r\n            }\r\n        }\r\n        return null; // null = 通过\r\n    }\r\n}\r\n```\r\n\r\n从命令中提取路径 token（`/`, `../`, `./` 等形式），解析为绝对路径后检查是否以监禁根目录开头。\r\n\r\n### 4. L3 OsSandbox — bwrap 沙箱\r\n\r\n仅 Linux 可用，使用 Bubblewrap 创建命名空间隔离：\r\n\r\n```java\r\npublic class OsSandbox {\r\n    public static boolean isAvailable() {\r\n        if (!System.getProperty(\"os.name\").toLowerCase().contains(\"linux\"))\r\n            return false;\r\n        try {\r\n            Process p = new ProcessBuilder(\"which\", \"bwrap\").start();\r\n            return p.waitFor() == 0;\r\n        } catch (Exception e) {\r\n            return false;\r\n        }\r\n    }\r\n\r\n    public static String run(String command, Path workDir, int timeoutSec) {\r\n        List<String> cmd = List.of(\r\n            \"bwrap\", \"--unshare-all\", \"--die-with-parent\",\r\n            \"--bind\", workDir.toString(), \"/work\",\r\n            \"--ro-bind\", \"/usr\", \"/usr\",\r\n            \"--proc\", \"/proc\", \"--dev\", \"/dev\",\r\n            \"--tmpfs\", \"/tmp\",\r\n            \"--chdir\", \"/work\",\r\n            \"bash\", \"-c\", command\r\n        );\r\n        return SandboxRunner.runWithTimeout(String.join(\" \", cmd), workDir, timeoutSec);\r\n    }\r\n}\r\n```\r\n\r\n`--unshare-all` 隔离网络/进程/IPC，`--bind` 只暴露工作目录，`--ro-bind /usr` 只读挂载系统工具。\r\n\r\n### 5. L4 ContainerIsolation — Docker 容器\r\n\r\n完整容器隔离，无网络、只读、限制资源：\r\n\r\n```java\r\npublic class ContainerIsolation {\r\n    public static boolean isAvailable() {\r\n        try {\r\n            Process p = new ProcessBuilder(\"docker\", \"info\").start();\r\n            return p.waitFor() == 0;\r\n        } catch (Exception e) {\r\n            return false;\r\n        }\r\n    }\r\n\r\n    public static String run(String command, Path workDir, int timeoutSec) {\r\n        List<String> cmd = List.of(\r\n            \"docker\", \"run\", \"--rm\",\r\n            \"--network=none\", \"--read-only\",\r\n            \"--memory=512m\", \"--cpus=1\",\r\n            \"-v\", workDir.toString() + \":/work\",\r\n            \"-w\", \"/work\",\r\n            \"ubuntu:22.04\", \"bash\", \"-c\", command\r\n        );\r\n        return SandboxRunner.runWithTimeout(String.join(\" \", cmd), Path.of(\"/\"), timeoutSec);\r\n    }\r\n}\r\n```\r\n\r\n`--network=none` 断网，`--read-only` 只读文件系统，`--memory=512m --cpus=1` 限制资源，防止容器内命令影响宿主机。\r\n\r\n### 6. 降级 — 带超时 ProcessBuilder\r\n\r\n前四层都不适用时的最终防线：\r\n\r\n```java\r\nstatic String runWithTimeout(String command, Path workDir, int timeoutSec) {\r\n    ProcessBuilder pb;\r\n    if (os.contains(\"win\"))\r\n        pb = new ProcessBuilder(\"cmd.exe\", \"/c\", command);\r\n    else\r\n        pb = new ProcessBuilder(\"bash\", \"-c\", command);\r\n    pb.directory(workDir.toFile());\r\n    pb.redirectErrorStream(true);\r\n    // ... 超时强杀\r\n}\r\n```\r\n\r\nWindows/Linux 自动选择 shell，cwd 锁定在 workDir，超时后 `destroyForcibly()`。\r\n\r\n### 7. 与 s09 + s10 的安全链\r\n\r\n```\r\n┌─────────────────────────────────────────────────────┐\r\n│               完整安全链                             │\r\n├─────────────┬──────────────┬────────────────────────┤\r\n│ s10 钩子     │ s09 权限      │ s23 沙箱              │\r\n│ PreToolUse   │ denyList     │ L1 静态分析            │\r\n│ 可插拔扩展   │ permission   │ L2 目录监禁            │\r\n│              │ rules        │ L3 OS 沙箱(bwrap)      │\r\n│              │ askUser确认  │ L4 容器隔离(docker)    │\r\n└─────────────┴──────────────┴────────────────────────┘\r\n```\r\n\r\n三层安全机制各司其职：\r\n- **s10 钩子**：工具执行前触发，可插拔扩展点\r\n- **s09 权限**：按黑白名单 + 用户确认决策\r\n- **s23 沙箱**：执行层物理隔离，即使工具被允许，命令也在沙箱中运行\r\n\r\n## 变更内容\r\n\r\n| 组件 | s22 | s23 |\r\n|------|-----|-----|\r\n| bash 执行 | 直接 ProcessBuilder | 四层沙箱编排 |\r\n| 安全机制 | 无 | L1 静态分析 + L2 目录监禁 + L3 OS + L4 容器 |\r\n| 新增类 | — | SandboxRunner, StaticAnalyzer, DirectoryJail, OsSandbox, ContainerIsolation |\r\n| 跨平台 | — | Windows 自动降级，Linux 可选 bwrap |\r\n| 超时控制 | 无 | 可配置超时 + destroyForcibly |\r\n| 错误信息 | 简单错误 | 分层拒绝原因（哪一层、哪条规则） |\r\n\r\n## 试一试\r\n\r\n```bash\r\ncd hoppinzq-module-agent-23-sandbox\r\nmvn clean compile\r\nmvn exec:java -Dexec.mainClass=\"com.hoppinzq.agent.Agent23\"\r\n```\r\n\r\n试试这些 prompt：\r\n\r\n1. `执行 rm -rf /tmp/test` — 期望：L1 静态分析拦截，提示 \"危险：递归删除根目录\"\r\n2. `列出 ../../etc 下的文件` — 期望：L2 目录监禁拒绝，提示路径越界\r\n3. `echo hello` — 期望：通过 L1+L2，降级到带超时 ProcessBuilder，正常输出 hello\r\n4. `执行 dd if=/dev/zero of=/dev/sda` — 期望：L1 静态分析拦截\r\n5. `执行 shutdown -h now` — 期望：L1 静态分析拦截，提示系统关机\r\n\r\n观察重点：危险命令是否被正确拦截？合法命令是否正常执行？拒绝信息是否包含具体的规则名和原因？Windows 下 L3/L4 是否自动跳过？\r\n",
    "githubUrl": "https://github.com/HOPPINZQ/hoppinai-agent",
    "downloadUrl": "https://github.com/HOPPINZQ/hoppinai-agent"
  }
]
